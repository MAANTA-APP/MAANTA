# MAANTA decisions log (repo mirror)

Notion holds the authoritative DECISIONS_LOG; this file mirrors the decisions
that are load-bearing for the codebase so sessions without Notion access don't
fly blind. When adding an entry here, add it to Notion too (or vice versa).

Format: date · decision · consequence in product/code.

## Frozen decisions

| Date | Decision | Where it bites in code |
|---|---|---|
| 2026-08-24 (fourth entry) | **Merchant self-onboarding no longer requires what3words. Browser geolocation is the primary self-serve location method, the coordinates the merchant confirms are MAANTA's canonical store-location data, and what3words is demoted to optional enrichment.** Founder ruling disposing of **D162**, where a lapsed what3words quota (HTTP 402 `QuotaExceeded`) made self-serve onboarding impossible to complete at all — a third party's billing state was holding the front door of the product shut. **The flow:** a "Locate my shop" action asks the device for a position **only when the merchant taps it** (`maximumAge: 0`, high accuracy — a cached fix from this morning's kitchen is the wrong answer); the pin is shown on a map; the merchant must **confirm** it before Continue unlocks; and what is submitted is the **confirmed** pin, which may be one they dragged, not the phone's first reading. **Nothing dead-ends:** a denied permission, an unavailable or slow fix, and a reading too coarse to identify one entrance all land on the same fallback — place or drag the pin on the map, or type coordinates. A coarse reading warns and opens the pin; it never refuses. **No continuous tracking** — there is no `watchPosition` anywhere in the flow, and the copy says so. **Field principle:** the merchant is expected to be at or immediately outside their own shop entrance when they tap; the objective is the shop door, not wherever the phone happens to be. **What is unchanged:** approval (`onboard_merchant` still inserts `status = 'pending'`; only the admin approve route flips it), the D158 verified-email and contact-channel rules, the KES 30 success fee, and the claim gate. what3words is now derived server-side from the confirmed pin, best-effort and time-bounded — quota, outage or a missing key all leave `what3words_address` NULL and onboarding completes. Restoring the quota restores enrichment, nothing more. | Migration `20260824120000_merchant_location_coordinates.sql` — `what3words_address` `DROP NOT NULL`, new `merchants_location_present` (coordinates OR three words) and `merchants_lat_lng_range` CHECKs, and `onboard_merchant` **dropped and recreated at 14 arguments** with `p_lat`/`p_lng` so the location is written in the same statement as the shop row (it was a post-insert UPDATE whose failure was logged and swallowed). Deliberately a drop-and-create, not an added overload: two overloads with defaults make every call ambiguous, the trap that broke `20260816020000` in CI. **Not applied to production** — Claude does not apply migrations; see `docs/ops/supabase-migrations.md`. Verified on a fresh database: the full 101-migration chain replays and **all 32 SQL suites pass**, including the new `supabase/tests/merchant_location_coordinates_test.sql` (8 scenarios: coordinate-only onboarding still `pending`, what3words-only still works, `location_required`, out-of-range/NaN/half-pair coordinates, direct-write constraints, exactly one RPC overload with grants intact, and a merchant unable to move another merchant's shop). App: one predicate module `src/lib/shop-location.ts` read by the wizard and `/api/merchants/onboard`; `src/app/merchant/onboard/locate-shop-step.tsx`; `src/components/merchant/shop-location-map.tsx`. Tests: `src/lib/__tests__/shop-location.test.ts`, `src/lib/__tests__/merchant-onboarding-geolocation.test.ts`, D162 block in `src/app/api/merchants/onboard/__tests__/route.test.ts`. Gates: lint, typecheck, 1115 vitest, build + three post-build gates, all green. **Merged and applied the same day as one operational action** (founder instruction: do not leave the merge-to-migration gap open): PR #268 merged as `14052ad` on a full green board — `ci` and a **real Supabase `db-tests` run** over the fresh 101-migration chain, both Cursor agents green with no findings — then `20260824120000` applied under explicit founder authorization minutes later. The apply minted `20260824163212` (**nine for nine**) and the ledger was repaired to the repo filename before anything else; it reconciles **101/101**. Read back: `what3words_address` nullable, `merchants_location_present` + `merchants_lat_lng_range` present, `merchants_contact_present` untouched, **exactly one** `onboard_merchant` overload at 14 args with `p_lat`/`p_lng`, grants intact after the DROP (anon no EXECUTE), 215 merchants / 353 users unchanged, no existing row lost its address. Smoke: a coordinate-only onboarding run against the live function inside a rolled-back block returned `status=pending`, `onboarding_mode=self_serve`, `what3words_address` NULL, and the no-location case still raised `location_required` — zero rows left behind, deliberately, so nothing can later be mistaken for Merchant 01. Drift **D162** stays `open` but is **no longer a Merchant 01 blocker**; it closes on live proof, not deployment. **D173** opened and closed (`convertTo3Words` ran unbounded) |
| pre-2026-06-30 | **KES 30 success fee** per verified redemption, all plans (Standard and Elite), charged at merchant verification. No price-review caveat. | `deduct_success_fee_or_record_arrears`; hardened in migration `20260702094145_harden_success_fee_amount.sql` with `30.00` fallback |
| pre-2026-06-30 | **Elite trial = 30 days**, then **7-day grace period**, then auto-downgrade to Standard if no paid conversion. Paid Elite **KES 3,500/month** (price under review Feb 2027). | `handle_trial_expiry` (migrations `20260701110443`, `20260701111223`) |
| pre-2026-06-30 | **Launch offer = the first 100 BBS Mall merchants get a 30-day Elite trial**; the KES 30 success fee still applies throughout the trial. Frozen in Notion "Frozen Scope & Rules"; recorded here 2026-07-30 by the truth audit, which found the rule mirrored nowhere in the repo despite being referenced in passing by the Node 0 opening-credit entry below. | **Enforced since 2026-07-30** — see the cap entry below. Trial granted by `activate_merchant` (`p_grant_elite_trial`) via `/api/admin/merchants/[id]/approve`, or by `grant-trial` on `/api/admin/plans/[id]`; expiry/grace/downgrade run in `handle_trial_expiry` |
| 2026-07-30 | **The launch-offer cap is enforced in the database**, not left to admin discipline. Closes open decision D2: the cap and the BBS-Mall scope previously existed only in the frozen rule and in public copy, while `activate_merchant` granted a trial on request with no counter or node check and `/api/admin/plans/[id]` bypassed the RPC entirely. `/pricing` advertises the cap publicly, so it has to hold. Two deliberate asymmetries: a **spent offer never blocks activation** (the merchant goes live on Standard, no error), while an **explicit admin grant raises** (they asked for this merchant specifically, so silence would be worse). A **downgrade does not free a slot** — otherwise the 100 could be recycled indefinitely. | Migration `20260730130000_enforce_elite_trial_first_100_cap.sql`: durable `merchants.elite_trial_granted_at` (stamped once, never cleared) + `app_config.elite_trial_merchant_cap=100` + `elite_trial_cap_status()` / `elite_trial_slot_available()` + trigger `trg_enforce_elite_trial_cap` on **every** path that sets `elite_trial_active` true. `activate_merchant` checks availability first and skips the trial gracefully. Demo and off-node merchants are outside the offer. Approve route now logs the **actual** outcome (`eliteTrialGranted`), not the request. Tests: `supabase/tests/elite_trial_cap_test.sql` (7 scenarios) |
| 2026-07-30 | **The feed defaults to the locked feed structure**, not to distance. The frozen three-rail order (flash = soonest expiry; Priority Placements = most recently boosted; All Active Deals = all-time verified redemptions descending) is the default; `Nearest` / `Newest` / `Ending soon` remain as explicit shopper overrides that apply to all rails. Closes open decision D1: the feed had defaulted to `sort=nearest`, re-sorting all three rails by distance from the **mall centroid** and discarding the locked orders — including the boosted placement Elite merchants pay KES 500/24h for. | Ordering applied server-side in `getLiveDeals` (`src/lib/data.ts`) via `lockedFlashOrder` / `lockedBoostedOrder` / `lockedStandardOrder` in `src/lib/deal-list-controls.ts`, so it lands in the 30s cache already ordered and the page cannot undo it. Boost order reads `boost_flags.starts_at` (`getBoostStartTimes`), degrading to newest-first rather than erroring the feed. Default is `DEFAULT_FEED_SORT`; Browse keeps `DEFAULT_BROWSE_SORT = nearest` (flat list, no rails). Tests: `src/lib/__tests__/locked-feed-order.test.ts`, plus locked-order cases in `get-live-deals.test.ts` |
| 2026-06-30 | **Redemption ticket expiry** = deal `expires_at` + 15 minutes (supersedes earlier rule). | `claim_deal` RPC comment, migration `20260702093134` |
| 2026-07-03 | **Zero-balance gate**: merchants with zero/negative balance cannot create new deals. Existing deals keep running; fees on their redemptions go to arrears. | migration `20260703190627_zero_balance_gate_deals.sql` |
| 2026-07-03 | **Verify-anyway**: if the fee charge status comes back `unknown`, the shopper's redemption still succeeds; a fraud-review task is created for admin/on-ground follow-up. Disputes are handled after the fact, auditably. | migration `20260703235152_unknown_fee_status_fraud_review_task.sql`; `/api/redemptions/verify` |
| 2026-07-05+ | **Stripe allowed as payment provider** alongside IntaSend; Stripe stays in **sandbox** during testing. M-Pesa STK via IntaSend is prepared for launch readiness but IntaSend availability is **not assumed**. | migration `20260705191128_allow_stripe_payment_provider.sql`; `src/lib/stripe.ts`, `src/lib/intasend.ts` |
| 2026-07-08 | **Multi-currency top-ups** (KES/USD/EUR/GBP) with live FX conversion to KES; failed webhooks logged to a failure table instead of dropped. | migration `20260708231241`; `src/lib/currency.ts` |
| 2026-07-09 | **All merchant money movements** go through one atomic, idempotent RPC (`record_merchant_ledger_entry`), keyed on unique provider reference. Refund/dispute idempotency keyed on Stripe `payment_intent`, not charge/dispute IDs. | migrations `20260709000151`, `20260709000211`; `src/lib/merchant-ledger.ts` |
| 2026-07-09 (this doc) | **BBS Mall is Node 0** and the sole proving ground until PMF. Shoppers, merchants, and mall operators are **separate audiences** from first signup (`segment_type` required). Notion = ops source of truth; repo = code source of truth; Drive/Obsidian = mirrors. | `docs/maanta-claude-operating-system.md` |

## Recent decisions

| Date | Decision | Where it bites |
|---|---|---|
| 2026-08-24 (third entry) | **D172 does not block Merchant 01. The automated browser E2E is DEFERRED, with a trigger: it becomes a hard gate before MAANTA moves from controlled Node 0 validation into routine or scaled releases.** Founder ruling, immediately after the D172 renumbering. **The state is a three-way distinction and should always be stated as one, because the binary version is misleading in both directions:** MAANTA **does** have automated database/RPC money-path coverage (E12 — `golden_path_test.sql`, `verify_redemption_money_path_test.sql`, run on a fresh chain by the `db-tests` job); it **has had** a successful **manual** browser E2E (the founder's full-role production run of 2026-08-23, including a staff-verified redemption and the KES 30 ledger read-back); and what it does **not** have is an **automated browser E2E actually executing in CI**. Only the third is missing. **Why it does not gate the pilot:** there is already substantially more direct production evidence than a one-merchant controlled pilot requires, and making browser E2E mandatory now would return the project to engineering at the exact moment the remaining uncertainty is market behaviour rather than correctness. **Why it gates later:** once deploys are routine and genuine customers depend on MAANTA, every production release should prove the golden path automatically instead of relying on someone remembering to run it by hand — frequency plus dependence is what converts this from a nice-to-have into a control. **No implementation task is authorized**; Claude is explicitly not to be given a D172 work item at this stage. **The pre-Merchant-01 board, as it now stands:** security adversarial audit **✅ CONDITIONAL GO** · fresh-DB / migration integrity **✅** · full manual production E2E **✅** · staff verification + KES 30 ledger **✅** · D172 automated browser E2E **🟡 deferred until scaled/routine releases** · **D162 what3words 🔴 must unblock** · **demo mode 🔴 must turn off** · genuine external merchants **0** · genuine external shoppers **0**. **The next spend of time or money is what3words, not CI.** Sequence once D162 clears: what3words working → paired **D14/D18** demo-off flip (DB flag and Vercel variable together) → verify synthetic content disappears → **Merchant 01 GO**. **A standing discipline comes with this ruling:** newly discovered engineering imperfections do not delay the field test. The system has already been shown to authenticate, isolate tenants, process a claim, let staff verify it and charge exactly one success fee. What remains is the question no test suite can answer — whether an independent merchant actually wants MAANTA, and whether an independent shopper will actually use it to walk into that merchant's shop. Changes no frozen business rule. | `docs/maanta-drift-register.md` (**D172** → `deferred`, trigger recorded); `docs/maanta-launch-readiness-tracker.md` (**E14** re-timed: GATE, but not before Merchant 01); Notion Launch Readiness, Decisions Log, Current State, OpTruth |
| 2026-08-24 (second entry) | **Duplicate drift identifier D168 resolved: the repository is canonical for drift IDs, and the Notion-only E2E finding is renumbered to D172.** Two different findings had been assigned **D168** by sessions that could not see each other — the security audit's inert tenant-isolation RLS policies (merged to `main` as part of PR #265) and a Notion-recorded finding that the authenticated dashboard E2E had never executed. **Ruling: the merged security D168 stays exactly as it is** — meaning, disposition and evidence unchanged, still "defer, and do NOT restore the `merchants` SELECT grant". The E2E finding takes the next genuinely unused canonical number, **checked rather than assumed**: the register on `main` holds D1–D171 with **no gaps**, and D172+ appeared nowhere in the repo or on either in-flight branch, so **D172** it is. The audit trail is preserved rather than erased — the row and every Notion reference state that it was *originally recorded as D168 in Notion and renumbered to D172 after the collision*. **The underlying E2E claim was independently re-verified against `main` @ `59160b1` before being recorded as fact, and it is CONFIRMED but NARROWED on three points.** Confirmed: `e2e.yml` gates its only job on `vars.E2E_BASE_URL != ''`, and the full Actions history reads **167/167 runs `skipped`, zero executions**, from run #1 (2026-07-24) to run #167 (the PR #265 merge itself). Narrowed: (1) the count is **167, not 166**, and rises on every push to `main` — cite the gate, not the tally; (2) on canonical `main` it is the **golden-path money loop** (browse → claim → ticket → verify → KES 30 → wallet), **not a "dashboard" suite** — authenticated, but asserting nothing about any dashboard. *In fairness to the original wording, `e2e/dashboards.spec.ts` does exist on PR #264's open branch, so "dashboard E2E" was accurate for the branch it was written against and simply is not what `main` holds; that branch-scoping is part of why the collision went unnoticed;* (3) **"MAANTA has never had authenticated E2E testing" is refuted** — what is missing is the *browser-level* proof, while the same money invariants are gated in CI at the RPC level by `golden_path_test.sql` and `verify_redemption_money_path_test.sql`, alongside 1061 vitest tests. And the framing softens: this is a **documented unprovisioned opt-in gate**, not a broken or deceptive one — the workflow says it "stays OFF until an operator provisions it", and two guards already exist against a false green (a preflight that **fails** when storage secrets are missing, and a `test.skip` in the spec). **Governance only: the E2E infrastructure was explicitly NOT fixed**, and E14 stays open with founder/ops as owner. Changes no frozen business rule. | `docs/maanta-drift-register.md` (**D172** opened; **D168** untouched); Notion Decisions Log, Current State (Start Here), OpTruth — every E2E reference renumbered |
| 2026-08-24 | **Security is CONDITIONAL GO for a tightly controlled Merchant 01 pilot, and six rulings dispose of every finding from the pre-field adversarial audit.** The audit attacked each boundary with real authenticated identities rather than reasoning about them — cross-tenant reads, direct RPC invocation with no UI in the path, privilege escalation, and a synchronised 8-way concurrent verification — and the money path survived with **exactly one KES 30 charge, one ledger row and one balance effect**. Founder framing: the result that matters is not "no criticals" but that the boundaries were actually attacked and held. Rulings: **(1) D168 — record and defer.** Do **not** repair it by restoring broad SELECT on `merchants`; the audit demonstrated that doing so resurrects the previous critical wallet/PII leak. It fails closed today. Resolve later with a deliberately designed isolation mechanism, not before Merchant 01. **(2) D169 — fix required before the next production migration is deployed.** Production is secure; a fresh deployment is weaker because `authenticated` receives INSERT/UPDATE/DELETE on `merchant_transactions`. Latent configuration drift on a money table is not left standing, but it does not delay Merchant 01 — it gates the *next* migration, with a ratchet. **(3) SEC-C — record/defer**, low priority while the privileges are genuinely unreachable through the application; this does not open a hardening cycle. **(4) SEC-D — investigate before changing schema.** Because email participates in identity, understand exactly what the duplicate group represents first; do **not** blindly add `UNIQUE(email)`, which could conflict with legitimate, null or historical states given this product's identity/relink history. **(5) D171 — defer.** An unenforced `is_blacklisted` is a product/governance inconsistency, not a reason to hold Merchant 01; decide what blacklisting is supposed to prevent before wiring it in. **(6) D170 — ruled: require staff acceptance before access becomes active.** An owner may invite an email, but the invited person must not silently become merchant staff merely because their address exists in MAANTA. For Merchant 01 / Staff 01 this may operate under explicit in-person consent, recorded operationally; the productized acceptance flow is required **before scaled staff-seat issuance**, not before the first controlled test. **The pre-field gate is therefore: Security CONDITIONAL GO ✅ · D162 what3words must unblock · demo mode off for genuine field measurement · then Merchant 01 GO.** Two non-security conditions are explicit: D162 is now the practical blocker — there is no value sending the first independent merchant into a journey already known to be uncompletable — and demo mode must be switched off before genuine field measurement, because 208 synthetic merchants visible while observing Merchant 01 and Shopper 01 contaminates the experiment; the shopper should see the actual Node 0 marketplace even if it initially contains one shop. **Market baseline untouched: 0 genuine external merchants · 0 genuine external shoppers.** The successful internal E2E proves MAANTA can execute the transaction safely; Merchant 01 begins proving whether anybody wants it. Auditing stops here — no further rounds, and no fixes were authorized alongside this record. Changes no frozen business rule. | `docs/security/pre-field-adversarial-audit-2026-08-24.md`; drift **D168**, **D169**, **D170**, **D171** (all opened and dispositioned here), **D162** and **D14**/**D18** named as the two non-security gate conditions |
| 2026-08-23 (sixth entry) | **Owner phone is OPTIONAL in self-serve merchant onboarding when the authenticated account already has a verified email; it stays available as an optional business contact.** Founder ruling on **D158 option B**, following the email-primary ruling of 2026-08-22 (sixth entry): with Clerk phone sign-in deferred, requiring a phone to onboard contradicted the only production auth path and forced the E2E merchant through the admin-assisted route. The exemption is **derived server-side** from `users.email` — written from `verifiedPrimaryEmail()` alone and frozen by D142, the same proof D154 links staff seats on — and is never read from the request body; the wizard's Continue button reads the same predicate purely as a convenience. A phone that IS supplied is still format-checked as Kenyan on this merchant-authored path (the admin route stays deliberately wider). **A shop can never hold zero contact channels**: when no phone is given the account's verified address becomes the shop contact, and a DB CHECK enforces it. Unlike D154 this weakens no guard — `merchants.phone` is contact detail and a top-up prefill, not an access-control input. | Migration `20260823130000_merchant_phone_optional_with_verified_email.sql` (phone `DROP NOT NULL`, `merchants_contact_present` CHECK, `onboard_merchant` normalises blanks and raises `contact_required`) — merged as `fec320e` (PR #258, full CI green including `db-tests` on a real fresh 100-migration chain) and **applied to production 2026-08-23** under explicit founder authorization. The MCP apply minted `20260823134241` (eight for eight); the ledger was repaired to the repo filename and reconciles **100/100** by full version+name diff. Read back: `phone` nullable, the CHECK present, exactly ONE `onboard_merchant` overload, 214 merchants / 353 users unchanged. **D158 closed.** Still owed: one real self-serve onboarding at Node 0. One predicate in `src/lib/merchant-onboarding.ts` read by both `/api/merchants/onboard` and `onboard-wizard.tsx`. Tests: `supabase/tests/merchant_phone_optional_test.sql`, `src/lib/__tests__/merchant-onboarding-phone-optional.test.ts`, D158 block in `src/app/api/merchants/onboard/__tests__/route.test.ts` |
| 2026-08-23 (sixth entry) | **Full-role production E2E run — admin, merchant owner, shopper, merchant staff and founder console — and the loop closes on email-only identities with a staff seat charging the fee.** 43 surfaces exercised across four accounts and six sign-in codes, measured against a parallel codebase audit that mapped every route per role. Money read-back: `success`, Guardian `clear`, `success_fee_charged` 30.00, balance 300 to 270, arrears 0. D25 confirmed live two ways (pause removed the deal from `deals_public_browse` instantly; production `claim_deal` contains the `deal_paused` gate). Re-claim refused 409; staff permission matrix correct; `POST /api/staff` as non-owner refused 403; counter Cancel charged nothing. **Seven defects found.** Two fixed and deployed mid-run: the what3words failure was invisible (a provider refusal was reported as the operator mistyping, unlogged) and 'Submitted now ago'. **D162 is a blocker and is billing, not code** — what3words returns HTTP 402 `QuotaExceeded`, so self-serve merchant onboarding cannot be completed at all. **D164 is the sharpest new find**: `Claims (7d)` reads 0 forever on both the admin console and the Founder dashboard because it filters `redemptions.created_at`, a column that does not exist — and `redemptions` records no claim timestamp at all, so claim-to-visit conversion, the most important early Node 0 signal, is invisible and fails silently. Also opened: D165 (owner-only staff form rendered to non-owners; API boundary holds), D166 (deal limit refuses only after the full wizard), D167 (three copy defects). Two of my own claims were withdrawn after retesting rather than left in the register. **Not proven:** agent surfaces (no agent account exists), cofounder (zero holders by design), reject-code, override, zero-balance gate, top-up rails, Elite grant. **Still a software proof, not field evidence — the Success ladder stays at 0.** | `docs/skills/full-role-e2e-2026-08-23.md`; drift **D162**, **D163**, **D164**, **D165**, **D166**, **D167** |
| 2026-08-23 (fifth entry) | **The staff-test rows were removed from production, and every test artefact from this session is now gone.** One FK-ordered transaction scoped to the shop: guardian event, redemption, deal, staff seat, both ledger rows and the merchant, with `kpi_counters` by cascade — read back 0 for each, surrounding data untouched (214 merchants, 353 users). Roles restored: `aragagency@gmail.com` and `aragagency+staff1@gmail.com` are `customer` again, each demotion guarded on owning no merchant AND holding no seat. Kept: the admin audit rows and all three user identities (deleting identities is what re-creates the D108 hazard). **Knowingly accepted: the staff-verified `success` is gone too** — non-demo `success` redemptions are back to 0, and both proofs (owner-verified §3b, staff-verified §3d) survive only in `docs/skills/node0-closing-pass-2026-08-22.md`. What remains live is the code and schema, not the data: the D154 email-invite path is deployed on `main`, its migration applied, ledger 99/99. | `docs/skills/node0-closing-pass-2026-08-22.md` §3e |
| 2026-08-23 (fourth entry) | **The counter loop is proven on email-only identities, staff included.** After D154 merged to `main` (`586c832`) and deployed, a seat was invited with the phone left blank and linked on first email sign-in — `merchant_staff.user_id` set, role promoted to `merchant_staff`, **`users.phone` still NULL**, permissions exactly as invited, one `users` row, verify keypad reachable. Then the full loop ran with three email sign-ins and no phone anywhere: merchant published Deal 02, the shopper claimed it, and **Staff 01 — not the owner — verified at the counter**: CODE VALID → −KES 30 → confirm → `status = success`, Guardian `clear`, `account_balance` 300 → 270, arrears 0.00, ledger `topup 300.00 | success_fee -30.00`. The `can_verify` permission granted at invite is what let the seat charge the frozen fee, and it behaved identically to the owner path. Still a software proof, not a Node 0 field result — **the Success ladder stays at 0**. | `docs/skills/node0-closing-pass-2026-08-22.md` §3c/§3d; drift **D154** |
| 2026-08-23 (third entry) | **The E2E test rows were removed from production on founder instruction ("remove the test rows").** Deleted in one FK-ordered transaction, each statement scoped to an exact id: the guardian event, redemption `dbdbd178…`, deal `39000f70…`, both `merchant_transactions` rows (the `topup 300.00` and the `success_fee -30.00`) and merchant `6c0f9c84…`, with `kpi_counters` by CASCADE; read back 0/0/0 with the surrounding data intact. **`admin_ops_log` rows were kept** — they are an admin audit trail, not test data. **The owner's role was restored to `customer`**, because deleting the shop left the account a `merchant_admin` owning nothing, a state the app has no screen for. **Consequence to accept knowingly: MAANTA's first non-demo `success` redemption no longer exists as a row.** The proof now lives only in `docs/skills/node0-closing-pass-2026-08-22.md` §3b and this log — that was the trade the instruction made, keeping production counts honest at the cost of the artefact. Noted while cleaning: production still holds **3 non-demo `pending` redemptions from 2026-08-14** (Apple-relay account, against demo shops) that predate this session and were left alone. | drift **D152**; `docs/skills/node0-closing-pass-2026-08-22.md` §3b cleanup |
| 2026-08-23 (second entry) | **Staff seats accept a verified EMAIL as well as a verified phone — D154 ruled option B and implemented.** Founder instruction: *"change staff seats phone only to email optional aswell to test staff seats next"*. Under the email-primary ruling the staff seat was the last phone-only door: `merchant_staff.phone` was NOT NULL, the invite API demanded a phone, and a seat linked to a person only when a Clerk-verified `users.phone` matched — NULL for every account while the Clerk phone attribute is off, so Staff 01 could not be onboarded at all. Now: `email` added to `merchant_staff` (nullable, lower-case CHECK, UNIQUE per merchant), `phone` nullable, and a CHECK requiring at least one channel so an unlinkable seat cannot be created; the invite form and route take either; `getMerchantContext` matches by email if the phone match found nothing — **phone is tried first, so every existing phone-invited seat behaves exactly as before**. **No security relaxation:** this reuses D124's argument rather than loosening it — `users.email` is written only from `verifiedPrimaryEmail` (Clerk-verified or nothing) and frozen against its holder by D142, and the lookup still matches only an unclaimed seat, so the seat-hijack guard is intact. Guarded by `staff-seat-email-linking.test.ts` (5 cases, verified to fail 3/5 with the branch removed); suite 1033 green, build clean. **Applied to production the same day** on explicit founder authorization: MCP apply, then read-back — column present, `phone` nullable, both CHECKs and both indexes live. The apply minted its own version once more (**nine for nine** — `20260823054259`), repaired to the repo filename `20260823120000` and re-read; **the ledger reconciles 99/99**. **D154 closed.** Nothing in the database now blocks Staff 01 — the remaining dependency is D156, whether the invite address can receive its code. Changes no frozen business rule. | migration `20260823120000_staff_seat_email_invite.sql`; `src/lib/merchant.ts`; `src/app/api/staff/route.ts`; `src/app/merchant/(app)/staff/new/page.tsx`; test `src/lib/__tests__/staff-seat-email-linking.test.ts`; drift **D154** |
| 2026-08-23 | **The production loop is proven end to end on email-only authentication — MAANTA's first non-demo `success` redemption, with the frozen KES 30 fee debited correctly.** Run by the founder from Oslo: admin sign-in → merchant sign-in → identity read-back (both resolved to their existing `public.users` rows, no duplicate) → shop created and activated (opening credit `topup 300.00`, Elite-trial slot deliberately **not** consumed) → Deal 01 live in `deals_public_browse` → new shopper provisioned cleanly on first sign-in → claim (OTP issued, `amount_kes` 400 snapshotted) → counter verify showing **CODE VALID / −KES 30 / wallet after KES 270** before charging → **`status=success`, Guardian `clear`, `account_balance` 300 → 270, `outstanding_arrears` 0.00, ledger `topup 300.00 \| success_fee -30.00`**. **This is a software proof, not a Node 0 field result** — no BBS Mall merchant, no real shopper, no physical visit; the Success ladder stays at **0** and the field HOLD is unchanged. **Two blockers surfaced and were fixed live by the founder:** **D157** — Supabase's third-party auth still pointed at the Clerk *development* instance, so every authenticated RPC on production was failing `PGRST301` (claim, tickets, favourites, counter verify) and had been since the 08-16 instance change, unnoticed because nobody had completed a signed-in write since; adding `https://clerk.maanta.app` fixed it mid-run. **D156** — Clerk's shared sender delivers nothing to Microsoft mailboxes (four emails sent per Clerk's own log, zero received at hotmail.co.uk, while Gmail got five in the same hour); under email-primary auth that locks out any Outlook/Hotmail participant, and the fix is a Clerk custom email domain on `maanta.app`. Also opened: **D158**, self-serve onboarding still requires a phone (the shop was created by the admin-assisted path instead) — a usability observation, not a blocker. **D152 closed.** Cleanup owed: the test shop, deal and redemption are non-demo rows and will show in real counts until the founder rules on keeping or removing them. | `docs/skills/node0-closing-pass-2026-08-22.md` §3a/§3b; drift **D152** (closed), **D156**, **D157** (opened+closed), **D158** |
| 2026-08-22 (sixth entry — email is primary; phone deferred) | **Supersedes the fifth entry's "re-enable phone" ruling. Clerk phone sign-in is behind a paid plan; email authentication is available without that upgrade. For Node 0, MAANTA does not require or purchase Clerk phone authentication merely to satisfy the historical phone-first design. Email becomes the primary production authentication method for the controlled Node 0 pilot; phone/SMS is deferred** until field evidence and economics show MAANTA needs it — i.e. Kenyan users demonstrably prefer or need it, not because an old specification says so. **D152 updated:** "phone re-enabled" removed from the close condition; production email-code/passwordless auth must work; password must not become a substitute requirement if email-code supports the flow; existing Clerk identities preserved, no replacement users; attach/verify the email on existing users where required; prove email auth resolves to the correct existing `public.users` identity with no D108 duplicate; re-run the E2E by email (admin → merchant → shopper → identity read-back → deal → claim → verification → KES 30 ledger). **D151 reclassified deferred / non-blocking** for Node 0 — SMS/phone authentication is unavailable under the current Clerk plan; the operator is not told to wait for SMS. **Staff seats inspected, not changed (founder instruction: identify and report, do not silently widen):** the staff-seat flow is hard phone-only — `merchant_staff.phone NOT NULL`, the invite API requires a phone, and a seat links only on a Clerk-verified `users.phone` match — so Staff 01 cannot be onboarded under email-primary auth without a code change; recorded as **D154** for a founder decision. The owner can still verify at the counter, so the Merchant 01 → Deal 01 → Shopper 01 → `success` loop is runnable with the owner verifying. **Field state unchanged:** prospecting GO; live pilot HOLD only until the email-based D152 E2E proves the production identity path. **Standing constraints:** no purchase/enablement of a paid Clerk feature, no new identities, no unrelated auth changes without founder approval. Changes no frozen business rule. | drift **D152** (close condition rewritten), **D151** (deferred), **D154** (new); `CLAUDE.md` § "Operating state" field-status block; Notion OpTruth, Decisions Log, Nairobi Field Operations |
| 2026-08-22 (fifth entry — D152 rulings) | **Four founder rulings on the production Clerk instance (D152 is an auth-configuration problem, not a code problem).** (1) **Re-enable Phone Number** on the production instance — existing users were created phone-first and disabling phone removed the identifier they were anchored to. **Do not recreate those users.** (2) **Remove "Password required"** — MAANTA's canonical auth model is OTP/passwordless (verification by code/contact); a mandatory password would introduce a second auth model the product was not designed around. (3) **Keep the existing Clerk user ids.** For the three known accounts, email sign-in is added by attaching the email to the **existing** Clerk user and verifying it properly — never by signing the address up as a new user (that re-creates the D108 duplicate-identity problem). (4) **Enable email-code/passwordless sign-in in production** so a genuinely new shopper can establish a verified email identity without depending on SMS — the claim gate already accepts a verified email. **E2E re-run order once applied:** admin sign-in → merchant sign-in → shopper sign-in → verify the same Clerk/user identities → merchant deal path → shopper claim → redemption → KES 30 ledger read-back. A phone-delivery failure during this is **D151 evidence, not a stop** if email works. **Field operating state corrected:** D152 blocks live account onboarding, staff seats and redemption testing — **not** field reconnaissance or merchant conversations. Field prospecting: **GO**. Live merchant onboarding / staff seat / redemption: **HOLD** until D152 closes. D151 SMS testing resumes once phone auth is re-enabled. Email fallback is a valid shopper claim path once production email sign-up/sign-in is verified. Changes no frozen business rule. | drift **D152** (close condition rewritten to these rulings); `CLAUDE.md` § "Operating state" (field-status block); Notion Nairobi Field Operations HOLD reworded (prospecting GO) |
| 2026-08-22 (fourth entry — operating state) | **Node 0 Field Validation Mode entered: product design and general engineering are frozen** unless field evidence at BBS Mall shows a genuine blocker or defect. No UI polish, redesign, feature work, speculative UX, security-audit rounds, refactors or architecture work on the basis of opportunity alone; completed work is not reopened to improve it. Claude Design is dormant (wakes on field-evidenced UX problems); Claude Code is a maintenance/verification engineer (wakes on demonstrated technical problems or specifically authorized work); the Nairobi field operator is the primary evidence source and the founder is the decision layer. Verified email stays an accepted claim path; D151 runs separately and does not block. D39 and other founder items authorize no unrelated engineering. D106's rule stands for production: reconcile `main`, the prod ledger and observed behaviour before any mutation, and mutate only with explicit founder authorization. Field-issue protocol: preserve evidence → reproduce → classify (blocker / defect / usability observation / feature request) → fix only blockers and approved defects → regression guard → verify → return to rest. Observations are not converted to features without founder approval. Changes no frozen business rule. The same instruction explicitly authorized one bounded closing pass: a codebase↔repo↔Notion sync, a final security audit, and a production end-to-end test with founder-controlled accounts. | `CLAUDE.md` § "Operating state: Node 0 Field Validation Mode" |
| 2026-08-22 (third entry) | **Two rulings closing out the claim-gate change. (1) `0 verified` stays visible on a deal card — it is not hidden, badged as `New`, or omitted for new shops.** Founder: *"the 0 verified will be kept in place for honesty and trust"*. At pilot start every shop reads zero, which was raised as possibly suppressing first claims; the ruling is that a shopper is never shown a track record a shop has not earned, and the count is worth showing later precisely because it was never inflated — the same principle the ranking rests on (a deal rises because someone actually walked in). Reversing this needs a new entry. **(2) The D151 SMS measurement is owned by the field operator**, not the founder — it is field work, done from real handsets in the test cities. Recorded with its prerequisite: the test must run against a Clerk **production** instance, and production currently serves the development one (D99), so provisioning that is a founder/eng step which must come first — otherwise the operator re-measures the same failure and learns nothing | `docs/ops/deal-card-kpis-2026-08-22.md` (open question → ruling, with the three non-fixes named); `docs/ops/field-operator-day-sheet.md` (new standing task, with the prerequisite and what each outcome means); drift **D151** (owner → field operator) |
| 2026-08-22 (second entry) | **The claim gate accepts a verified CONTACT CHANNEL — phone OR email — superseding the S2 phone-only gate of 2026-07-23, "for now", pending an OTP provider.** Founder ruling, in session: the product must be testable in **Oslo, London and Nairobi**, and Clerk is not delivering SMS to Norwegian, Kenyan or UK numbers, so an SMS-only claim gate blocks the entire test programme. Under the Clerk strategy an email sign-in **is** an emailed one-time code, so a verified email is proof of channel control of the same kind the SMS OTP gives. **This is a widening of the channel, not a removal of the gate:** a session with no verified channel at all is still refused, and the anti-fraud intent — every claim traces to a channel the shopper proved they control — is unchanged. Explicitly time-boxed: revisit once an OTP provider is in place. **Related finding, recorded as D151:** production is measured on the Clerk *development* instance (D99), which is the likeliest cause of the SMS failures; if a production instance restores SMS, this ruling should be re-examined rather than assumed permanent | `maanta-app/src/lib/launch-auth.ts` (`PHONE_REQUIRED_AT_CLAIM` → `VERIFIED_CONTACT_REQUIRED_AT_CLAIM`), `maanta-app/src/lib/auth.ts` (`currentUserHasVerifiedEmail` / `currentUserHasVerifiedContact`), `maanta-app/src/app/api/redemptions/route.ts` (the single enforcement point), `/verify-phone` copy; guards `launch-auth.test.ts`, `redemptions/__tests__/route.test.ts`, new `verified-contact-claim-gate.test.ts`; drift **D151** |
| 2026-08-22 | **Visual refresh direction ruled: Direction A ("Editorial calm") for every surface — founder instruction "i want version A for everything", chosen from the 22-board directions canvas** (both canvases recorded in `docs/ops/ui-visual-refresh-directions-2026-08-21.md`; Direction B "Market board" rejected everywhere, and the per-role-mix option not taken). Direction A = stone ground, **borderless white cards on `shadow-card`**, editorial type, image-forward leads, price anchored beside the action — all **within** the frozen UI rules, which this decision does not touch. Implementation proceeds surface-by-surface through gated PRs. **Slice 1 shipped in this change:** the app-wide card idiom `rounded-card border border-line bg-white` → `rounded-card bg-white shadow-card` across shopper, merchant, admin, agent, founder and auth surfaces (92 occurrences, 50 files); hairline dividers, form inputs and the FilterDropdown popover deliberately keep their borders, and the marketing site flips in its own slice. Guard: `maanta-app/src/lib/__tests__/direction-a-cards.test.ts` (walks every non-marketing surface and fails if the bordered idiom returns) | `maanta-app/src/components/ui/claude/deal-card.tsx`, `maanta-app/src/components/ui/cards.tsx`, 46 surface files; the guard test; `docs/skills/claude-design-system.md` |
| 2026-08-20 | **The two all-real duplicate-email pairs are resolved (founder instruction: "resolve the duplicate email pairs"), and the verified-email relink is now deterministic for every address in production.** Executed as a measured data operation, not a guess. **Evidence first**: activity counted across all eleven FK paths into `public.users`. The admin pair was one-sided — keeper `d8c1aa1e-f89e-4aeb-8ec1-e42b06101409` holds 29 `admin_ops_log` rows, 2 favourites, the merchant onboarding attribution and the live-instance sub, while the shadow `16cb15b5-22b5-41e7-8a9a-cc47deb3cd8f` holds zero ops and one inert `agents` seed reference (the FK that already made deletion off-limits). The customer pair had **zero activity on both rows**, so provenance decided: keeper `bf197f6e-4d94-4bcb-bf15-fe4511371080` (Clerk row, carries the person's name, matches the live auth strategy); loser `2373fca4-955b-474d-a4c1-68f9a205ce94` (Supabase-email-OTP-era row, no Clerk sub ever, `auth_uid` deliberately left intact). **The operation**: `email` set NULL on exactly the two loser rows — no delete, no role change — through the service_role claim the D142 trigger admits, each UPDATE guarded on the expected prior email. **Read back**: both keepers are the sole real carriers of their addresses; **zero email addresses on more than one real row**; the surviving duplicate group is the founder's address on one real admin plus one demo customer, which the fallback's `is_demo` filter already resolves and the never-touch-demo-data rule keeps untouched. Same sitting: **PR #236 merged** through `main` on its own green CI (squash `5b910bc`) — the D108/D142 closes and the 97/97 ledger record are now on `main`. **The controlled pilot's identity gates are all clear**: code live, migration applied, duplicates resolved. What remains is physical — the SKANDI SKAN staff seat and the first real verification. | Production `axrrslqssmbngbataejg` (`public.users` — two email retirements, read back), `docs/maanta-drift-register.md` (D108 residual note), `docs/maanta-decision-queue-2026-08-19.md` (§0 note updated). No code changed — the enforcement (verified-only, single-match, hard-fail) is untouched; this cleared the data the hard-fail was protecting |
| 2026-08-19 | **PR #235 merged on the fresh green CI, production verified serving it from `main`, and the founder authorized the MCP apply of `20260819200000` — the identity chain is live end to end; D108 and D142 closed.** The merge gate was the actual CI result, not the expectation: the re-run on `e48f249` completed green including `db-tests`, and only then was the squash merge made (`693ca47`). Production picked it up the right way and this was read back rather than assumed: the Vercel deployment carries `target: production`, ref `main`, the merge SHA, READY at 22:43 UTC — and the served `/manifest.webmanifest` now reads "Launching at BBS Mall, Eastleigh" with `start_url` `/app-bootstrap` and the raster icons resolving as real PNGs, so D138, D141 and D93's repo half are confirmed on **served output**, not source. **The apply, by the book**: founder instruction "bypass claude and apply supabase migrations" (22:49 UTC) — the third authorized MCP apply round, seventh apply overall. Ledger read first per D121 (96 rows, high-water `20260818160000`, exactly one pending file); the apply minted `20260819225106` — **seven applies, seven minted versions** — repaired to the repo filename per D86; read-back confirmed the `email` clause, the four-column error message, owner-only EXECUTE, the trigger wiring and the comment; the full ledger-to-repo diff reconciles at **97/97**. The fallback ran unfrozen on production for roughly eight minutes (deploy READY 22:43, freeze live 22:51), in demo mode, with no sign-up activity since 2026-08-16. **What this leaves for the controlled pilot**: founder-owned data and ops only — resolve the two all-real duplicate-email pairs, invite SKANDI SKAN's first real staff seat, run the first physical verification. | Production `axrrslqssmbngbataejg` (function + ledger read-backs), Vercel production deployment of `693ca47`, `docs/maanta-drift-register.md` (D108 and D142 closed), `CLAUDE.md` (97/97, seven-for-seven), `docs/maanta-decision-queue-2026-08-19.md` (§0 superseded note, Q1 answered). No product code changed by this entry — the code was PR #235's, logged in the fifth-session entry |
| 2026-08-19 | **The external review's three verified gaps are registered as founder-owned gates, its refuted finding is recorded as refuted, and every gate is classified controlled-pilot vs public/commercial launch — no product behaviour changed, no open question resolved.** The review (assessed against live production the same day) contributed three genuinely new items, now rows and tracker gates: **D143** (the `unknown` fee path's task message instructs a reconciliation that has no sanctioned rail to land in the ledger — accounting semantics are a founder ruling before anyone implements; gates commercial launch), **D144 / gate O7** (the published privacy policy promises "Erasure — within 14 days" and a retention table nothing implements — the operational package O5/O6 do not cover; gates public launch), **D145 / gate O8** ("Supabase managed backups on" is claimed and unverified, and no restore has ever been tested — a backup never restored is an assumption; gates commercial launch), plus **D146 / gate O9** (the site's most-published compliance sentence, "ODPC registration is in progress", has no owner and its truth is unestablished from the repo; Notion check owed; gates public launch). **Its sharpest P0 was refuted by read-back and the correction is recorded where it counts**: demo isolation is enforced in the SQL view itself — `deals_public_browse` carries `(NOT d.is_demo OR is_demo_mode())` on both deal and merchant — so §9's "demo could leak at the data/view layer" is wrong, the flip closes discovery at the database, and the remaining launch risk is precisely **D18**'s pairing of the two switches. The four-step flip verification (both switches → redeploy → real event tags `is_demo=false` → anonymous shopper retrieves zero demo rows) is now in `docs/ops/demo-mode-runbook.md`, and D18's row is annotated with the classification: **gates public launch; the controlled pilot runs with demo mode ON**, as every rehearsal has. **Classification, stated once**: the controlled pilot (one merchant, one real staff seat, one real shopper, one verified redemption) is gated by nothing in this entry — its gates are PR #235 merged through `main`, migration `20260819200000` applied, and the two duplicate-email pairs resolved (D108). O7/O9 and the D14/D18 flip gate PUBLIC launch; D143 and O8 gate COMMERCIAL launch. **Explicitly not resolved**: D143's accounting semantics, the privacy package's content, the ODPC state, and every previously queued ruling. | `docs/maanta-drift-register.md` (D143–D146 opened; D18 annotated), `docs/maanta-launch-readiness-tracker.md` (gates O7–O9), `docs/ops/demo-mode-runbook.md` (flip verification). Verified same-session: `deals_public_browse` definition, redemption census (3 real, all pending, 0 success), privacy-policy promises at `maanta-app/src/content/legal/privacy-policy.md`, backup claims in `docs/maanta-staged-readiness-now-launch-10k-100k.md`. no code changed |
| 2026-08-19 | **Founder ruling A (decision-queue Q1): `ensureAppUser` falls back to a Clerk-verified email when the JWT `sub` matches no row — verified-only, single-match, hard failure on ambiguity, never a closest match.** This is D108's prevention half. A Clerk `sub` is scoped to the instance that minted it; after an instance change (which D99 measured actually happening on this product) every returning person used to get a silent second empty account or, once D129 populates `users.phone`, a `users_phone_key` violation and no account at all. The fallback relinks them into their own row instead. **The rule, precisely**: the caller's primary email must be verified on the **current** instance (current control of the mailbox is the trust anchor — the same model as email-based account recovery everywhere); the match pool is **real rows only** (`is_demo = false`); exactly one match relinks by overwriting the dead `clerk_user_id` and re-entering the normal path so the phone backfill runs in the same sign-in; zero matches insert; **two or more matches return null** — loud, and never a guess. The insert path now stores only the verified email, D126's rule applied to the column the moment it became an identity key. **Measured before implementing, and it changes what to expect**: production carries three duplicate-email groups — two real admin rows on one address (almost certainly D108's path (a) having already fired on the D99 instance change), the founder's address on a real admin plus a demo customer (resolved by the demo filter), and one customer address on two real rows. The two all-real pairs will hard-fail the fallback until an admin retires or merges one row of each — a data decision this session did not take. **Companion hardening, written not applied**: `email` joins the D124 trigger's guarded set (migration `20260819200000`), because an unfrozen, non-unique relink key lets a shopper manufacture the exactly-two-rows ambiguity against a target — D142. Local `make db-verify` could not run (no Docker in this session); the CI `db-tests` job on the PR is the verification, per the D74 precedent. | `maanta-app/src/lib/auth.ts` (`verifiedPrimaryEmail`, the relink, verified-only email at insert), `maanta-app/supabase/migrations/20260819200000_freeze_users_email_identity.sql`, `maanta-app/supabase/tests/users_identity_immutable_test.sql` (email scenarios). Guards: `maanta-app/src/lib/__tests__/clerk-email-relink.test.ts` (**proved non-vacuous — deleting the fallback fails 8 tests**), instance-change end-to-end in `maanta-app/src/lib/__tests__/staff-seat-linking-chain.test.ts`. Drift **D108** (updated, closes on deploy), **D142** (opened, closes on migration read-back) |
| 2026-08-19 | **The four no-ruling items from the decision queue's §5 are done — D140, D141, D31 closed; D82's standalone half shipped.** Picked because they were the easiest to implement that needed no founder call; everything in the queue's ranked sections still waits on its ruling, and nothing here touches one. **D141**: the manifest `start_url` contract is guarded — a test maps app-router URLs to page files (route groups elided, the same resolution Next performs) and asserts `start_url`, the `/login` bounce, and every `destinationForRole` target resolve; a one-character `start_url` typo turns it red. **D140**: all 20 unrooted `frames.json` `frontend` entries normalised to one `maanta-app/`-relative convention, each resolved mechanically rather than retargeted by hand, and the interim three-form resolver deleted in the same change — a lenient matcher kept beyond its purpose teaches the next author that any spelling is fine. **D82 (standalone half only)**: the /you profile editor's Save is now gated until-dirty, computed from all three editable fields against the same parsing the editor seeds from; the founder half — whether 16c's phone/email editing ships — stays open behind D59. **D31**: the reinterpretation instruction in the 2026-07-30 branch audit is replaced with the position stated once (only the ungoverned free-month wording was withdrawn; the governed first-100 trial stands) and the authoritative source named (`OFFERS.eliteTrial` in `facts.ts` + this log). A sweep found no other live document carrying the claim — and found that the second document the earlier sweep note blamed was a **false positive** (its "removed entirely" is about a Suspense boundary), corrected in the register the same day. **Deliberately not done**: Q3's `is_demo` constraint, although the queue calls it "not even a judgement call" — it is a migration, this session has no Docker daemon or Supabase CLI, and a migration that cannot pass `make db-verify` does not ship from here. It stays queued for a session that can run the gate. | `maanta-app/src/lib/pwa/__tests__/app-bootstrap.test.ts` (+3 contract tests), `maanta-app/design/current-reality/frames.json`, `maanta-app/src/lib/__tests__/frames-inventory.test.ts` (resolver → `existsSync`), `maanta-app/src/app/(shopper)/profile/profile-card.tsx` + `maanta-app/src/lib/__tests__/profile-save-gate.test.ts`, `docs/skills/repo-branch-audit-2026-07-30.md`. All four guards proved non-vacuous by mutation |
| 2026-08-19 | **The constraint moved from engineering to rulings, and the queue is written down.** The register carries 36 open rows; **23 are founder-owned** and three more are eng-owned with a ruling blocking the engineering half. `docs/maanta-decision-queue-2026-08-19.md` ranks them with each question stated so it can be answered in a sentence, the evidence that makes it answerable, and the options with their consequences. It is a **derived view, not a second register** — it carries no state, closes nothing, and is dated in its filename so it goes stale visibly rather than quietly competing with the register (**D27**'s lesson, applied to this document before it could repeat it). **Ranked first, and the only item with a deadline: D108's prevention half** — whether `ensureAppUser` should fall back to a Clerk-**verified** email when the JWT `sub` matches no row. Measured live the same day: 10 Clerk-linked rows, **9 on path (a)** (silent second account) and 1 on path (b), that one being the seeded demo admin; all 9 real users are path (a); production still serves `main @ 5040acda` and D129's commits are previews with `target: null`, so **the decay has not started**. It starts at the first sign-in with a verified phone after D129 deploys. **Correction to how that risk was first written, from this session:** "worse" splits into two axes that point opposite ways. Path (b) is *louder* — `prod-auth-deals-recovery.md` is right that a `users_phone_key` violation is easier to diagnose — and *harder to repair*, because path (a) leaves a duplicate row to harvest the new `sub` from while path (b) never inserted and needs a Clerk dashboard lookup per person. For a shopper at a counter, "no account at all" is worse than "signed in but empty". At 9 users that repair is an afternoon; at 200 pilot shoppers it is not. **Nothing is resolved by this entry** — D129 stays on its branch, and the sequencing recommendation is to settle the prevention half and ship the two together, through `main` and never a promote. | `docs/maanta-decision-queue-2026-08-19.md` (new), `docs/maanta-drift-register.md` (D108 refined with the measured split and both axes; header links the queue), `CLAUDE.md` (orientation map row). No code changed |
| 2026-08-19 | **The web app manifest is generated code, not a static file, because a static file cannot be told the company is not trading yet.** `public/manifest.webmanifest` carried "Now live at BBS Mall, Eastleigh" — a string the pre-launch guard's own regex matches — and the guard never saw it, because its coverage was defined as two `.tsx` directories under `src/`. The manifest description is what the Android install prompt renders at the moment someone installs, on a surface `PrelaunchNotice` cannot follow, so it is the worst-placed instance of that claim rather than the least. **Two decisions.** (1) The manifest moved to `src/app/manifest.ts` and takes its description from a new `SITE_DESCRIPTION` in `live-claims.ts`, which the root metadata now also uses — the same sentence had been a `DEMO_MODE` ternary in one file and a frozen post-launch literal in the other, which is the one-claim-two-literals shape D87 exists to prevent. The served URL is unchanged (`/manifest.webmanifest`), verified in the build, so no installed client sees a moved path. (2) The guard now **enumerates `public/`** rather than naming files, per the D52/D38 rule that a hand-maintained list only checks what someone remembered; and the root-description case was rewritten to assert the resolved constant instead of splitting `layout.tsx` on `DEMO_MODE`, a mechanism that would have gone vacuously green the moment the ternary moved — which this change moves. | `maanta-app/src/app/manifest.ts` (new), `maanta-app/src/lib/marketing/live-claims.ts`, `maanta-app/src/app/layout.tsx`; `public/manifest.webmanifest` deleted. Guard: `maanta-app/src/lib/__tests__/prelaunch-consistency.test.ts`, **proved non-vacuous in both placements** — the exact original string fails the suite whether it is restored to `public/` or written into the generated manifest. Drift **D138**, in the **D46**/**D87** family |
| 2026-08-19 | **D93's repo-side half shipped, and the install funnel is recorded as unproven rather than working.** The manifest now declares both icon purposes at SVG **and** raster 192/512, generated from the shared mark by a route handler rather than committed as binaries — the reason `apple-icon.tsx` already gives, that a committed binary is a second copy nothing checks. `appleWebApp` metadata ships too, so the iOS home-screen title is "Maanta" instead of the truncated document title. Per **D114** the rasters draw the *previous* shield, not the newly supplied artwork that still awaits a square opaque export: rasterising the mark that already ships changes the format, not the brand, so this does not pre-empt that decision. **What was deliberately not done:** no service worker `fetch` handler (that is **D95**, founder-owned, and building one to make an install prompt appear would be answering a product question with a workaround), and no device measurement, which is not a repo task. The measurement and its consequence are split out as **D139** so shipped icons are never read as a working funnel. | `maanta-app/src/app/icons/[icon]/route.tsx` and `maanta-app/src/lib/brand/app-icons.ts` (new), `maanta-app/src/app/manifest.ts`, `maanta-app/src/app/layout.tsx`. Guard: `maanta-app/src/lib/__tests__/brand-icons.test.ts` — every declared raster must be one the route can produce, and the route may never reference a `/brand/` asset while D114 is open |
| 2026-08-19 | **Every open drift row was re-verified against HEAD, and the register was corrected where it read worse than the code.** Twenty-five rows swept; the sweep changed nothing in the codebase and closed nothing, because a sweep is evidence, not a ruling. The material corrections: **D83** is partly fixed — the IntaSend null-reference double-credit and the amount reconciliation are both implemented and only the static-secret authentication remains, so the row should not be read as a live double-credit; **D93** described a manifest with one icon and no apple touch icon, and both were already untrue when re-read; **D26** claimed `frames.json` contained no drift reference at all when it contained several, though its conclusion still holds because the one `design-ahead` surface still cites none; **D86**'s "no standing procedure existed" is no longer true. **D108** moved in the other direction and is the one worth acting on: **D129**'s phone backfill shifts the population from its silent-second-account path toward the hard-lockout path, so its measured 8-of-9 split is now decaying and must be re-measured before any Clerk instance work. **D94 closed** on its own stated smallest fix: `/download` and `/app-bootstrap` added to the canonical surface inventory, which had omitted the entry point of the installed product. | `docs/maanta-drift-register.md` (25 rows swept in place, D94 closed, D138–D140 opened), `maanta-app/design/current-reality/frames.json`. Guard for D94: `maanta-app/src/lib/__tests__/frames-inventory.test.ts`, which also asserts every `frontend` citation resolves — and found two more that did not |
| 2026-08-19 | **Staff-seat linking is repaired at its actual break point: `public.users.phone` is now backfilled on sign-in, NULL-only.** The column was written once, at provisioning. An email-first signup therefore had no verified phone to write, so it was written NULL and never revisited — verifying a phone in Clerk afterwards did not backfill it. That column is what `getMerchantContext` matches a pre-invited `merchant_staff` seat on, so the NULL short-circuited the link: a shop assistant invited by phone signed in, verified their number to get past the claim gate, and reached the counter as an ordinary shopper with no verify keypad — nothing errored, nothing logged. Measured on production the same day: **all 10 real `users` rows had `phone` NULL, and no staff seat existed on the one real merchant**, so the path had never run against real data. **What was decided beyond the obvious fix.** (1) The backfill is **NULL-only, never an overwrite** — the column is an access-control input frozen against its holder by the D124 trigger, and a change of number is an admin identity event, not a side effect of signing in. (2) The value is **canonicalised through `normalizeStaffPhone`**, the same function the staff-invite route stores the other side with, so the two sides of the match are canonical by contract rather than by both happening to be E.164; D127 closed by observing the link had worked "by luck, not by contract", and one shared canonicaliser is what removes the luck. (3) The backfill is attempted **only when the caller asked for the `phone` column**, keeping a Clerk Backend API round trip off the claim, favourites and push paths for the phone-less majority; the coupling is pinned by a test rather than left implicit. Deliberately not done: no data migration — there is no real staff row to backfill, and every affected user self-heals on their next signed-in request. | `maanta-app/src/lib/auth.ts` (`ensureAppUserFromClerk` backfill; `verifiedPrimaryPhone` now returns the canonical form), `maanta-app/src/lib/merchant.ts` (trust-chain comment corrected — the column now has two writers, both through `verifiedPrimaryPhone`). Guards: `maanta-app/src/lib/__tests__/staff-seat-linking-chain.test.ts` (the real `ensureAppUser` → `getAppUser` → `getMerchantContext` chain, **proven non-vacuous** by reverting the fix and watching it fail), plus `phone-backfill.test.ts` and `merchant-staff-linking.test.ts`. Drift **D129**, closing the chain **D124** → **D126** → **D127** |
| 2026-08-19 | **`/search` filters paused deals — the last shopper discovery surface that did not** (drift **D119**, diagnosed 2026-08-18). Pausing a deal removes it from discovery immediately; `/feed`, `/browse` and `/map` inherited that from `getLiveDeals` and the `deals_public_browse` view, while `/search` built its own query with no `is_paused` predicate, so a paused deal stayed findable by name — and both `/feed` rails link into `/search`. No money was ever at risk: `claim_deal` raises `deal_paused` and the route returns 409, so no claim succeeded. The defect was that a merchant who paused a deal was still advertising it, and the shopper's dead end arrived at the tap rather than never being offered. | `maanta-app/src/app/(shopper)/search/page.tsx` — `.eq("is_paused", false)` on both the title-match and shop-name-match selects, matching `selectLiveDealBucket`. Guard: `maanta-app/src/lib/__tests__/search-paused-filter.test.ts`, behavioural rather than a source grep — it executes the page and records the filters each query applies, so a fix to one select cannot pass for a fix to both |
| 2026-08-19 | **Three docs corrected on the `unknown` success-fee path, and the live trust formula written down for the first time.** `verify_redemption`'s `EXCEPTION WHEN OTHERS THEN v_fee_status := 'unknown'` handler writes **only** an `agent_tasks` row — no `merchant_transactions` row at all — and its own message says the fee "was neither charged nor recorded as arrears". Three repo docs presented a two-outcome model instead, one of them instructing an operator to validate "one real redemption is exactly one fee ledger entry" against production, a check that fails legitimately on that path. Corrected, with the three-outcome table stated once. The Notion pages carry the same wording and the correction is owed there too. Separately, `recalculate_trust_metric` — `(0.5×R)+(0.3×A)−(0.2×F)`, `is_visible` below 0.50, `is_featured` above 0.90, a `retraining` task on the first crossing — was documented nowhere in the repo, so it is now recorded from a production read-back. | `docs/skills/payments-rails.md` (Rail 3 three-outcome table), `docs/skills/money-trust-engineering-guardrails.md` (§2), `docs/maanta-production-rollout-plan.md` (money-path invariant 2), `docs/skills/redemption-disputes.md` (the trust metric as it actually runs). Drift **D130**. Not a behaviour change — verify-anyway is untouched |
| 2026-08-18 | **Shopper deal categories: ten buckets, attached to the DEAL, not the merchant.** Founder ruling, from the annotated `/feed` screenshot asking for category filtering. Both halves are load-bearing. **Ten buckets** — Fashion & fabric / Beauty & perfume / Food / Phones & electronics / Shoes & bags / Home & living / Jewellery & watches / Health & pharmacy / Kids & baby / Services. **Ruled in two steps the same day, and the second step is the interesting one.** It was first locked at three — the three the pilot merchants sell (Nuur Fashion House, Bilan Beauty & Cosmetics, Macmacaan Sweets & Café). Building against three immediately produced the counter-evidence: four of the sixteen demo catalogue items (a phone screen protector, wireless earbuds, a prayer mat, a 24-inch suitcase) fit no bucket at all, and were left uncategorised rather than mis-filed. That gap was raised as **D117** rather than papered over, and the founder resolved it by widening the set to ten — explicitly "at least 8 to 10" — rather than by forcing the orphans somewhere wrong. The original three keep their keys **and** their labels, so no stored row changes meaning and no relabelling is implied. The orphans now sit where a shopper would look: `electronics`, `home`, `shoes`. The demo catalogue has zero uncategorised items and spans nine of ten buckets — nothing at BBS Mall is a Health & pharmacy deal, and inventing one would put a shop on the demo feed that does not exist. **Attached to the deal**: a fabric shop that sells snacks at the counter files each deal where a shopper would look for it, and a merchant who changes what they sell does not silently re-file every deal they have ever run. **Uncategorised is a first-class state, not a migration debt**: every deal that exists predates the column, and back-filling them by guessing from titles would put real deals in the wrong bucket and make the filter lie. They stay visible under All and under no chip; the column is NULLable in SQL and required in the merchant wizard, so the uncategorised set only shrinks. Two consequences ruled at the same time: the chip row is **withheld entirely** when it cannot change what is on screen (no categories, or every live deal in one bucket) — which is also what makes the feature safe to ship ahead of its migration; and a category that empties the feed says so in its own words rather than reusing "No deals live right now", which would tell a shopper the mall is dead when it is their own filter. Separately, **`Category` stopped being the label on a deal-type control**: `/map`, `/feed` and `/browse` all called their Flash/Boosted/Standard dropdown "Category" or "Filter", which was harmless only while the product had no categories. All three now read "Deal type" | Taxonomy: `maanta-app/src/lib/deal-categories.ts` (keys are stored, labels are copy — one array, and adding a bucket is one entry here plus one value in the SQL CHECK; a test fails if only one of the two is widened). Because the migration had never been applied, the widening is an edit to it rather than a follow-up — **but if it is ever found to have been applied while it still listed three keys, editing it is not enough**: the ledger treats it as done, `db push` skips it, and production refuses seven of the ten keys. Migrations **not applied — human `db push` step, drift D116**: `maanta-app/supabase/migrations/20260818150000_deal_categories.sql` (column + named ten-key CHECK + `deals_public_browse` recreate carrying the column and re-asserting the pause predicate) and `20260818160000_demo_reseed_categories.sql` (demo catalogue). Write path: merchant wizard details step, `/api/deals` validation, the deal edit sheet as the correction path, and `/api/deals/repost` carrying a snapshot's category forward. Read path: `DEAL_SELECT`, `/feed` and `/browse` through the shared `maanta-app/src/components/browse/deal-category-chips.tsx`. Everything touching the column degrades while it does not exist (`maanta-app/src/lib/deal-category-column.ts`, `selectDealsWithMerchants`) so a missing column costs the category and never the feed or the deal. Tests: `maanta-app/src/lib/__tests__/deal-categories.test.ts` (47), `maanta-app/src/components/__tests__/deal-category-chips.test.ts` (5), `maanta-app/src/components/__tests__/category-filtering.test.ts` (6, renders the real component), `maanta-app/supabase/tests/deal_categories_test.sql`. Drift **D116**–**D118** opened, **D117 closed** by the widening; **D119** opened for a separate finding — `/search` has no `is_paused` filter; **D120** opened and closed for three defects an adversarial review of the diff found — a merchant edit reporting a discarded category as saved, an empty state telling shoppers the mall was empty when a filter emptied it, and no guard on the filtering itself |
| 2026-08-17 | **The `/merchants` counter walkthrough is illustrated too — founder instruction extending the 2026-08-16 decision to the merchant audience.** Same terms: drawn rather than screenshotted, that page only. The four existing steps keep their copy and each gains a drawing of the screen it describes. **The decision worth recording is the amber one.** This is the first marketing illustration to carry an amber fill — exactly one, the drawn `Confirm redemption — KES 30 fee` button — because the panel's whole purpose is to show that a single named deliberate action is the only thing that charges, and the fee is on screen before it. Removing that amber to keep the page palette tidy would delete what is being taught. It is `aria-hidden`, non-interactive and inside a bordered device panel under an Illustration caption, so it is not a second action competing with the page CTA. Frozen rule 1 is unbroken; the guard pins the count at exactly one so this reasoning cannot be reused for a second element | `maanta-app/src/components/marketing/MerchantWalkthrough.tsx`, mounted in the "How it works at your counter" section of `maanta-app/src/app/(marketing)/merchants/page.tsx`. Panels drawn from `maanta-app/src/app/merchant/(app)/deals/new/new-deal-wizard.tsx`, the shopper's `claimed-code.tsx` card, `maanta-app/src/app/merchant/(app)/redeem/redeem-keypad.tsx` and its `disclose` screen with `FeeDisclosure`'s exact wording. Third surface on drift row **D50**, still reading the shared `maanta-app/src/lib/marketing/sample-deals.ts`, so no new invented names. Guards in `maanta-app/src/lib/__tests__/marketing-hero-shot.test.ts`: disclosure, `sr-only`, money never amber, no `KES` in the code card, the fee-disclosure copy and fee-bearing button label, `FACTS.successFeeKes` never typed as a literal, and exactly one amber fill |
| 2026-08-16 | **The `/shoppers` walkthrough is illustrated, not screenshotted — founder decision on the second synthetic-content marketing surface.** `marketing-hero-shot.test.ts` asserted `HeroShot` was Home-only and recorded that adding an illustrated mockup to another page "needs its own decision rather than inherited silently". The founder was asked, with the alternatives priced, and chose **drawn illustrations over real screenshots**, **shopper flow**, **`/shoppers` only**. The reason screenshots were declined is the durable part: a capture taken today shows **demo data** — 213 of 214 merchants are `is_demo` — so it would present the same invented shops while *looking* like genuine evidence, which is weaker honesty than a labelled illustration rather than stronger. It also costs no image bytes on mall wifi, restyles with the tokens, and cannot go stale unnoticed, since no build gate can detect a stale PNG. Consequence for any later session: a real capture becomes the right answer only once BBS carries real deals, and at that point the caption and drift row **D50** both go — replacing an illustration with a screenshot of seed data is not that moment | `maanta-app/src/components/marketing/ShopperWalkthrough.tsx` draws the three real screens (feed row, the claimed-code card from `maanta-app/src/app/(shopper)/tickets/[id]/claimed-code.tsx`, the `status === "success"` verified state); mounted in the existing "Three steps" section of `maanta-app/src/app/(marketing)/shoppers/page.tsx`, whose step copy moved into it verbatim so a text rail and a picture rail do not state the same three steps twice. Both illustrated surfaces now read invented rows from one `maanta-app/src/lib/marketing/sample-deals.ts`, and the walkthrough follows `SAMPLE_DEALS[0]` through all three panels, so it introduced no new invented names. Guards in `maanta-app/src/lib/__tests__/marketing-hero-shot.test.ts`: disclosure, `sr-only` description, money never amber, no `KES` inside the code card (frozen rule 6, checked in rendered HTML too), no amber fill outside the status dot (frozen rule 1), and no invented name retyped outside the shared list |
| 2026-08-16 | **The agent console is the agents' lite admin console: onboard and monitor the pipeline — founder ruling, superseding the design-side R4 posture.** Design Brief v1.4's R4 note (2026-08-03) recorded "no agent-facing product surface exists... building one is a separate session; any document listing `/agent/*` is describing routes, not a product." The founder ruled the console in: agents use `/agent` (dashboard + weekly target), `/agent/leads` (pipeline), `/agent/leads/new` (capture) and the lead detail as their working surface. Scope stays narrow by design — leads and onboarding handoff only, no money surfaces, no admin queues, and an agent sees only their own leads. Onboarding remains **merchant-authored**: the new "Onboard this shop" affordance on an unconverted lead opens `/merchant/onboard?shop=<name>` for the agent to hand the device to the owner, who signs in and submits themselves; the wizard's agent question records the assist (frozen attribution model, #68). The agent never becomes the submitter | `maanta-app/src/app/agent/leads/[id]/page.tsx` (the handoff), prefill consumed by `maanta-app/src/app/merchant/onboard/page.tsx` (`?shop=`, survives the login redirect). Guard: `maanta-app/src/lib/__tests__/agent-console-lite.test.ts` pins the handoff link, that the wizard reads the prefill, the hand-the-device-over copy, and that the handoff stays off the screen's amber budget (Link to merchant is the one amber action). Design side owes a `frames.json`-adjacent update reclassifying `/agent/*` from "routes, not a product" |
| 2026-08-16 | **The new-merchant opening-credit wallet state shows only while the credit is unspent — founder ruling closing the open half of D105.** Design Brief v1.4 §9 specifies the state and its copy but scopes it to a "new merchant" without saying when a merchant stops being one. The state shipped 2026-08-15 on the narrowest honest reading — visible while the ledger holds an opening-credit row and no `success_fee` or `success_fee_arrears` row — because the specified sentence claims "your first N verified redemptions covered" and that claim expires at the first charge. The founder closed the question on the shipped behaviour rather than commissioning a second sentence, so **there is deliberately no partly-spent state**: a merchant who has redeemed once sees the ordinary wallet, and the credit remains visible as its ledger row. Consequence for any later session: showing this notice past the first success fee would make the product state something untrue, and needs a new ruling plus new copy from the brief, not a predicate tweak. The companion half of D105 — that the covered count is derived from `app_config`, never typed — was ruled the same way on 2026-08-15 | `hasUnspentOpeningCredit()` in `maanta-app/src/lib/merchant-ledger-copy.ts` is the predicate; the count comes from `openingCreditRedemptionsCovered()` over the granted row amount and the `app_config` success fee. Rendered last in the wallet's state chain (`maanta-app/src/app/merchant/(app)/wallet/page.tsx`) so a real warning always wins, in the neutral `info` variant of `InlineAlert` rather than the rust warning tone. Tests: `maanta-app/src/lib/__tests__/merchant-ledger-copy.test.ts` pins the unspent-only trigger, the derivation at three fee/credit combinations, and that none of the brief's three numerals appear in code |
| 2026-08-09 | **Wireframe sections §1–12 are frozen as historical record; §13–17 are the build sections — founder ruling on the two-error-reds conflict, boundary settled at §12|§13.** Raised by the 2026-08-09 design-side reconciliation: the legacy error red #E8431F circulated in the wireframe canvas — sometimes as message text — while the shipped product uses #8C1D18. Founder ruled freeze-over-retokenise; the ranges as first stated overlapped at §13–15, and the design-side audit settled it: §13–15 are current-reality build sections and carried 28 uses of the retired red across 15 frames, so they were **retokenised** (fills/borders → #8C1D18, message text → ink), not bannered — annotating 15 frames as exceptions would have taught the retired red fifteen times. §1–12 carry the do-not-build banner on every section. The same pass then caught the deeper violation with a computed-style audit: the build sections used **#8C1D18 itself as message text 21 times** (source grep found 15; 6 more were inherited styles invisible to grep), including 16k's SLA countdown — all message text is now ink, with borders, chip fills and icon glyphs intact, which is exactly the shipped rule (error red is borders/icons/chips only, never copy). Consequence for any repo session handed a wireframe frame as input: **a §1–12 frame is history, not spec — colours included**; build from §13–17 or the shipped surface. Repo fact underpinning all of it: `flame` (#8C1D18) in `maanta-app/tailwind.config.ts` is the only error red and #E8431F appears nowhere in app source, so no code change follows | No repo change — the token layer already agrees (`flame: "#8C1D18"` in `maanta-app/tailwind.config.ts`; error-usage rules ratcheted by `maanta-app/src/lib/__tests__/frozen-ui-rules.test.ts`). Enforcement of the freeze and the retokenisation is design-side: banners on §1–12, the §0 and §16 boundary notes, and the `github.md` receipt. Design-side method lesson recorded there and worth mirroring: for colour rules, computed style is the authoritative audit — source grep under-reports inherited colour |
| 2026-08-09 | **The favourites surface is the My deals Shops tab — founder ruling on D79; frame 16a is superseded.** The shipped 8ab tab stands as the shopper's saved-shops list: shop rows with floor, the verified-redemptions count (the frozen "ranking is verified redemptions" rule — deliberately not 16a's live-deal count), an unfollow heart, and the "No saved shops yet" empty state; /you's "Your favourites" section stays the pointer into it. Frame 16a's separate `/you/favourites` page is not to be built — a second list of the same thing is drift by construction. Closes **D79** | Guard: `maanta-app/src/lib/__tests__/favourites-surface.test.ts` — pins the Shops tab's shape (merchant_favourites query, unfollow control, verified count, shop links, ruled empty copy), the /you pointer, and asserts `/you/favourites` does not exist, so building 16a without a new ruling fails CI. Inventory: `/my-deals` surface added to `maanta-app/design/current-reality/frames.json` with 16a marked superseded |
| 2026-08-09 | **Rail 3 keeps its name and its order — founder ruling on D77.** "Deals near me" stays the rail's shopper-facing title (frozen by R2, same day) and the locked order stays all-time verified redemptions per merchant (D1). The label is copy, not a sort description: the rail is scoped to the shopper's selected mall (its subtitle is "Standard deals at your mall"), so "near me" holds at rail scope, while placement *within* the rail stays the earned-placement ranking the product promises merchants. No distance re-sort, no rename. Closes **D77** | Order pinned by `maanta-app/src/lib/__tests__/locked-feed-order.test.ts` (locked rail 3); name pinned by `maanta-app/src/lib/__tests__/rail-names.test.ts` (the R2 copy guard) — both were already standing, so the ruling changes prose, not behavior. Docblocks in `maanta-app/src/lib/deal-list-controls.ts` and `rail-names.test.ts` updated from "open question" to the ruling |
| 2026-08-09 | **Feed rail names are shopper-facing frozen copy — founder ruling R2** (design brief v1.4, delivered via the 2026-08-09 design-sync brief). The shipped feed titles — "Top picks near you", "Neighbourhood favourites", "Deals near me" — are the rail names everywhere a user sees a rail named, merchant surfaces included. Notion's locked-structure labels (Flash / Priority Placements / All Active Deals) survive only as the internal `flash` / `boosted` / `standard` rail identifiers, never as UI copy. This resolves **D66**'s three-way question in the first direction: the UI copy stands, and the merchant boost surfaces — which billed a merchant under "Priority Placements", a name no shopper ever sees — were the outliers. Rail *orders* are untouched (they are D1's, still locked). Deliberately **not** resolved by R2: rail 3 is titled "Deals near me" but ordered by all-time verified redemptions, and that label/order pairing stays an open founder question — recorded as **D77** so closing D66 does not lose it | Merchant boost surfaces renamed to the shopper-visible rail name: `maanta-app/src/app/merchant/(app)/deals/[id]/deal-actions.tsx` (10e boost sheet), `.../deals/[id]/page.tsx` (active-boost banner), `.../plan/upgrade/page.tsx` (10h Elite benefit), `.../alerts/page.tsx` (boost-ended fallback). Stale internal references updated in `deal-list-controls.ts`, `locked-feed-order.test.ts`, `cards.tsx`. Guard: `maanta-app/src/lib/__tests__/rail-names.test.ts` — pins the three feed titles and bans the retired name from app source (comment-stripped via the shared D38 lexer). Closes **D66** |
| 2026-08-07 | **Three rulings from the dead-code/governance engagement, plus the day's state.** (1) **Disclosure spec wired in, not retired** (drift D75): placeholder regulatory identifiers render through `<PlaceholderId>` and the DECIDED 2026-07-31 `RegulatoryStatus` block now actually renders — footer legal bar and `/merchant-terms` above clause 7 — with the transposed `DEMO-ODPC-…` literal (which silently defeated the `/-DEMO-/` launch net) deleted. (2) **`formatKes` mass-adoption is a won't-do**: 41 inline `KES {n.toLocaleString("en-KE")}` sites stay as they are, because the shared helper rounds first and a blind swap is not output-identical — inline formatting at money sites is the accepted pattern, recorded here so the "do later" item stops resurfacing. (3) **`escapeHtml` consolidated on the superset**: one shared helper (escapes `&<>"'`); the only output change is apostrophes in contact-form emails arriving as `&#39;` in HTML source, identical on screen. Same day: dead-code PR #176 merged to `main` (−260 lines, behavior-identical, plus the frozen extras line single-sourced through `extrasLine()`); the **D73** per-node cap reland and the **D74** cofounder read-policy layer landed in the repo as `20260807160000`/`20260807161000`, both **pending the human production apply**; the fifth branch-promote (D76) was caught by the D71 tripwire in ~38 minutes and rolled back the same day | `maanta-app/src/lib/__tests__/prelaunch-disclosures.test.ts` (D75 guard); `maanta-app/src/lib/escape-html.ts`; `maanta-app/src/lib/pricing.ts` `extrasLine` + `pricing.test.ts`; migrations `20260807160000`/`20260807161000` + `node0_opening_credit_test.sql` scenarios E/F + `cofounder_role_test.sql` scenarios B/B2; drift rows **D75**, **D76** closed, **D73**, **D74** → pending-deploy; promote policy §"Production deployments and Promote" in `docs/maanta-launch-ops-runbook.md` |
| 2026-07-31 | **Node staffing model: one node manager and up to four agents per node.** Agents are shopper- and merchant-facing on the mall floor — onboarding shops, setting up staff accounts, helping at the counter. The node manager coordinates with mall management so the node runs smoothly and owns the operator relationship. The cap of **four agents at any one node** is part of the decision, not an estimate. Public copy states this as *how a node is staffed* — a description of the operating model, which is what a mall operator evaluates — and never as a headcount standing in a named mall on a given day; a present-tense count is a measured figure and would have to render through `ScenarioStat`. Supersedes the vague "our team" phrasing the copy decks carried, which both registers had flagged as unverified. | `NODE_TEAM` in `maanta-app/src/lib/marketing/facts.ts` is the single source; `/mall-operators`, `/merchants` and `/about` all render from it. Guarded by `maanta-app/src/lib/__tests__/marketing-shell.test.ts` ("describes the node staffing model from one source"). Closes drift **D35** |
| 2026-08-01 | **The four legal routes are `noindex` but publicly crawlable.** They are no longer `Disallow`ed in `robots.txt`; each still emits `<meta name="robots" content="noindex, nofollow">` while `DEMO_MODE` is true, and each stays out of `sitemap.xml`. Founder ruling on option B of `LEG-02`. The superseded position was belt-and-braces — disallow *and* noindex — which does not hold up: `Disallow` blocks a crawl, not an index, so a disallowed URL linked from every page's footer can still be listed as a bare URL with no snippet, *because* the crawler was forbidden from reading the page that says `noindex`. The disallow was working against the noindex rather than reinforcing it. The deciding factor is external: app-store and payment-provider reviews commonly require the privacy policy to be publicly fetchable, and a failed submission is a worse outcome than a ranked draft. **This is not a licence to index the drafts** — the `noindex` is the protection and must stay until the documents are counsel-reviewed. **Does not touch `DEMO_MODE`**, which remains `true`; flipping it is a separate launch action that would also publish these routes to the sitemap and strip every pre-launch disclosure sitewide. | `maanta-app/src/app/robots.ts` — the `...(DEMO_MODE ? LEGAL_ROUTES : [])` spread removed, with both now-unused imports. Page-level `noindex` unchanged in `maanta-app/src/app/(marketing)/{privacy,terms,merchant-terms,cookies}/page.tsx` and guarded by `maanta-app/src/lib/__tests__/marketing-a11y.test.ts`. Sitemap exclusion unchanged in `maanta-app/src/lib/marketing/nav.ts`. Analysis and the production verification that reframed the question: drift **D42** |
| 2026-07-10 | **Waitlist signups live in the email platform**, not in this repo's Supabase (no `waitlist_signups` table, no `/api/waitlist` route). Platform choice confirmed later. | `docs/maanta-waitlist-data-schema.md` (Option A archived); tracker items E7/E8 become form/platform configuration, not repo backend work |
| 2026-07-10 | **Resend is the email platform** for the waitlist. Contacts (with `segment_type` and consent properties) live in a Resend audience — still no Supabase table. Amends the same-day entry above in one respect: a **stateless** `/api/waitlist` proxy route exists in-repo, because the segmentation plan requires server-side keyed API access (the Resend key can't be exposed to browser forms). The route stores nothing. | `/waitlist` page, `POST /api/waitlist`, `src/lib/resend.ts`, `src/lib/waitlist.ts`, `src/lib/waitlist-emails.ts`; env `RESEND_API_KEY` / `RESEND_AUDIENCE_ID` / `RESEND_FROM_EMAIL` |
| 2026-07-15 | **Boost is Elite-only — enforced server-side.** Closes a live breach where `purchase_boost` / `move_boost` checked owner/admin, deal state, no-duplication and balance but **not** `merchants.tier`, letting a Standard merchant with balance buy/move boosts by calling the RPCs directly. Both RPCs now raise a stable `BOOST_ELITE_ONLY` error for non-Elite merchants. The gate checks the merchant's tier and is **not** bypassed by admin/service_role (it sits outside the caller-auth block). No change to boost price, duration, trial length, or Node-0 credits. | migration `20260715194145_boost_elite_only_gate.sql` (`CREATE OR REPLACE` of both RPCs); `/api/boosts` + `/api/boosts/move` map `BOOST_ELITE_ONLY` → HTTP 403; tests in `src/app/api/boosts/**/__tests__/route.test.ts` |
| 2026-07-16 | **Node 0 opening credit = KES 300**, granted by admin at activation to the first **100** launch merchants activated during the Node 0 launch window. A launch-period **promotional credit** (same class as the free Elite trial), **not** a collection — does not breach the manual-billing ban. Amount/cap/launch-node are **frozen** but read from `app_config` (`node0_opening_credit_kes=300`, `node0_opening_credit_merchant_cap=100`, `node0_launch_node=BBS Mall`); window reuses `node0_launch_period_ends_at`. | Written **inline** in `activate_merchant` (migration `20260716084804_node0_opening_credit_on_activation.sql`) as `topup`/`manual`/`KES`, tagged `node0_opening_credit`, in the same transaction as the status flip. **Not** routed via `record_merchant_ledger_entry` (service_role-only), mirroring `purchase_boost`. Idempotent per merchant via `provider_reference = 'node0_opening_credit:<merchant_id>'`. Tests: `maanta-app/supabase/tests/node0_opening_credit_test.sql` |
| 2026-07-20 | **Clerk is the authentication layer; Supabase stays the data + RLS layer** (Clerk wired in as a Supabase **third-party auth provider**, not a rip-and-replace). Clerk owns login UI (phone OTP + email, matching the prior flow) and mints the session JWT Supabase verifies. The Clerk user id arrives as the JWT `sub` and is stored on `public.users.clerk_user_id`. Because every RLS policy and every authz-enforcing SECURITY DEFINER RPC funnels through `current_user_id()` / `current_user_role()`, **re-pointing just those two helpers** migrates the whole security model in place — no policy or RPC bodies change. The helpers resolve **Clerk-primary with a legacy fallback**: `clerk_user_id = auth.jwt()->>'sub'` OR `auth_uid::text = auth.jwt()->>'sub'` (text compare, never `sub::uuid` — SQL doesn't short-circuit OR/AND, so a cast would throw on a Clerk text id; validated against the `security_hardening_test.sql` regression suite, which authenticates by setting `sub = users.auth_uid`). The old `on_auth_user_created` trigger only fired on Supabase-Auth sign-ups, which no longer happen, so mirror `public.users` rows are provisioned in-app on first authenticated request (`src/lib/auth.ts` `ensureAppUser`). **Requires manual dashboard wiring before it works** (Clerk: enable phone+email, enable the Supabase integration so the token carries `role: authenticated`; Supabase: add Clerk as a third-party auth provider) — see `docs/skills/clerk-auth.md`. Open follow-ups: validate Clerk SMS deliverability/cost for Kenya, and backfill/link any existing `auth_uid`-keyed users to `clerk_user_id`. | migration `20260720140000_clerk_third_party_auth.sql` (adds `users.clerk_user_id`, redefines the two identity helpers); `src/lib/auth.ts`, `src/lib/supabase/{server,client}.ts` (attach Clerk token via `accessToken`), `src/middleware.ts` (`clerkMiddleware`), `src/app/layout.tsx` (`<ClerkProvider>`), `src/app/login/[[...rest]]` + `src/app/sign-up/[[...rest]]` (Clerk `<SignIn>`/`<SignUp>`), all `/api/*` routes swapped from `supabase.auth.getUser()` to `ensureAppUser`/`currentClerkUserId` |
| 2026-07-18 | **Shopper `YOU PAY` price model** (design brief §4/§10). A deal now carries `price_kes` (base the shopper pays) + `charges` (disclosed mandatory extras: `{label, type:"fixed"\|"percent", value}`) + optional `compare_at_kes` (struck "Was"). **YOU PAY = price + Σ extras**, computed in exactly one place (`src/lib/pricing.ts`) so the tile, deal detail and claimed code always show the same number. Extras are **itemised only in deal detail**; everywhere else they collapse to one line ("Includes KES N in taxes and charges"). Disclosure is a **mandatory create-deal step (M9)** — neither option preselected, Publish carries the number; a charge not declared here **cannot** be added at the counter (the merchant app has no field for it). This is the amount the **shopper** pays the **merchant** — it does **not** touch the frozen KES 30 MAANTA success fee. Columns are NULLable, so legacy deals simply show no price until re-published. | migration `20260719233037_shopper_you_pay_price_model.sql` (`deals.price_kes/compare_at_kes/charges`, `redemptions.amount_kes` snapshot at claim); `src/lib/pricing.ts` (+ tests `src/lib/__tests__/pricing.test.ts`); `/api/deals` validates+persists; `new-deal-wizard.tsx` M9 step; shopper feed/tiles, `deals/[id]`, `tickets/[id]` display; `/api/redemptions` snapshots `amount_kes` |

| 2026-07-21 | **Top-ups settle arrears FIRST** — code corrected to match the frozen rule (ENGINEERING_NOTES §3; boards M6 arrears / M7 top-up: "arrears settle first, remainder credits your balance, never pre-credit"). The prior `record_merchant_ledger_entry` credited the full top-up straight to `account_balance` and never touched `outstanding_arrears`, so a merchant who owed arrears kept **both** the full balance and the full arrears — the opposite of the frozen behaviour. Now a `topup` credit pays down `LEAST(arrears, amount)` first and credits only the remainder; it writes the full `+amount` `topup` row (the real figure + idempotency anchor) plus a `−settled` **`arrears_settlement`** row (new `transaction_type`) so the ledger reconciles to both balance and arrears. Debit/refund path and idempotency unchanged. This corrects a code divergence — it does **not** change any frozen rule. | migration `20260721120000_topup_settles_arrears_first.sql` (`arrears_settlement` added to the `merchant_transactions` type CHECK; `CREATE OR REPLACE record_merchant_ledger_entry`); both webhooks (`/api/webhooks/{intasend,stripe}`) inherit it via `recordMerchantTransaction`; `src/lib/merchant-ledger.ts` type union. **DoD (§8.4/§8.2/§8.5) closed the same session:** money-path SQL tests `supabase/tests/{verify_redemption_money_path,topup_settles_arrears}_test.sql` (double-verify one-winner, owed@20, unknown+fraud task, settle-first, ledger reconciliation — validated locally against Postgres 16 with a Supabase shim, run in CI `db-tests`); `/demo` index of seeded logins (`src/app/demo/page.tsx`); a frozen-UI enforcement test `src/lib/__tests__/frozen-ui-rules.test.ts` (money-never-amber, closed vocabulary, failure-dark-not-red) plus fixing two money-in-amber violations (`plan/upgrade`, `pricing` → `text-white`). |

| 2026-07-21 | **Guardian v1 — redemption-time fraud checks are now IMPLEMENTED** (supersedes the "velocity/geofence/collusion proposed, not implemented" line). Three verify-time check families run inside `verify_redemption` *after* OTP match and *before* status/money finalise, producing one recommendation: `clear` → success as today; `flag` (allow+flag, verify-anyway preserved) → success with the KES 30 fee applied unchanged + a logged suspicious event; `soft_block` → redemption **held** (`status='flagged'`), **no fee moves**, released by `admin_release_redemption`; `hard_block` → redemption **declined** (`status='failed'`), **no fee moves**, non-accusatory in-ink error. **Verify-anyway refinement:** the frozen rule still holds for clear/flag; block-severity hits are the sole, conservative, auditable exception and move no money — so YOU PAY, the KES 30 pin, the 3-state `feeChargeStatus` ({charged,owed,unknown}, `NULL` on held/blocked), and the frozen UI colour/copy rules are all untouched. Thresholds are deliberately high (block bands above plausible legitimate repeat activity); merchant-velocity never blocks; missing-GPS never penalises. New `guardian_events` table (per-redemption audit, keyed by `redemption_id`); `admin_redemption_detail` surfaces events + recommendation for future admin UI. | design note `docs/maanta-guardian-v1.md`; migration `20260721140000_guardian_v1.sql` (`guardian_events`, `guardian_evaluate`, `verify_redemption` +guardian, `admin_release_redemption`, `admin_redemption_detail`); `/api/redemptions/verify` (block/held → non-accusatory 409); SQL tests `supabase/tests/guardian_v1_test.sql` (clear / allow+flag / soft-block+admin-release / hard-block×2 / collusion — validated locally against Postgres 16 with the Supabase shim, run in CI `db-tests`; all pre-existing money-path suites stay green). |
| 2026-07-21 | **Error state is never colour alone — error message text is #111, red is borders/icons only.** Resolves the R4 audit flag by enforcing the token spec (`status-error #8C1D18` = borders/icons only; text stays `#111`) rather than carving out an exception, so every status stays readable in greyscale. Error **message** text moved from `text-flame` to `text-ink` across the merchant/admin/agent surfaces; the red token remains on semantic borders and icons (`InlineAlert` error variant, `Button` destructive-outline, the `flagged` status chip, icon glyphs). Now a CI ratchet — `text-flame` outside a border/icon/status context fails the frozen-rules test. | `src/lib/__tests__/frozen-ui-rules.test.ts` (new "error body text in red" check); `inputs.tsx`, `deals/new/new-deal-wizard.tsx`, `deals/[id]/deal-actions.tsx`, `deals/archived/archived-actions.tsx`, `topup/topup-flow.tsx`, `staff/new`, `staff/[id]/manage-staff.tsx`, `merchant/onboard/onboard-wizard.tsx`, `admin/billing/plan-actions.tsx`, `admin/merchants/[id]/merchant-admin-actions.tsx`, `agent/leads/new`; audit `docs/skills/frozen-ui-locked-rules-audit.md` |
| 2026-07-22 | **Guardian hard-blocks are appealable — no longer terminal.** A hard-block DECLINES at the counter (`status='failed'`, `fraud_flags @> {guardian_hard_block}`, no fee). New admin-gated `admin_appeal_hard_block(redemption_id, approve)` lets an admin overturn a false positive after the fact: `approve` → `failed→success` + the KES 30 fee via the **frozen** money path (`charged`/`owed`/`unknown`, success committed before the fee step, never rolled back), tagged `guardian_appeal_approved`; `reject` → stays `failed`, no fee, tagged `guardian_appeal_rejected`. Guards: only a `failed` redemption flagged `guardian_hard_block` and not already appeal-rejected is appealable (a redemption that failed for any other reason — expired, merchant-rejected — can never be "completed" here), and the decision is one-time. Mirrors `admin_release_redemption` (soft-block release); no money-path or schema change. Surfaced on the admin detail page as the **Declined by Guardian — appeal** panel (Approve & complete / Uphold block); the held/appeal/reverse-fee amber actions are mutually exclusive by status so at most one shows. | migration `20260722160000_guardian_hard_block_appeal.sql` (`admin_appeal_hard_block`, admin/service_role only); route `POST /api/admin/redemptions/[id]/appeal`; UI `appeal-actions.tsx` + `/admin/redemptions/[id]` panel; SQL test `supabase/tests/guardian_hard_block_appeal_test.sql` (approve / reject + re-appeal refused / non-hard-block guard / non-admin refused — validated locally against Postgres 16 with the Supabase shim; all 10 SQL suites green in CI `db-tests`). Design note `docs/maanta-guardian-v1.md` §3/§5. |
| 2026-07-22 | **Guardian outcomes are instrumented to PostHog** (`guardian_outcome` event, one per verify, for every outcome — clear/flag/soft_block/hard_block). Emission is a dependency-free server-side `fetch` to PostHog `/capture/` (`src/lib/analytics.ts`), **off unless `POSTHOG_PROJECT_KEY` is set** (dev/CI/tests emit nothing; build stays clean), and **best-effort + non-blocking** (`void`ed, error-swallowing, 2 s timeout) so the counter/verify path is never delayed — upholds the frozen "never block the shopper" rule. `distinct_id = merchant.id`; properties carry `recommendation`, `severity`, `redemption_status`, `fee_charge_status`, `disputed`, `deal_id`, `redemption_id`, `node`. The recommendation-rate dashboard is a `query-trends` insight broken down by `recommendation` on a `Guardian` dashboard — spec + ready-to-run PostHog commands in the design note §8 (live provisioning pending PostHog MCP approval; the event enters PostHog's schema on the first production verify). No money-path, schema, or frozen-UI change. | `src/lib/analytics.ts` (+ `src/lib/__tests__/analytics.test.ts`); `/api/redemptions/verify` fires `captureGuardianOutcome` before the block/held branches; `.env.example` (`POSTHOG_PROJECT_KEY`, `POSTHOG_HOST`); design note `docs/maanta-guardian-v1.md` §8. |
| 2026-07-22 | **Guardian v1 thresholds are tunable live via `app_config`.** The velocity/geofence/collusion thresholds move out of hardcoded constants in `guardian_evaluate` into a single JSON `app_config` row (`guardian_thresholds`), read at function entry — ops can retune Guardian without a redeploy (same pattern as `success_fee_kes` / `node0_*`). **Fail-safe:** every value keeps a hardcoded fallback equal to the shipped default; a missing key, a missing row, or a **malformed** row falls back to defaults, so a bad config can never fail Guardian **open** (never silently clears everything). No behaviour change at the shipped defaults, no money-path or schema change, and the block bands still sit above plausible legitimate repeat activity (`velocity_deal.soft` < `velocity_shopper.hard`). | migration `20260722140000_guardian_thresholds_config.sql` (seeds the default `guardian_thresholds` row `ON CONFLICT DO NOTHING`; `CREATE OR REPLACE guardian_evaluate` to read config with per-key fallbacks + a guarded read). SQL test `supabase/tests/guardian_thresholds_config_test.sql` (defaults reproduced / live geofence retune flips 300m flag→hard_block / missing-row fallback / malformed-row fallback — validated locally against Postgres 16 with the Supabase shim, run in CI `db-tests`; restores defaults so `guardian_v1_test` stays green). Design note `docs/maanta-guardian-v1.md` §2. |
| 2026-07-22 | **Admin success-fee REVERSAL — MAANTA may reverse a KES 30 success fee when the merchant is clearly in the right (including after the shopper has redeemed), reviewed by an admin, and applied ONLY by crediting the merchant's top-up wallet.** The original redemption row and the original success-fee ledger row are left **intact**; **no direct balance edits and no silent offsets** — the credit always writes a ledger row like every other money movement. This does **not** change the frozen KES 30 fee, the fee pin, verify-anyway, or the `{charged,owed,unknown}` fee-status model — it is a **purely additive** compensating credit. The credit follows the **frozen top-up wallet-credit rule** (settle arrears first, remainder to balance): a merchant whose fee was charged gets it back on balance; a merchant whose fee sits as arrears has that arrears cleared — both "credit the top-up wallet", both keep the ledger reconciling. Guards: redemption must be `success`, a success-fee ledger row (charged or arrears) must be linked to it (blocks crediting an unknown/never-applied fee), **one reversal per redemption** (`UNIQUE(redemption_id)`), and the recorded approver must be a real admin. Audit trail in `fee_reversals` (aligned to `MAANTA-Fee-Reversal-Log.xlsx`) + export view `admin_fee_reversal_log`. | migration `20260722120000_admin_fee_reversal_wallet_credit.sql` (`fee_reversal` added to the `merchant_transactions` type CHECK; new admin-gated SECURITY DEFINER `reverse_success_fee`; `fee_reversals` table + RLS; `admin_fee_reversal_log` view). Admin route `POST /api/admin/redemptions/[id]/reverse-fee` (`requireAdminApi`, passes the admin id as approver). Admin UI `/admin/redemptions/[id]` detail + `reverse-fee-action.tsx` (one amber primary; money in ink/tabular) — also resolves the `TODO(admin-redemption-detail)`. SQL tests `supabase/tests/fee_reversal_test.sql` (charged / idempotency / arrears-settle-first / no-fee / approver-must-be-admin — RPC logic validated locally against Postgres 16 with a Supabase shim, run in CI `db-tests`; all pre-existing money-path suites stay green). Skill doc `docs/skills/fee-reversals.md`. |
| 2026-07-23 | **Every fee reversal requires a decision note.** The reverse-fee route rejects an empty note (400) and the RPC enforces `note_required` as a backstop, so no KES 30 reversal can be applied without an auditable reason. Reversal reference is the **redemption / OTP code itself — no `DP-` prefix** (founder ruling; supersedes any earlier prefixed-reference idea). | migration `20260723150000_reverse_success_fee_note_required.sql`; `POST /api/admin/redemptions/[id]/reverse-fee` |
| 2026-07-23 | **Public browse views run with `security_invoker = false` by design.** Anon SELECT on `merchants`/`deals` base tables was revoked (`20260720120000`), so `merchants_public_browse` / `deals_public_browse` must execute as the view owner to project only the safe column list to `anon`. Supabase's advisor flags these as "SECURITY DEFINER views" — this is an **accepted, documented** trade-off, not a defect. | migration `20260723130000_fix_browse_views_security_invoker.sql`; SQL test `supabase/tests/browse_views_test.sql` |
| 2026-07-23 | **Merchant financial columns and core-table writes are locked to sanctioned RPCs.** `authenticated` INSERT/UPDATE/DELETE on core tables revoked; protected merchant money columns writable only via the sanctioned SECURITY DEFINER money RPCs; admin actions land in an `admin_ops_log`. | migrations `20260723001651_lock_down_merchant_financial_columns.sql`, `20260723120000_revoke_authenticated_writes_core_tables.sql`, `20260723140000_admin_ops_log.sql`; SQL tests `revoke_authenticated_writes_core_tables_test.sql`, `admin_ops_log_test.sql` |
| 2026-07-24 | **72-hour dispute-resolution SLA** (founder ruling). Admin resolves a redemption dispute within 72h: uphold → redemption reversed + KES 30 credited back via the fee-reversal path; reject → the fee stands. Shopper-facing copy corrected 24h→72h. A later `20260724120000` **drops** the redundant merchant-financial guard trigger in favour of the column-level lock above (removing a double-guard, not relaxing protection); `20260724130000` makes the `fee_reversals` note column `NOT NULL` at the schema level. | `docs/skills/redemption-disputes.md`; migrations `20260724120000_drop_redundant_merchant_financial_guard.sql`, `20260724130000_fee_reversals_note_not_null.sql` |
| 2026-07-26 | **Merchant geolocation + shopper language preference land as additive columns.** `merchants.lat`/`lng` back the Browse map + what3words GPS (prod `W3W_API_KEY` required, fails closed without it); `users.preferred_language` records a shopper's UI language **preference** (Kiswahili shown as "coming soon") — this is a preference flag, **not** full i18n. An "architecture now-fixes" migration corrects feed counts, browse views and readiness signals. | migrations `20260726120000_merchant_lat_lng.sql`, `20260726180000_user_preferred_language.sql`, `20260726200000_architecture_now_fixes.sql`; SQL test `architecture_now_fixes_test.sql`; `src/lib/what3words.ts` |
| 2026-07-28 | **Auth is a runtime strategy toggle, not a single hardcoded provider.** `MAANTA_AUTH_STRATEGY` (+ `NEXT_PUBLIC_` twin) selects the auth path with **no code change**: `clerk` (default, launch — email + phone OTP, `/verify-phone` claim gate) vs `supabase` (dev/staging rehearsal — Supabase email OTP, no Clerk SMS spend, phone gate relaxed). `authjs` is a reserved alias that falls back to Supabase. Production currently runs **Clerk** (`pk_live`, `clerk.maanta.app`). Supabase prod email-auth was hardened the same day (canonical `www` host, token_hash template, `/auth/callback` accepts PKCE + token_hash, `[maanta-auth]` diagnostics). | `src/lib/auth/strategy.ts`, `src/middleware.ts`, `src/app/auth/callback/route.ts`, `src/app/app-bootstrap/page.tsx`; `docs/ops/auth-strategies.md`, `docs/skills/supabase-prod-email-auth.md` |
| 2026-07-29 | **Trial-expiry cron is registered via `cron.schedule()`, not a direct `cron.job` INSERT.** A 2026-07-29 full-state audit found `handle_trial_expiry()` was **never scheduled on production**: pg_cron is installed but `cron.job` held zero rows because the earlier migrations' direct `INSERT INTO cron.job` (with `EXCEPTION WHEN OTHERS THEN NULL`) silently failed — leaving 101 Elite-trial merchants with no path to grace/auto-downgrade (the KES 3,500 conversion loop inert). Corrective migration uses the supported `cron.schedule()` upsert and RAISE NOTICEs instead of swallowing errors. **No business-logic change** — `handle_trial_expiry` itself is untouched. Must be applied to prod, then verified via `SELECT jobname, schedule, active FROM cron.job;`. | migration `20260729092118_schedule_trial_expiry_cron.sql` (applied to prod 2026-07-29, job `maanta_handle_trial_expiry` active); audit `docs/skills/full-state-audit-2026-07-29.md` §7.2 |

| 2026-08-02 | **Docs↔code sync audit run; documentation corrected, no code changed.** Every falsifiable claim in the doc set — route paths, RPC and migration names, KES amounts, caps and windows, status assertions, env/config claims — was checked against `main` rather than against another document. **Overall sync: 88%** across 214 checked claims (188 MATCHES, 17 DRIFTED, 6 STALE-STATUS, 3 UNVERIFIABLE). The money and trust rules held up: KES 30 charged only inside `verify_redemption`, the KES 300 Node 0 credit written as a promotional `topup` and called a credit everywhere (no "fee waiver" survives in copy), verification never wallet-blocked while only deal creation gates at zero, the fee-reversal path admin-only and settle-arrears-first with a required note and the OTP code as reference and no `DP-` prefix, the 72h dispute SLA present in shopper copy, Guardian v1 live with admin release and appeal, boosts Elite-only, and the waitlist stateless with no Supabase table. **Three worst documents:** `docs/maanta-technical-handoff.md` (57%), `docs/maanta-project-overview.md` (72%), `docs/skills/clerk-auth.md` (78%). Fixes were docs-only by design — where the code appeared to contradict a frozen decision it was recorded, not edited. **2 High findings remain open for a human:** the auth-default conflict (**D59**) and the feed rail naming (**D66**). New drift rows **D58**–**D67** (renumbered from D56–D65 after #167 took D56 mid-review); D58, D60–D65 and D67 opened and closed in the same change, D59 and D66 left open with the founder. **The review round added 8 further corrections and one row.** CodeRabbit caught four places where this audit fixed a claim in the document that named it and left the same claim standing in another — the money-path "single entry point" line in the technical handoff, the `leads` row still calling the public waitlist non-existent, the waitlist spec's superseded `DECIDED` block, and the overview's one-variable Clerk summary — plus four `Last updated` headers older than their own edits. All fixed; recorded as **D67**, whose durable output is a rule: **when a row corrects a sentence, grep the repo for that sentence before closing it** — the discipline the verification bar already imposes on numbers, extended from values to claims. | Docs changed: `CLAUDE.md`, `AGENTS.md`, `docs/README.md`, `docs/maanta-project-overview.md`, `docs/maanta-technical-handoff.md`, `docs/maanta-launch-readiness-tracker.md`, `docs/maanta-waitlist-data-schema.md`, `docs/maanta-staged-readiness-now-launch-10k-100k.md`, `docs/maanta-production-rollout-plan.md`, `docs/skills/clerk-auth.md`, `docs/skills/frozen-ui-overall-handoff.md`, `docs/ops/tech-stack-deep-dive-2026-07.md`, `docs/ops/marketing-site-repo-map.md`, `docs/ops/website-handoff.md`, `docs/ops/copy/contact.md`, and this log (two migration filenames corrected — see **D61**). Verified with `npm run test` (70 files / 533 tests green, 2026-08-02) and by diffing every doc-cited migration filename and backticked repo path against the tree |

| 2026-08-05 | **Production migration map corrected + paused-deal discovery filtering recorded as intended behaviour.** (1) The pause-gate migrations landed on production **2026-08-04**, initially under MCP-minted ledger versions; later on 2026-08-05 the **entire ledger was reconciled with the repo (D24 closed)** — the pause-gate rows repaired to the repo filenames `20260730180000`/`20260730190000`, prod's uncommitted `node_scoped_opening_credit_cap` exported into the repo at `20260730120000` (its per-node change is **not in effect** — clobbered by `20260730130000`'s `CREATE OR REPLACE`; recorded as **D73**, must be relanded before a second node), and the notes migration renamed to `20260730160000` to match the ledger. Full read-back diff: local and remote agree on all **85** version/name pairs. The standing rule generalises: **never reuse a version production's ledger already holds for different content**. (2) **Scope note:** `paused_deals_discovery_filter` goes beyond D25's claim-gate scope — pausing a deal now also removes it from `deals_public_browse`, i.e. from anonymous SQL discovery, not just from claiming. This is **intended behaviour, not overreach**: it is the D32 fix, shipped in the same PR #150 as the claim gate, and matches the frozen semantics in `docs/skills/paused-deal-semantics.md` and CLAUDE.md's Paused deals section ("pausing immediately removes the deal from shopper discovery"). Recorded here so the scope is a decision on the record rather than an accident of the apply. (3) The `cofounder` role CHECK (`20260804010000`, decided in PR #172) was applied to production 2026-08-05 with the ledger version matching the repo filename; no user holds the role — assignment stays founder-held (Q14). | `docs/ops/supabase-migrations.md` (corrected checklist + version map); drift rows D24/D25/D32/D69/D72 (closed), D73 (open — node-scoped cap reland); PR #150 (pause semantics incl. discovery filter), PR #172 (cofounder role); read-backs recorded in the drift register |

| 2026-08-05 (evening) | **Production state re-verified live and the record aligned to it; one new drift row opened.** An evening session with Supabase/Vercel/Sentry access re-verified the day's closures rather than trusting them: (1) **D69 confirmed closed** — `users_role_check` includes the exact string `'cofounder'` (one word, no underscore), zero users hold it, and the ledger's newest rows read `20260730180000 restore_claim_deal_pause_gate` / `20260730190000 paused_deals_discovery_filter` / `20260804010000 cofounder_role`, the interim MCP-minted `202608041529xx` versions renumbered away; the pause gate re-verified live in `claim_deal`. The same read-back found **no database policy references `cofounder`** — its narrower-than-admin scope is app-enforced only, which is D69's one piece of residue and is now tracked as **D74** (open): add RLS/policies before the role is ever assigned. (2) **D70 state confirmed**: Sentry shows zero unresolved production issues and `JAVASCRIPT-NEXTJS-4` no longer surfaces; its `resolved` status was set by the **system** (`set_resolved_in_release`, triggered by the fix commit reference), not a knowing human closure — the deliberate closure note added 2026-08-05 is what makes it an owned resolution. (3) **D71 stays open, narrowed**: Vercel deployment protection is **enabled** — Vercel Authentication ON, scope `all_except_custom_domains`, so branch/preview URLs are no longer public while `maanta.app` stays open. That closes the preview-URL exposure, not the promote path; the remaining residue is the Production Branch confirmation, the promote-rights/token audit, and the row's close condition. (4) The Notion OpTruth hub was synced to all of the above on 2026-08-05 — mirrors should reference that date | Drift rows: D69/D70 (closed, evidence in the register), D71 (updated in place), **D74 (new)**; `docs/skills/role-permissions.md` (pending-deploy header corrected; D74 provisioning warning); `docs/maanta-launch-readiness-tracker.md` header note; `CLAUDE.md`; `docs/maanta-claude-operating-system.md` (D25 paragraph corrected); dated as-of notes in `docs/ops/live-pilot-day-one-prep-2026-07-30.md` and `docs/ops/demo-mode.md` |

| 2026-08-14 | **Preview auth parity: Option A — Preview gets the production-serving public Clerk publishable key; the auth strategy stays Clerk; nothing else moves.** Founder ruling on **D98** (every Vercel Preview deployment red since at least 2026-08-10 on `@clerk/nextjs: Missing publishableKey`, including docs-only branches — environmental, not code). Authorized: **one write** — `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` added to the Vercel **Preview scope only**, value = the public pk_test key production already serves — then a dashboard redeploy of PR #201's existing commit (`27fcd76`) and read-back verification. Explicitly **not** authorized: `CLERK_SECRET_KEY` or any other secret, any Production/Development env change, any switch of either strategy var to Supabase merely to force green, any code/DB/Clerk-dashboard change, any production deploy or transaction. If a precondition fails or the redeploy fails: stop and report — no fallback. Option B (Preview on the Supabase strategy) **rejected** because it makes deploys green by bypassing the Clerk branch Production executes, preserving the only-in-production blind spot behind D70/D96. Sequencing also ruled: Preview fix → rebase and merge #201 → D93 device **measurement** (the PWA icon/metadata work is a distinct task, not folded in) → only then B2 and the Clerk production-instance assessment, both held until #201/D93 complete. Same session, **D99** opened: production measured 2026-08-14 serving the Clerk **development** instance (pk_test, `cheerful-sailfish-3.clerk.accounts.dev`) — contradicting this log's 2026-07-28 entry's pk_live read-back; whether dev-instance-for-pilot is intentional is an open founder ruling, deliberately not assumed here. Notion mirror of this entry: owed, not yet done | Vercel Preview env scope (operator-executed; session tooling cannot write env vars); runbook `docs/ops/preview-auth-parity-option-a-2026-08-14.md` (preconditions, one-write scope, three-scenario outcome table incl. the D70-class split-brain detectable via `/login` HTML and public `/api/healthz?ready=1`); drift rows **D98 (new)** / **D99 (new)**; `docs/ops/outstanding-work-inventory-2026-08-14.md` |

| 2026-08-15 | **Preview remediation completed and the posture frozen as Option C: Preview stays a Clerk-strategy environment verifying build, server middleware, SSR and anonymous browsing; the Clerk client widget on ephemeral `*.vercel.app` hosts is an accepted residual limitation, not a defect to chase.** Three rulings in sequence. (1) The 2026-08-14 secret-key exclusion was lifted after a read-only audit demonstrated the requirement: `CLERK_SECRET_KEY` (development-instance) added to the Vercel **Preview scope only**, with the audit's env-snapshot lesson recorded — a variable saved after a deployment exists never reaches that deployment; save-then-redeploy is the only valid order. (2) **Option C adopted** for Preview posture: no Preview strategy change (Supabase-for-green rejected a second time — it would reinstate the D70/D96 blind spot), no Clerk domain/origin/plan change, no stable staging domain yet, and explicitly **no origin-allowlist entry while Production shares the development instance** (D99) — with a shared instance, any instance-level change is a production-auth-surface change. `test:clerk-smoke` stays the strategy-resolution guard; Preview now covers the D96 class (build + Clerk middleware + SSR) on every deploy. (3) **Production-provenance prevention** (from D100's near miss, same shape as D37/D53): no manual redeploy or promote of a PR/Preview build to Production, ever — production ships only via merge to `main` and the git integration. D98 closed on read-back evidence; D99 stays deferred until #201/D93 complete; B2 still held. Notion mirror: owed | Drift rows **D98 (closed)**, **D100/D101 (new, closed)**; `docs/ops/preview-auth-parity-option-a-2026-08-14.md` outcome appendix; `docs/skills/get-started-blank-screen-fix.md` correction (D101); Vercel Preview env scope (operator-executed); `docs/ops/outstanding-work-inventory-2026-08-14.md` addendum |

| 2026-08-22 (Node 0 field validation) | **Node 0 pilot sequencing recorded, and the opening-credit wall ruled into the experiment rather than out of it.** (1) **Keep KES 300 for Merchant 01 — do not raise `node0_opening_credit_kes` before activation.** The credit covers exactly **10** verified redemptions (KES 300 ÷ the frozen KES 30 success fee). Redemption 11+ still verifies — verify-anyway holds and the fee records as `owed` — but `trg_enforce_zero_balance_gate` blocks **new deal INSERTs** at `account_balance <= 0`, so the merchant cannot post again until they top up, and **E6** leaves no live rail to top up with. Rather than paper over that, the wall is treated as a measurement: *after 10 verified redemptions, does the merchant ask how to continue?* A merchant who asks is stronger evidence of willingness to pay than one who reposts indefinitely on free credit; a merchant who does not ask is also a result. Phase 7's "voluntarily posts another deal" criterion is to be read against this — a blocked repost is not a failed Phase 7 if the merchant asked how to unblock it. Rejected: raising the credit before activation (the only moment it could be raised, since the grant fires at activation and is not retroactive), manually crediting the wallet on empty, and capping the pilot at 10 to widen to a second merchant. **Not decided here:** what happens once the answer is in — if Merchant 01 asks to top up there is still no rail, and that is a separate decision at the time. (2) **Week 1 sequencing revised**: the D151 SMS test runs **first**, not as trailing parallel work, because it turned out to be unblocked — SMS test → if +254 fails, inspect the Clerk instance's SMS country permissions / destination allowlist / sender config → continue with email as a valid claim route either way → Merchant 01. (3) **D151's stated prerequisite withdrawn as an error**: the row and the field operator day sheet both required a Clerk production instance to be provisioned first, citing D99 — but that cited D99's *opening* measurement (2026-08-14) and missed its **closure** on 2026-08-16, when production was re-measured serving the **production** instance (`pk_live` decoding to `clerk.maanta.app$`; `x-clerk-auth-reason: session-token-and-uat-missing`, not the `dev-browser-missing` header that identified the development instance), corroborated by **D59** on 2026-08-19. Consequences: nothing gates the SMS test, and the founder's failed SMS attempts were already on a pk_live instance — so the development-instance hypothesis D151 was opened on is dead, which **strengthens** the 2026-08-22 claim-gate widening (it answered a real delivery problem, not a provisioning oversight) and moves the investigation to that instance's SMS settings. (4) **E2/E3/E4 restated as field-closure gates** — they close on real people completing the journeys, not on CI; a green build says nothing about them. **Three decisions deliberately left open** and recorded as such: the one-time Clerk dashboard confirmation that email sign-up + verification is enabled (the pilot's whole email premise rests on it, and `src/lib/launch-auth.ts` flags it as a SPEC-GAP since Clerk owns which factors the hosted widget renders), whether demo mode stays on during the pilot (it is on, and `claim_deal` has **no** demo guard, so Shopper 01 must open the real deal by direct link `/deals/{id}` rather than browsing `/feed`), and **O2**'s named support owner | `docs/maanta-node0-field-validation-timeline.md` (new — the sequence, the operating model, five pre-Week-1 findings verified against migrations and `app_config`, and the open-decision table); `docs/maanta-drift-register.md` (**D151** evidence and status corrected); `docs/ops/field-operator-day-sheet.md` (standing SMS task — the withdrawn prerequisite called out in plain language so the operator does not follow the old instruction); `docs/maanta-launch-readiness-tracker.md` (pointer to the timeline). Docs only, no behaviour change; vitest 1028/1028, lint and typecheck clean |

## Pending decisions

| Decision | Needed by | Notes |
|---|---|---|
| Browser golden-path E2E (Playwright) | When a live Supabase + Clerk test env exists | RPC-level golden path is covered (`supabase/tests/golden_path_test.sql`); the browser flow is not. Add a Playwright golden path `/demo → claim → verify → wallet` and gate CI on it. Own ticket, depends on infra — deliberately not scaffolded (an unrunnable suite is false coverage). See `docs/skills/frozen-ui-locked-rules-audit.md` |
| Kenya incorporation + entity details | Nov 2026 Nairobi trip | Blocks publishing legal docs; governing-law refs assume Kenya |
| ~~Which email platform hosts the waitlist~~ | ~~Before campaign build~~ | **Resolved 2026-07-10: Resend** (see Recent decisions) |
| Payment processor final choice for launch | Nov 2026 | See `maanta-app/legal/payment-processor-comparison.md` |
| Elite subscription price (KES 3,500/mo) review | Feb 2027 | Success fee (KES 30) is explicitly NOT under review |
| ~~**D1 — Feed default sort vs the locked feed structure**~~ | ~~Before launch~~ | **Resolved 2026-07-30 (see Frozen decisions): the feed now defaults to the locked structure.** |
| ~~**D2 — Enforce the Elite-trial first-100 cap, or make it discretionary**~~ | ~~Before launch marketing goes out~~ | **Resolved 2026-07-30 (see Frozen decisions): the cap is now enforced in the database.** |
| Paid FX provider (replace keyless open.er-api.com) | Before live non-KES charges | Flagged in `src/lib/currency.ts` |
| Cross-border data transfer basis (Supabase in eu-west-1) | Lawyer review | Flagged in `maanta-app/legal/privacy-policy.md` |

## How to add an entry

1. Record the date, decision, and the exact code/doc it affects.
2. If it changes a frozen rule, say what it supersedes.
3. Mirror to Notion and mark the repo copy's "Last updated".

**Notion mirror status:** the Notion Decisions Log page is mirrored through
**2026-08-22, including the evening addendum** (two founder instructions the
same day, both "Update notion"). The addendum records the Clerk email-sign-up
confirmation, the distinction that **enabled is not delivered**, and the
close-of-day deployment read (production on `main @ 18e72fd`; six consecutive
production deploys all from `main`, no promotes — evidence of discipline, not a
guard, so **D71** stays open). The first pass covered
all six 08-22 rulings: Direction A shipped product-wide, the claim gate widened
to a verified email or phone, `0 verified` kept visible, O2 accepted-not-closed
for the pilot, the three field documents, and the Node 0 pilot sequencing with
the opening-credit ruling. The same pass updated the **OpTruth hub** (new dated
banner + Last-updated entry, explicitly recording that **no production read was
taken on 2026-08-22**, so its snapshot tables still date from 08-16 and the
08-19 delta) and **Nairobi Field Operations — Node 0**, where the stale
"a failed Kenyan SMS is a 🔴 BLOCKER" instruction was replaced — it would have
stalled the pilot on exactly the thing the widened gate exists to unblock.

*This note previously read "mirrored through 2026-08-16" while the Notion page's
own banner already said 2026-08-20 — the repo under-reported the mirror by four
days. Corrected here rather than in a register row: the mirror state is this
note's own subject, not a claim about the product.*

Last updated: 2026-08-22 (sixth entry: email is primary production auth for Node 0, phone deferred behind the paid Clerk plan; D151 deferred; D154 opened — staff seats are phone-only, owner verifies meanwhile). Previous: 2026-08-22 (fifth entry: four D152 rulings — re-enable phone, drop required password, keep Clerk ids and attach emails, enable email-code sign-in; field prospecting GO, live pilot HOLD until D152 closes). Previous: 2026-08-22 (Node 0 Field Validation Mode entered — engineering/design frozen pending field evidence; one bounded closing pass authorized: sync, security audit, production E2E). Previous: 2026-08-22 (Node 0 field validation timeline recorded; opening credit held at KES 300 for Merchant 01 with the 10-redemption wall ruled into the experiment as a willingness-to-pay signal; Week 1 resequenced to run the D151 SMS test first after its stated prerequisite was withdrawn as an error — production has served the Clerk production instance since 2026-08-16). Previous: 2026-08-22 (0 verified kept visible for honesty and trust; D151's SMS measurement assigned to the field operator with its production-instance prerequisite named). Previous: 2026-08-22 (claim gate widened to a verified contact channel — phone OR email — "for now" pending an OTP provider, so the pilot can be tested in Oslo, London and Nairobi; D151 opened for the Clerk development-instance SMS finding). Previous: 2026-08-22 (visual refresh ruled: Direction A everywhere — founder instruction from the 22-board directions canvas; slice 1, the app-wide borderless-shadow card flip, shipped with its ratchet guard in the same change; marketing slice and per-surface refinements to follow). Previous: 2026-08-20 (the duplicate-email pairs resolved on founder instruction — email retired on the shadow admin row and the Supabase-era customer duplicate, keepers chosen on measured FK activity, read back to zero real duplicate groups; PR #236 merged through `main` as `5b910bc`, landing the D108/D142 closes; every identity gate on the controlled pilot is now clear). Previous: 2026-08-19, seventh session (PR #235 merged — squash `693ca47` through `main` on the fresh green CI; production deploy read back as `target: production` from `main`, served manifest confirming the new tree; founder-authorized MCP apply of `20260819200000`, read back, ledger repaired to the repo filename and reconciled at **97/97**; D108 and D142 closed — the identity chain is live end to end). Previous: sixth session (the external review's verified gaps registered — D143–D146, gates O7–O9, the D18 flip check written; its refuted demo-isolation P0 recorded as refuted; pilot-vs-launch gating classified; nothing implemented). Previous: fifth session (Q1 ruled **A** and implemented: the verified-email relink, with the email column frozen as its companion hardening — D108 updated, D142 opened; the three duplicate-email groups measured and named as the hard-fail residual). Previous: fourth session (the queue's four no-ruling items done: D140, D141, D31 closed, D82's standalone half shipped; every ranked question still awaits its ruling). Previous: third session (decision queue built — 23 founder-owned rows ranked, D108's prevention half first and the only one with a deadline; D108 refined against a live re-measurement). Previous: 2026-08-19, second session (web manifest generated and gated — D138; D93's repo-side half shipped and the install funnel recorded as unproven — D139; every open row re-verified against HEAD, D94 closed, D140 opened). Previous entry the same day: (staff-seat linking repaired at `users.phone` — D129; `/search` pause filter — D119 closed; three `unknown`-fee-path doc corrections plus the trust formula written down — D130; seven tier-2 rows opened D131–D137 and D128 widened from one table to six, all founder-owned). Previous: 2026-08-18 (shopper deal categories ruled: **ten buckets**, attached to the deal, not the merchant — locked at three, then widened the same day when four demo items fit nowhere (D117, opened and closed); uncategorised stays a first-class state rather than a back-fill; the chip row is withheld when it cannot change the screen, except that an active filter always keeps an All chip; empty states are derived from counts so they name the filter that actually emptied the screen; `Category` stopped labelling the deal-type control on /map, /feed and /browse. Migrations 20260818150000 and 20260818160000 written and NOT applied — D116. D118 opened for the filter running after the feed's row limits, D119 for /search missing an is_paused filter, D120 opened and closed for three defects an adversarial review found. Notion mirror: owed. Prior entry: 2026-08-16 (second entry: the agent console ruled in as the agents' lite admin console — onboard via merchant-authored handoff plus pipeline monitoring, superseding the design-side R4 "routes, not a product" posture. First entry: D105's open half closed: the new-merchant opening-credit wallet state shows only while the credit is unspent, with no partly-spent state — ruled on the shipped behaviour rather than commissioning a second sentence; its companion ruling of 2026-08-15 derives the covered count from `app_config` instead of typing the brief's literal 10. Prior entry: 2026-08-15 — Preview remediation completed and Option C posture frozen — D98 closed on read-back of the final Preview deployment; D100 opened-and-closed for the production-provenance near miss (dashboard redeploy/promote of the unmerged #201 build, restored to `main` via empty commit and verified) with the no-manual-promote rule now explicit; D101 opened-and-closed for the dev-keys claim in the blank-screen skill doc; origin-allowlist and staging-domain decisions deferred behind D99; B2 still held. Prior entry: 2026-08-14 — Preview auth parity ruled — Option A: a single authorized Preview-scope env write (the public Clerk publishable key), redeploy of PR #201's commit, read-back verification, stop-on-failure, no fallback; D98 opened for the all-previews-red env-parity gap and D99 opened for production measured on the Clerk development instance (pk_test), contradicting the 2026-07-28 entry's pk_live read-back; sequencing ruled Preview fix → #201 merge → D93 device measurement, with B2 and the Clerk instance assessment held; runbook `docs/ops/preview-auth-parity-option-a-2026-08-14.md`; Notion mirror owed. Prior entry: 2026-08-07 — three rulings recorded above: D75 wire-in, formatKes won't-do, escapeHtml superset; dead-code PR #176 merged; D73/D74 migrations in the repo pending the human prod apply; D75/D76 closed; the fifth branch-promote caught by the D71 tripwire and rolled back same day; promote policy added to `docs/maanta-launch-ops-runbook.md`. Prior entry: 2026-08-05 evening — production state re-verified live: D69 closure confirmed by read-back — exact role string `'cofounder'`, zero holders, repaired ledger versions; D70's Sentry auto-resolve distinguished from a knowing closure and noted; D71 narrowed — Vercel Authentication enabled at `all_except_custom_domains`, promote path still ungated; D74 opened for the missing cofounder DB policy layer; Notion OpTruth synced same day. Prior entry same day: production migration map corrected and the ledger fully reconciled with the repo — 85/85 version/name pairs matching, D24 closed, D73 opened for the clobbered node-scoped cap; discovery filtering recorded as intended per PR #150/D32; cofounder CHECK applied to prod. Prior entry: 2026-08-02 docs↔code sync audit: 88% overall, docs-only corrections across 16 files, rows D58–D66; two migration filenames in this log corrected per D61; auth-default conflict D59 and feed rail naming D66 left open for the founder. Prior entry: 2026-08-01 legal routes made publicly crawlable but `noindex` — LEG-02 option B; `DEMO_MODE` untouched. Prior entry: 2026-07-31 node staffing model frozen — one node manager, max four agents per node; closes D35. Prior entry: 2026-07-30 truth audit: launch offer recorded and its first-100 cap enforced in the DB; feed defaults to the locked feed structure — closes D1 and D2; prior 2026-07-23 waitlist/Resend and 2026-07-22 Guardian and fee-reversal entries retained)
