# MAANTA drift register

Last updated: 2026-08-02

The **canonical** durable record of **known gaps between what MAANTA claims and
what is true**, with open/closed state per row. Created 2026-07-30 to close FU-1
of `docs/skills/truth-audit-2026-07-30.md`.

"Canonical", not "only": `docs/skills/parity-drift-register-2026-07-30.md` also
exists and is a dated narrative companion, not a state tracker. That split is
itself tracked as **D27** and is not the intended end state — items there should
land here as rows and close by D-number. This paragraph exists because the header
previously said "single durable record" while D27 recorded a second register four
screens below it, which is precisely the claim-vs-reality gap this file is for.

## Why this file exists

Before it, drift was recorded in dated audit documents (`docs/skills/*-audit-*.md`).
Those are good narratives and bad trackers: nothing carries state forward, so a
finding resolved in one audit can be re-raised or forgotten in the next. That is
not hypothetical — it is what happened:

- The Elite-trial "14 vs 30 days" conflict was resolved on 2026-07-29, and a code
  comment still called it open on 2026-07-30 (**D5**).
- The launch offer was frozen in Notion and had never reached the repo mirror at
  all, despite being referenced in passing by another decisions-log entry (**D6**).
- Two invariants this repo described as enforced were not (**D3**, **D9**).

So the register's job is not to be tidy. It is to make *"we already fixed that"*
a checkable claim.

It started earning that before it was even merged. The first draft carried a row
saying `MAANTA_DEMO_MODE` was unverified; while the register sat on a branch, that
verification landed on `main` and turned up three further gaps in the process
(**D17**–**D20**). Rebasing forced the row to be re-checked against reality rather
than shipped as written — which is the entire mechanism, applied to itself.

## The rules

1. **Rows are append-only.** Never delete or renumber a row — close it. The
   history of what was once wrong is the point.
2. **A row may only be `closed` when its evidence names a guard** — a test, a
   migration, or a decisions-log entry: something that would fail or be
   contradicted if the drift came back. `drift-register.test.ts` fails if the
   cited path does not exist, and fails if the only citation is a document the
   row merely relates to.

   Some drift cannot be guarded — a misleading comment, or behaviour that needs
   test infrastructure this repo lacks. Those may still close, but the evidence
   must say **`no guard: <reason>`** so it is a visible choice. The first draft of
   this register did not have that rule, and an open row flipped to `closed`
   passed the suite because it happened to cite a background doc. That hole was
   found by trying it.
3. **`pending-deploy` is not `closed`.** A fix merged to `main` but not yet live
   (an unapplied migration, an unset env var) stays open in substance. This
   distinction exists because "merged" is the exact point at which people stop
   tracking things.
4. **`deferred` needs an owner and a next step**, not just a shrug.
5. Every audit that finds drift adds rows here **before** writing its narrative,
   and closes prior rows by ID rather than re-describing them.

### Status values

| Status | Meaning |
|---|---|
| `open` | The gap exists right now. |
| `pending-deploy` | Fix is merged but not yet live where it counts. Still a gap. |
| `closed` | Fixed, live, and evidenced. |
| `deferred` | Accepted knowingly, with an owner and a trigger for revisiting. |

### Category values

`code-outlier` · `mirror-stale` · `doc-stale` · `db-metadata` · `product-decision` · `prototype-only` · `process`

---

## Register

| ID | Status | Category | Opened | Domain | Claim vs reality | Evidence / next step | Owner |
|---|---|---|---|---|---|---|---|
| D1 | closed | product-decision | 2026-07-30 | Feed ordering | Frozen feed rule locks three per-rail sorts; `/feed` defaulted to distance and re-sorted all three, discarding the boosted order Elite merchants pay KES 500/24h for | `maanta-app/src/lib/deal-list-controls.ts`, `maanta-app/src/lib/__tests__/locked-feed-order.test.ts`, `docs/maanta-decisions-log.md` | founder |
| D2 | closed | product-decision | 2026-07-30 | Trials / money | Frozen launch offer is capped at the first 100 BBS Mall merchants; nothing enforced the cap or the node scope | Enforced by `maanta-app/supabase/migrations/20260730130000_enforce_elite_trial_first_100_cap.sql` + `maanta-app/supabase/tests/elite_trial_cap_test.sql`. **Live on production 2026-07-30**: `elite_trial_cap_status()` returns cap 100, granted 0, remaining 100 — the full offer is unspent, so no backfill surprise | founder |
| D3 | closed | doc-stale | 2026-07-30 | Feed ordering | Notion "Frozen Scope and Rules" claimed the feed structure was "enforced in code", citing a test file that never covered it | Notion bullet corrected with a dated sub-note; now cites `maanta-app/src/lib/__tests__/locked-feed-order.test.ts` | founder |
| D4 | closed | code-outlier | 2026-07-30 | Pricing copy | `/pricing` promised "first month of Elite free" — no cap, no node scope, no fee caveat — against a frozen offer that has all three | `maanta-app/src/app/(marketing)/pricing/page.tsx`, guarded by `maanta-app/src/lib/__tests__/pricing-copy.test.ts` | founder |
| D5 | closed | mirror-stale | 2026-07-30 | Trials | A code comment called the 14-vs-30-day trial conflict "an open spec/DB conflict" after the 2026-07-29 audit had already resolved it to 30 days. First recorded re-discovery | Corrected in `maanta-app/src/app/api/admin/merchants/[id]/approve/route.ts`. no guard: a test cannot assert that prose is not misleading; the 30-day value itself is covered by `maanta-app/supabase/tests/elite_trial_cap_test.sql` | eng |
| D6 | closed | mirror-stale | 2026-07-30 | Trials | The launch offer was a frozen rule in Notion and appeared nowhere in the repo mirror, though another decisions-log entry referenced it in passing | `docs/maanta-decisions-log.md` | eng |
| D7 | closed | code-outlier | 2026-07-30 | Pricing copy | "Free" rendered as Standard's headline price, while a Standard merchant pays the success fee on every verified redemption | Copy fixed on both public pages and guarded by the bare-"Free" check in `maanta-app/src/lib/__tests__/pricing-copy.test.ts` | founder |
| D8 | closed | code-outlier | 2026-07-30 | Pricing | The frozen success fee existed as four independent `30` literals across public pages, page metadata and a fallback | Single-sourced as `SUCCESS_FEE_KES` in `maanta-app/src/lib/pricing.ts`; uniqueness asserted by `maanta-app/src/lib/__tests__/pricing-copy.test.ts` | eng |
| D9 | closed | db-metadata | 2026-07-30 | Pricing | Live `app_config.success_fee_kes` notes gave the Elite price review as "Oct 2026" (superseded by the 2026-07-20 ruling) and cited two files that do not exist in this repo | `maanta-app/supabase/migrations/20260730120000_correct_success_fee_config_notes.sql` + `maanta-app/supabase/tests/frozen_commercial_config_test.sql`. **Live on production 2026-07-30**, read back from `app_config`: notes now cite `CLAUDE.md` and `docs/maanta-decisions-log.md` and give the Feb 2027 review. Applied under a different version number than the file carries — see D24 | eng |
| D10 | closed | process | 2026-07-30 | Ops | Two merged migrations were not applied to production, so D2 and D9 were merged-but-not-true | Pushed by a human operator per `docs/ops/supabase-migrations.md` and verified read-only on 2026-07-30: cap live at `cap 100, granted 0, remaining 100` (recorded against D2), corrected notes live in `app_config` (D9). no guard: whether a migration has been applied is a property of the live database, not of this repo — the check is the read-back, which is why the numbers are recorded here rather than asserted in a test | founder |
| D11 | closed | code-outlier | 2026-07-30 | Feed ordering | An unrecognised `?sort=` reached the distance branch, undoing the D1 locked order from a URL; an unrecognised `?filter=` emptied every rail and claimed the mall had no deals | `parseDealListSort` / `parseDealListFilter` in `maanta-app/src/lib/deal-list-controls.ts`, covered by `maanta-app/src/lib/__tests__/locked-feed-order.test.ts` | eng |
| D12 | closed | code-outlier | 2026-07-30 | Trials / money | The D2 cap was gated on UPDATE only, so an INSERT carrying a trial bypassed it unstamped — which `node0_rehearsal_seed.sql` actually does — making the slot count under-report permanently | Trigger widened to `BEFORE INSERT OR UPDATE`; Scenario H in `maanta-app/supabase/tests/elite_trial_cap_test.sql` | eng |
| D13 | closed | code-outlier | 2026-07-30 | Audit trail | The approve route logged the trial *request* as the outcome, so a refused or unverifiable grant was recorded as granted | Three explicit outcomes in `maanta-app/src/app/api/admin/merchants/[id]/approve/route.ts`. no guard: asserting the logged outcome needs a route-level integration test against a live Supabase, which this repo does not have yet — same blocker as the browser golden-path item in the decisions log | eng |
| D14 | open | process | 2026-07-30 | Demo mode | `app_config.demo_mode_enabled` is `true` on production. Correct for rehearsal, and its own notes say it must be `false` at launch — so today the live product shows synthetic shops and deals to anyone who visits | Flip to `false` as part of the launch checklist and close this row with the timestamp. Runbook: `docs/ops/demo-mode-runbook.md` | founder |
| D17 | closed | process | 2026-07-30 | Demo mode | The `MAANTA_DEMO_MODE` Vercel var could not be read from a Claude session, so it was assumed rather than known — and the property schema was not evidence, since the only prior server events predated the tagging entirely | Proved from the event stream on 2026-07-30 01:19Z, recorded in `docs/ops/optruth-demo-release-2026-07-29.md`. no guard: a deployed env var cannot be asserted from the repo; the proof is an observed event, so re-verify after any change to it | founder |
| D18 | open | process | 2026-07-30 | Demo mode | The two demo switches can be flipped independently, and `make demo-off` touches only `app_config`. Turning demo mode off while `MAANTA_DEMO_MODE` stays `true` tags **real** events as demo — the precise inversion of what the tagging exists to do | Flip both together at launch, or make the ops target set both. Trap documented in `docs/ops/optruth-demo-release-2026-07-29.md` | founder |
| D19 | open | process | 2026-07-30 | Demo mode | `MAANTA_DEMO_MODE` is unset on the Preview environment, so synthetic preview traffic reaches PostHog indistinguishable from production activity. It already cost one untagged event during the D17 verification | Harmless while previews are one-off checks. Set it on Preview before previews are ever used to demo to anyone. See `docs/ops/optruth-demo-release-2026-07-29.md` | founder |
| D20 | closed | code-outlier | 2026-07-30 | Analytics | Every server-side capture — `deal_viewed`, `deal_claimed`, `guardian_outcome`, both top-up events — dropped an unknown share of events: the in-flight fetch died when the Vercel instance froze after the response. Found while verifying D17; predates demo mode | Fixed with `waitUntil` in `captureServerEvent` — `maanta-app/src/lib/analytics.ts`, covered by `maanta-app/src/lib/__tests__/analytics.test.ts`. **Data caveat stays true forever**: any server-side funnel computed over events from before that deploy is a floor, not a measurement | eng |
| D21 | closed | code-outlier | 2026-07-30 | Analytics | `captureDealViewed` sent `clerkUserId ?? "anonymous"`, and browsing needs no account — so most of the top of the funnel collapsed onto one PostHog person. `uniq(person_id)` on `deal_viewed` returned 1 for all signed-out traffic however many people it was, and a view→claim funnel could not join for signed-out users. Stayed hidden because D20 meant the events were not arriving at all | Fixed by reusing the browser's own id — `maanta-app/src/lib/analytics-identity.ts`, covered by `maanta-app/src/lib/__tests__/analytics-identity.test.ts`. **Data caveat stays true forever**, like D20: signed-out unique-viewer counts and view→claim funnels over events from before this deploy are not measurements | eng |
| D22 | open | code-outlier | 2026-07-30 | Analytics | The D21 fix reads `distinct_id` from the posthog-js cookie, which is correct only for the current config: default persistence, no `persistence_name` override, and `defaults` below the 2026-05-30 cutoff where `split_storage` moves the id out of that cookie. If any of those changes the read silently returns nothing and the server falls back — **indistinguishable from a genuine first-ever view, so no runtime warning is possible** | Re-verify signed-out attribution after any posthog-js upgrade or config change; `distinct_id_source` (`clerk` / `posthog_cookie` / `none`) is the property to watch — a sudden shift toward `none` is the symptom. no guard: the failure is silent by construction, so a test in this repo cannot detect it; it needs an occasional look at the live property distribution | eng |
| D15 | deferred | doc-stale | 2026-07-30 | Pricing | `/pricing` hardcodes KES 3,500 with no `app_config` key behind it, unlike the success fee and boost fee, so the UI is the only place a price change lands | Add `elite_subscription_kes` when subscription billing is wired to a processor. Revisit at the Feb 2027 Elite price review | eng |
| D16 | closed | process | 2026-07-30 | Process | Drift was tracked only in dated audit narratives, so resolved findings were re-discoverable and "already fixed" was not a checkable claim — the root cause behind D3, D5, D6 and D9 | This file, enforced by `maanta-app/src/lib/__tests__/drift-register.test.ts` | eng |
| D23 | closed | mirror-stale | 2026-07-30 | Trials / money | The D2 cap migration told the reader "The admin UI reads elite_trial_cap_status() to know this will happen before ticking the box" — written as fact when no UI read the RPC at all, so an admin could exhaust the offer with no warning. Fifth recorded case in this audit of a comment outrunning the code it describes | Made true on `main` by #144: three call sites now read the RPC (`maanta-app/src/app/admin/billing/page.tsx`, `maanta-app/src/app/admin/merchants/[id]/page.tsx`, `maanta-app/src/lib/elite-trial.ts`), written up in `docs/skills/e2e-readiness-2026-07-30.md`. no guard: the drift was a false sentence in a migration comment, and no test can assert that prose matches behaviour — the callers are the reality, and they are now what the comment claims | eng |
| D24 | open | process | 2026-07-30 | Ops | Production's migration ledger and this repo disagree about which migration each version number *is*. Prod records `20260730120000` as `node_scoped_opening_credit_cap` — a migration with **no file anywhere in this repo** — while the repo's `20260730120000` is the success-fee notes fix, which prod recorded at `20260730160000` instead. This contradicts CLAUDE.md's claim that the migration history is "the authoritative record of DB behavior" | Reconcile the ledger with a human operator: export `node_scoped_opening_credit_cap` from prod into a repo file so the applied change is in version control, then repair the version numbering so filenames and ledger agree. Claude Code must not run migrations — see `docs/ops/supabase-migrations.md`. Verified read-only against `supabase_migrations.schema_migrations` on 2026-07-30 | founder |
| D25 | pending-deploy | code-outlier | 2026-07-30 | Core loop | **Implemented in repo, not fully live in production.** Production `claim_deal` still lacks `deal_paused` (confirmed 2026-07-30), so a paused deal can accept new claims despite merchant “No new claims while paused” copy. App rails (#146) and SQL browse filter (#150 / D32) hide paused deals from discovery, but RPC is the enforcement point — deep link / cached client / direct API still get a claim until deploy | **Repo complete in PR #150** (builds on renumbered `maanta-app/supabase/migrations/20260730180000_restore_claim_deal_pause_gate.sql` + `20260730190000_paused_deals_discovery_filter.sql`; lifecycle tests in `maanta-app/supabase/tests/claim_deal_pause_gate_test.sql`). Semantics: `docs/skills/paused-deal-semantics.md`. **Still pending-deploy:** human must `supabase db push`; close only when production `pg_get_functiondef` for `claim_deal` contains `deal_paused`. Do not treat pause as live enforcement on prod until that read-back | founder |
| D26 | open | process | 2026-07-30 | Design truth | The audit brief asked for a guardrail asserting that design-ahead frames reference open drift rows. It was deliberately skipped, and both `docs/skills/truth-audit-2026-07-30.md` §0 and the docblock in `maanta-app/src/lib/__tests__/drift-register.test.ts` justified the omission by saying this repo has no frames artifact. #146 added `maanta-app/design/current-reality/frames.json`, so that justification is now void — the stated reason for not building it no longer holds | Decide whether every `design-ahead` surface must cite an open drift row, then build the guardrail. It would fail today: the file has exactly one design-ahead surface, `/contact`, and the whole file contains no drift-row reference at all (0 occurrences of "drift"). The stale claims in the two docs are corrected in this change; the missing guardrail is what keeps this row open | eng |
| D27 | open | process | 2026-07-30 | Process | A second drift register now exists — `docs/skills/parity-drift-register-2026-07-30.md`, added by #146 — recreating the exact pattern D16 closed: a dated narrative that carries no forward state and is enforced by nothing. `docs/README.md` and `CLAUDE.md` both present `docs/maanta-drift-register.md` as the durable record, which is no longer the whole picture. It is already drifting: its M1 entry previously cited the pause gate as `20260730160000`, the number D25 renumbered away from (now `20260730180000` on the consolidation map) | Fold the parity items into this register as rows, or make the parity doc an explicitly narrative companion that closes items by D-number here. Until one or the other, "we already tracked that" is again unanswerable — which is what D16 existed to prevent | eng |
| D28 | closed | code-outlier | 2026-07-30 | Support | `/contact` renders a success state without sending anything: `onSubmit` calls `setSent(true)` and there is no fetch, no action and no API route behind it. A shopper or merchant who uses it believes they have contacted support, and nobody receives the message. `frames.json` classifies the surface `design-ahead` and its own note says "Fake success — no API" | **Closed by wiring the form to a real destination:** `maanta-app/src/app/api/contact/route.ts` delivers the enquiry to `admin@maanta.app` via Resend with `reply_to` set to the sender, and autoresponds to the sender when they gave an email address. The page (`maanta-app/src/app/(marketing)/contact/page.tsx`) now shows its confirmation only after that request succeeds; a failed send surfaces the error and offers WhatsApp. Guarded by `maanta-app/src/app/api/contact/__tests__/route.test.ts`, which asserts a failed delivery returns 502 rather than a success shape, and that the page cannot reach `setSent(true)` straight from the submit handler. The removed "within 24 hours" promise is also guarded — no response time is published until one is committed to (`website-handoff.md` §9) | founder |
| D29 | open | code-outlier | 2026-07-30 | Core loop | `public.deals.expires_at` is nullable, but `public.redemptions.expires_at` is `NOT NULL`, and `claim_deal` explicitly tolerates a NULL deal expiry (`IF v_deal.expires_at IS NOT NULL AND ... <= NOW()`) before inserting `v_deal.expires_at + INTERVAL '15 minutes'`. A claim on a no-expiry deal therefore dies on a raw NOT NULL constraint violation instead of a clean domain error, and the returned redemption window would be NULL. Latent, not live: 0 of 409 production deals have a NULL expiry, verified read-only 2026-07-30 | Pre-existing — the pause-gate migration is a surgical restore that kept this body, so it did not introduce it. **Deliberately not fixed here**: rejecting or defaulting a no-expiry claim changes claim behaviour on the money path, which is a product decision, not a review fix. Decide the intended semantics (reject with `deal_expired`, require an expiry at write time via a NOT NULL constraint, or define a no-expiry branch), then guard it in `maanta-app/supabase/tests/claim_deal_pause_gate_test.sql` or a sibling suite. Found by CodeRabbit on #137 | founder |
| D30 | closed | doc-stale | 2026-07-30 | Ops | `docs/skills/repo-branch-audit-2026-07-30.md` told operators to replace this branch's `170000` pause-gate file with `180000`, and to rebase shared branches while a later step forbade force-pushing rebased history. Both were unfollowable: the `170000` file no longer exists, and rebasing a shared branch can only be pushed by the force-push the same checklist bans | Both corrected in place with the invariant that matters — exactly one `*_restore_claim_deal_pause_gate.sql`, versioned above production's ledger max — and the sync step now says merge, matching the convention the same document states. Recorded per rule 3 (open-and-close in one change still gets a row); the fix would otherwise have left no trace that the instructions were ever wrong. no guard: a test cannot assert that operator prose is followable — the guard for the underlying migration state is `maanta-app/supabase/tests/claim_deal_pause_gate_test.sql` | eng |
| D31 | open | doc-stale | 2026-07-30 | Pricing copy | `docs/skills/repo-branch-audit-2026-07-30.md` says documents still claiming the launch offer was "removed entirely" **should be read as** "ungoverned free-month line removed". Only the ungoverned wording was withdrawn (D4/D7); the governed first-100 trial stands. Instructing readers to reinterpret stale documents rather than correcting them is the exact failure this register exists to end — the contradictory documents stay discoverable, and a reader who misses the reinterpretation can reintroduce the banned copy | Identify the documents that still say the offer was removed and correct them, or name the single authoritative replacement they should defer to. The banned phrasing itself is guarded by the bare-"Free" and launch-offer checks in `maanta-app/src/lib/__tests__/pricing-copy.test.ts`, so the code path is safe; the exposure is operator and marketing copy written from a stale doc. Raised by CodeRabbit on #137 | founder |
| D32 | closed | code-outlier | 2026-07-30 | Core loop | `deals_public_browse` filtered active + unexpired merchants but **not** `is_paused`, so a paused deal could appear via the SQL discovery view even though `getLiveDeals` hid it and `claim_deal` would raise `deal_paused` | **Closed in-repo via PR #150:** `maanta-app/supabase/migrations/20260730190000_paused_deals_discovery_filter.sql` (`is_paused IS NOT TRUE`); scenarios A–C (+ browse Scenario D) in `claim_deal_pause_gate_test.sql` / `browse_views_test.sql`; `docs/skills/paused-deal-semantics.md`. Production receives `190000` on the **same** human `db push` that closes D25 — until then the browse-view fix is not live on prod, but this row is closed against repo evidence. Track live RPC enforcement under D25 | eng |
| D33 | closed | code-outlier | 2026-07-31 | Marketing site | The demo-mode disclosure is mounted in the **public/marketing** shell, not just the app shells. `(public)/layout.tsx` renders `<DemoModeBanner />` above `PublicNav`, so whenever `app_config.demo_mode_enabled` is `true` every marketing page — `/`, `/pricing`, `/for-merchants`, `/merchants`, `/about` — is topped by "Demo mode — sample data for rehearsal. These shops, deals and codes are not real." Those pages sell the product; the banner tells a prospective merchant or mall operator that the thing they are reading about is not real. The component is correct on the two app shells (`(shopper)/layout.tsx`, `merchant/(app)/layout.tsx`) where synthetic rows actually render; the marketing shell shows no deal data at all, so the disclosure has nothing to disclose there | **Closed by the Phase 1 marketing shell:** the banner is gone from the marketing layout (`maanta-app/src/app/(marketing)/layout.tsx`, renamed from `(public)` in the same change) and both app-shell mounts are intact. Guarded by `maanta-app/src/lib/__tests__/marketing-shell.test.ts`, which fails if the banner reappears on a marketing route **and** if either app shell drops it — scoping it must not become deleting it. Note for whoever fixes it: the switch is **not** an env var — `isDemoModeEnabled()` reads `app_config.demo_mode_enabled` via the service client (`maanta-app/src/lib/demo-mode.ts`, migration `20260729140000_demo_mode_tagging.sql`), so this cannot be verified by inspecting `.env`. Evidence: `maanta-app/src/app/(marketing)/layout.tsx` | founder |
| D34 | closed | code-outlier | 2026-07-31 | Pricing copy | `/merchants` advertises "Boost any deal for KES 500 / 24h" to prospective merchants, but boosts are **Elite-only** and have been since 2026-07-15: `purchase_boost` raises `BOOST_ELITE_ONLY` for any non-Elite merchant, and the gate is explicitly not bypassed by admin or service_role. The lead-gen page that merchants see *before* signing up promises a feature that every merchant who lands on Standard — the default plan — cannot use. `/pricing` is correct (boosts listed only under Elite) but states no boost price, so the two public pages disagree on both availability and cost. Separately, the `500` is inlined in the page while the canonical value is `app_config.boost_fee_kes`, the same single-source pattern `SUCCESS_FEE_KES` exists to enforce | Correct the `/merchants` bullet to state the Elite requirement, and surface the price on `/pricing` where the plan context is. `maanta-app/supabase/migrations/20260715194145_boost_elite_only_gate.sql` is the authority on gating; `20260709175532_deal_pause_boosts_staff.sql` seeds `boost_fee_kes = 500`. **Closed 2026-07-31.** `/merchants` now states the Elite qualifier and `/pricing` carries the price in the Elite card; both read `FACTS.boostPer24hKes` from `maanta-app/src/lib/marketing/facts.ts`, so neither page can restate it. Guarded by `maanta-app/src/lib/__tests__/marketing-shell.test.ts` on both halves — it fails on the phrase "Boost any deal" and on any `KES 500`/`KES 3,500` literal inlined into marketing JSX. Row flipped late: the fix shipped on 2026-07-31 but the status was left `open` while the implementation report already described it as closed, which the register caught on a later sweep — the exact discrepancy rule 5 exists to surface | founder |

| D35 | closed | code-outlier | 2026-07-31 | Marketing site | Marketing copy described the in-mall team as a vague plural — "Our team is in the mall", "Our team works the floors unit by unit", "a team that is in the building most days" — with no structure, no roles and no bound. Both copy decks flagged it (`copy/merchants.md` #8, `copy/mall-operators.md` #8) as "partly true — confirm team capacity matches the promise". A first pass on 2026-07-31 read the founder's "solo founder" answer as meaning no team exists and stripped the claims back; **that reading was wrong**. The founder ruled the same day that the site demonstrates MAANTA at post-launch and that the staffing model is defined: **one node manager and up to four agents per node**, agents shopper- and merchant-facing on the floor, node manager coordinating with mall management | **Closed by stating the model instead of a plural.** `NODE_TEAM` in `maanta-app/src/lib/marketing/facts.ts` holds the structure, cap and both role descriptions; `maanta-app/src/app/(marketing)/mall-operators/page.tsx`, `maanta-app/src/app/(marketing)/merchants/page.tsx` and `maanta-app/src/app/(marketing)/about/page.tsx` all render from it, so the three pages cannot describe the model differently. Frozen in `docs/maanta-decisions-log.md` (2026-07-31) and guarded by `maanta-app/src/lib/__tests__/marketing-shell.test.ts`, which fails if any of the three describes the team without reading `NODE_TEAM` or hardcodes the agent cap in prose. This is stronger copy than either previous version — an operator evaluating a pilot learns the structure and who they call, where "our team" told them nothing. Phrased as how a node is staffed, never as a headcount standing in a named mall on a given day: the model is a product description and belongs in production, whereas a present-tense count is a measured figure and would have to render through `ScenarioStat` | founder |

| D36 | closed | code-outlier | 2026-07-31 | Support | Both live support surfaces linked to `https://wa.me/254700000000` — a placeholder number, not the real support line. `maanta-app/src/app/(shopper)/help/page.tsx` and `maanta-app/src/app/merchant/(app)/support/page.tsx` each rendered a "Chat on WhatsApp" button reaching nobody. `website-footer-legal-docs-plan.md` §2 told the implementer to "reuse the link already live on /help", which would have propagated the dead number onto the marketing footer as well. Same family as D28: a support channel that looks like it works and does not | **Closed**: both surfaces now render `ENTITY.whatsappLink` from `maanta-app/src/lib/marketing/demo.ts`, so the number is declared once and the marketing footer, contact page and both app support surfaces cannot disagree. Guarded by `maanta-app/src/lib/__tests__/marketing-shell.test.ts`, which fails on any hardcoded `wa.me/` link outside the constants module. **Correction 2026-08-01:** that guard did not work when this row was closed — see D38, and D43 for the residual gap D38's fix left. It does now, proven by planting the number and watching it fail | eng |

| D43 | closed | process | 2026-08-01 | Guard integrity | **D38's fix left a narrower version of D38's bug in place.** That row consolidated three private comment-strippers into `maanta-app/src/lib/__tests__/helpers/comment-stripping.ts`, whose `lineCommentAt()` skips a `//` **preceded by a colon** — which repairs `https://` and nothing else. Its docblock stated the limit out loud and argued it was safe, on the grounds that "a false *strip* can only ever make a guard miss something, which the mutation tests in each consumer are there to catch". A guard that misses is not a mitigated failure mode — it is D36 exactly: green while the file contains the string it forbids. And the colon rule still truncates any other literal carrying a double slash: a protocol-relative "//cdn.example.com", a doubled path segment, a regex source held in a string. Each silently deletes the rest of the line before the scan reads it, in a helper that four suites now depend on. The mutation tests cited as the backstop test the *consumers* against planted URLs, so they confirm the colon case and cannot see the others. Found in review of PR #153, independently of D38 | **Closed by making the lexer string-literal aware.** `stripCommentLines()` now tracks single-quote, double-quote and backtick literals with escape handling, so a double slash inside any literal survives regardless of what precedes it, and `lineCommentAt()` is gone as subsumed. Export names and the file path are unchanged, so D38's consumers and citations still resolve. Exercised directly by `maanta-app/src/lib/__tests__/comment-stripping-lexer.test.ts` — first assertion is the wa.me line that started this, then a double slash inside all three quote styles, escaped quotes, a template literal spanning lines, and an unterminated apostrophe that must not swallow the file. Both remaining limits are now stated as limits rather than as safety: regex literals are untracked, and template interpolations count as string interior — both erring toward scanning too much. **Demonstrated rather than argued:** a protocol-relative `href="//wa.me/254700000000"` planted under `src/components/marketing/` now turns the D36 guard red with file and line; run through the colon rule the same line strips to `return <a href="` and the guard regex returns false, so it would have passed. Negative control still green: the same URL inside a genuine `//` comment is ignored. Directory walking moved to `maanta-app/src/lib/__tests__/helpers/source-files.ts` so each helper has one job | eng |

| D44 | closed | code-outlier | 2026-08-01 | Marketing site | `ScenarioStat` threw on an unwrapped modelled figure only when `process.env.NODE_ENV === "development"`. A preview deployment is built with `NEXT_PUBLIC_SCENARIO_MODE=true` **and** `NODE_ENV=production` — which is the only configuration in which modelled figures render at all. The guard was therefore disabled in precisely the build it existed to protect: a stat placed on a page that forgot `<ScenarioNotice>` would render `121` with no `Modelled` badge and no "Preview build" band, to the investor or mall operator the preview is for. The component's own docblock asserted the opposite, reasoning that production is safe because the fallback returns first — true, and irrelevant to the preview case it overlooked. Found in review of PR #153 | **Closed** by dropping the `NODE_ENV` condition entirely (`maanta-app/src/components/marketing/ScenarioStat.tsx`): the throw now fires in any build where a modelled figure would render unlabelled. Real production is unaffected — `NEXT_PUBLIC_SCENARIO_MODE` is unset, so the fallback returns on the preceding line and the guard is unreachable, meaning no visitor can be shown a crash in place of honest copy. Guarded by `maanta-app/src/lib/__tests__/scenario-stat.test.ts`, which asserts the render matrix behaviourally and asserts that the guard reads no `NODE_ENV` at all. That second assertion exists because `NODE_ENV` cannot be varied within one vitest process — React binds `react/jsx-dev-runtime` at module load — so the property is checked at its source rather than simulated | eng |

| D45 | closed | process | 2026-08-01 | Guard integrity | `scripts/check-tokens.mjs` was described — in `CLAUDE.md`, in `docs/ops/website-handoff.md` §8 and in the implementation report — as failing the production build if any `{{TOKEN}}` reaches rendered output. It scanned `.next/server/app` for `.html`, `.rsc` and `.body` only. Most routes in this app build as `ƒ` (dynamic) and emit **no HTML at build time**, and a token inside a client component ships in a JS chunk rather than in any HTML at all, so two whole classes of token could reach a visitor while the gate printed "clean". Same family as D38 and D43: a check whose reported success did not mean what its callers believed. Raised in review of PR #153 | **Closed by a second pass over compiled JS** (`.next/server/app`, `.next/server/chunks`, `.next/static/chunks`) with a narrower pattern — `{{SCREAMING_SNAKE}}`, which is MAANTA's token convention — because vendor libraries bundle lowercase `{{url}}`, `{{key}}`, `{{auto}}` and `{{source}}` placeholders of their own and failing on those would make the gate noise. Served documents keep the original any-token pattern. Proven on both missed classes: a token planted in a client component fails on `.next/static/chunks/app/(marketing)/waitlist/page-*.js`, and one planted in a **server** component on the same dynamic route — which reaches no chunk directory and no prerendered HTML — fails on `.next/server/app/(marketing)/waitlist/page.js`. Coverage went from 47 files to 47 rendered files plus 392 compiled chunks. Source maps are excluded: they retain comments, and `facts.ts` discusses token names in prose. The scan scope itself is guarded by `maanta-app/src/lib/__tests__/check-tokens-scope.test.ts` — the script fails CI on any token in scope, but nothing else would notice the scope being narrowed back | eng |

| D46 | closed | code-outlier | 2026-08-01 | Marketing site | Every marketing page footer carries `PrelaunchNotice` — "Pre-launch demonstration. MAANTA is not yet trading." — while two surfaces asserted the opposite: the OG image footer read "Live at BBS Mall, Eastleigh · Nairobi", and the root metadata description read "Now live at BBS Mall, Eastleigh." Both render **before** the page does — an OG image is what a WhatsApp forward shows, and the description is the search-result snippet — so they are precisely the surfaces the footer disclosure cannot reach. In a market where WhatsApp is how these links travel, the contradicting claim reached more people than the page that qualified it. `demo-mode-spec.md` §2a does sanction the "Live at" string, but as the `#hero` status line **on `/mall-operators`**, above that page's footer; reading the sanction as covering any surface is what let it onto these two. Raised independently by two reviewers on PR #153 (CodeRabbit, and the Cursor audit as "'Live at BBS Mall' vs prelaunch footer") and **dismissed once** on the grounds that the string was spec-sanctioned — a check of the string that never checked the surface | **Closed** by deriving both from `DEMO_MODE`: `OG_STATUS_LINE` in `maanta-app/src/lib/marketing/og.tsx` states the location without a trading claim, and the description in `maanta-app/src/app/layout.tsx` says "Launching at". Flipping `DEMO_MODE` to false at launch restores both. Guarded by `maanta-app/src/lib/__tests__/prelaunch-consistency.test.ts`, which asserts the invariant rather than the strings — no surface may assert trading while `DEMO_MODE` holds — and first asserts that `PrelaunchNotice` still says "not yet trading", so the suite cannot pass vacuously if that copy changes. Verified by reinstating the original line and watching it fail | founder |

| D37 | closed | process | 2026-08-01 | Release | `main` and the production deployment had diverged in **both** directions, so neither was a superset of the other. `origin/main` was `314b5ef8`; production served `038e3bc0` from `claude/maanta-marketing-site-y8fesm`. `main` was missing three commits including `038e3bc0`, the fix that closed two vacuously-passing marketing guards — confirmed in the files, not merely inferred from the commit graph: at `origin/main` the comment stripper in `maanta-app/src/lib/__tests__/marketing-shell.test.ts` still treated `://` as a line comment, and `copyText()` in `maanta-app/src/lib/__tests__/pricing-copy.test.ts` performed no comment stripping at all. So `main`'s suite was green for the wrong reason while CLAUDE.md presented those guards as enforced. Production, conversely, was missing `314b5ef8` (#154), which changed only `.gitignore` and a docs file. **Proved by mutation:** planting `https://wa.me/254700000000` into a marketing page failed the merged guard and **passed 7/7 against `origin/main`'s copy**, so `main` would have shipped the exact placeholder number D36 exists to keep out | **Closed 2026-08-01 — both stated conditions verified, not assumed.** The row required `git merge-base --is-ancestor <production-sha> origin/main` to succeed **and** Vercel to report `githubCommitRef: main`. Both now hold. Production is `dpl_4Zq5F3UK4QgpmeThCQzbgLqCrfqz`, `state: READY`, `target: production`, `githubCommitRef: main`, `githubCommitSha: b6f19716`, holding `www.maanta.app` and `maanta.app`; and `git merge-base --is-ancestor b6f19716 origin/main` passes, trivially, because it is `main`'s tip. The three commits this row named as missing are each now ancestors of `main`, checked individually: `480bdf6`, `6a22c0f` and `038e3bc0` all return true. The guard vacuity that made this row urgent was separately fixed and is covered by D38, whose remedy — one shared comment lexer at `maanta-app/src/lib/__tests__/helpers/comment-stripping.ts` imported by every copy guard — is what stops the underlying defect returning. Closing this row does **not** mean branch-served production cannot recur: it did recur the same day by a different route, tracked separately as **D53**. no guard: no test can assert which commit a Vercel production alias serves; that state lives outside the repository, and the honest control is that `main` pushes drive production deploys automatically | founder |

| D38 | closed | code-outlier | 2026-08-01 | Testing | `maanta-app/src/lib/__tests__/held-claims.test.ts` is cited across this repo as the guard that keeps §9 held claims out of shippable content, but its `codeOnly()` helper strips line comments with `.replace(/\/\/.*$/gm, "")` — which truncates every line from the first `//`, including the `//` inside any `https://` URL. Any held claim sharing a line with a URL is therefore invisible to the scanner. This is the identical defect that `038e3bc0` fixed in `maanta-app/src/lib/__tests__/marketing-shell.test.ts` and `maanta-app/src/lib/__tests__/pricing-copy.test.ts`; that commit patched two of the three files carrying it and left this one, on both branches. Neither the gap audit nor the finish plan records it | **Closed 2026-08-01.** `lineCommentAt()` ported into `maanta-app/src/lib/__tests__/held-claims.test.ts`, and `codeOnly()` rewritten to strip line comments line-by-line through it instead of by blanket regex — so the fix covers all three assertions the helper feeds, not just the `HELD`-pattern scan. **Verified by mutation in both directions, against both trees.** Positive: a held claim (`anything left in your balance stays yours`) planted on the *same line as an `https://` link* — the exact shape the old stripper deleted before scanning — turns the guard red; **the identical mutation passes 5/5 against `origin/main`'s copy of the file**, which is the blindness demonstrated rather than argued. Negative controls, both still green so the fix does not over-correct: the same claim inside a `{/* … */}` JSX comment, and after a genuine `//` line comment, are each still correctly ignored. De-vacuuming it revealed **no** hidden violation — the guard was blind but the content behind it was clean, the same outcome as the two files `038e3bc0` fixed. Suite 481/481 after the change. **The triplication is also gone**, in the follow-up change: the one implementation now lives in `maanta-app/src/lib/__tests__/helpers/comment-stripping.ts` and all three guards import it, removing 131 lines of duplicated lexer for 19. A fourth private copy is how this defect recurs, and it was found twice — once by `038e3bc0`, once as this row — so the duplication is the root cause rather than a tidiness point. The helper is deliberately not named `*.test.ts`, because the suite glob in `maanta-app/vitest.config.ts` matches only that suffix and would otherwise collect the helper and run it as an empty suite; the count stays 61 files / 481 tests. Its docblock records the trap and states the scope limit out loud (a small lexer, not literal-aware — a `//` inside a quoted string is still treated as a comment start, which can only ever make a guard miss, never false-positive). **Re-verified after the extraction, since it refactors already-verified guards:** four positive mutations all red (hardcoded `wa.me`; `KES 40 success fee`; a held claim on a line with a link; `Boost any deal` in JSX) and four negative controls all green (each of those planted inside a `{/* */}` or `//` comment, including a `wa.me` URL *inside* a comment — which is the both-directions case, since the stripper must ignore `//` in `https://` while still honouring a genuine `//` comment that contains a URL) | eng |

| D39 | open | doc-stale | 2026-08-01 | Marketing site | `docs/ops/marketing-site-gap-audit.md` GAP-03 records `/how-it-works` as a live **rewrite** serving `/shoppers` at a second URL, on the evidence of an HTTP 200 with `x-matched-path: /shoppers` and a byte-identical body. The repo says otherwise: `maanta-app/next.config.mjs` declares `{ source: "/how-it-works", destination: "/shoppers", permanent: true }` inside `redirects()`, which is a 308, and `rewrites()` contains only the three PostHog `/ingest/*` proxy rules. The audit's observations are also exactly what a **followed** 308 looks like to a redirect-following HTTP client, and the PostHog pageviews it cites are explained by `/how-it-works` having been a real page until the marketing rebuild (`docs/skills/e2e-surface-matrix-2026-07-30.md`). So either the finding is a measurement artifact or something outside the repo is intercepting the path | Resolve with `curl -sI` and redirect-following **off**: a 308 closes this row and closes finish-plan Step 3 with no code change; a genuine 200 means the cause is outside `maanta-app/next.config.mjs` and must be found in the Vercel project configuration before anything is edited. **Do not edit the redirect on the strength of the audit alone.** Reasoning in `docs/ops/marketing-site-repo-map.md` §4 | eng |

| D40 | closed | code-outlier | 2026-08-01 | Marketing site | Not one page on the marketing site emits `<link rel="canonical">` — there is no `alternates` key anywhere under `maanta-app/src/app/(marketing)/`. Separately, `maanta-app/src/app/layout.tsx` sets `openGraph.url` to the bare site origin rather than each page's own URL, so every share of a sub-page canonicalises its unfurl to the homepage; and because a page-level `openGraph` object **replaces** the parent's wholesale in the App Router rather than merging field-by-field, the five pages that declare their own lose `og:url`, `og:site_name`, `og:locale` and `og:type` entirely. The site therefore claims per-page social identity it does not have. `metadataBase` is already correctly set, so this is narrower than it first appears | Add a shared metadata helper setting `alternates.canonical` and spreading the shared `openGraph` base before per-page overrides, and route all 15 marketing pages through it. The helper must read the same `NEXT_PUBLIC_APP_URL` fallback constant as `maanta-app/src/app/layout.tsx`, or preview deployments will emit production canonicals. **Closed 2026-08-01 (finish-plan Step 4).** `maanta-app/src/lib/marketing/page-metadata.ts` builds every marketing page's `Metadata`: `alternates.canonical` plus a shared `openGraph` base (`type`, `siteName`, `locale`) spread in **before** the page's overrides, so a page can restyle its social title without silently dropping the other four fields. **All seventeen** marketing `page.tsx` files now go through it — enumerated, not sampled: `find src/app/(marketing) -name page.tsx` returns seventeen, and all seventeen call `pageMetadata`. Two of them reached this row by a different path than the one originally written here. `/pricing` had no metadata at all when this fix was authored and `/merchants/join` was a `"use client"` module that could not export any; both were fixed independently in the intervening work (**D52**, and the `/pricing` card rebuild), so this pass wraps metadata they already had rather than creating it. The `/merchants/join` server/client split therefore predates this change — `maanta-app/src/app/(marketing)/merchants/join/join-form.tsx` already existed, mirroring the `waitlist` shape, and is unchanged here. `path` is passed **relative** and resolved against the root layout's `metadataBase`, so this module holds no second copy of the origin and a preview deployment cannot emit production canonicals. GAP-05 fixed in the same pass: `/mall-operators` and `/about` no longer split one sentence across `og:title` and `og:description`, both halves recombined from each page's own approved copy. **Verified in built HTML, not source** — 16 prerendered routes each carry a canonical and `og:url` equal to their own URL, with all three OG base fields; `/waitlist` is dynamically rendered so it has no build artefact to read and is reported by name rather than skipped. Guarded by `maanta-app/scripts/check-canonicals.mjs`, chained into `npm run build` beside the token gate — **not** a vitest suite, because CI runs `test` before `build` and a test reading `.next/` would fail there. It derives its expectations from the generated sitemap, so "sitemap URL and page canonical agree" is enforced rather than coincidental, and it refuses to report success when it cannot do its job: missing build dir, truncated sitemap, or fewer than 14 routes inspected all exit 1, and it asserts in the inverse direction that the 404 has **no** canonical. Mutation-tested four ways — canonical removed, `og:url` reverted to the bare origin, `og:site_name` dropped, canonical wrongly added to the 404 — each turns it red; and two vacuity controls (absent build dir, sitemap with zero pages) make it refuse rather than pass, which is the specific failure D38 and the three strippers before it were made of. **The wiring is guarded too**, by `maanta-app/src/lib/__tests__/build-gates.test.ts`: a post-build gate is only as durable as the `build` script that invokes it, and deleting the `&& npm run check:canonicals` would have disabled it silently with a green suite and a green build. That test asserts both output gates exist, are named in `build`, run **after** `next build`, and are joined with `&&` rather than `;` so a failure actually fails the build — mutation-tested by unchaining one and by re-joining with `;`. It also closes the same hole for `check-tokens.mjs`, which had shipped unguarded in exactly that way since it was written | eng |

| D41 | closed | code-outlier | 2026-08-01 | Marketing site | `/contact` is a target marketing page whose enquiry form **does not exist in server HTML**. `maanta-app/src/components/marketing/EnquiryRouter.tsx` is a client component calling `useSearchParams()` to read `?topic=`, and `maanta-app/src/app/(marketing)/contact/page.tsx` wraps it in a `Suspense` whose fallback is a bare pulsing rectangle — so the rendered page ships zero `<form>` and zero inputs, directly above server-rendered copy promising "This form and email — We reply within 1 business day". Without JavaScript the page promises a form it never renders. This is **D28's successor**: D28 was a form that POSTed nowhere while telling the sender it had arrived, closed by wiring `/api/contact` to Resend; the form is now correctly wired and is no longer rendered. The same failure mode, one layer down | Move the topic-dependent behaviour off the render path so the form markup is server-rendered, following the in-repo precedent — `maanta-app/src/app/(marketing)/waitlist/waitlist-form.tsx` prerenders because it is a client component that never calls `useSearchParams()`, **not** because it is a server component, which the finish plan states incorrectly. Add a `<noscript>` naming WhatsApp and the support email. Close by citing a guard asserting `/contact`'s built HTML contains a `<form>` and zero `BAILOUT_TO_CLIENT_SIDE_RENDERING` markers. Note the analytics half needs no work: `marketing_form_submitted` already exists in `maanta-app/src/lib/marketing/analytics-events.ts` and is already called — it has simply never fired, because the form has never rendered. Detail in `docs/ops/marketing-site-repo-map.md` §6. **Closed 2026-08-01 (finish-plan Step 5).** `EnquiryRouter` no longer calls `useSearchParams()`; it reads `?topic=` from `window.location.search` in a mount effect, which never runs on the server and so cannot affect rendered HTML. The `Suspense` boundary and its pulsing-rectangle fallback are deleted from `maanta-app/src/app/(marketing)/contact/page.tsx` — with nothing suspending, a boundary there would only be a fallback with nothing to fall back from. **`/contact` stays a static prerender** (`○ /contact` in the build output), which the alternative in `marketing-site-repo-map.md` §6 — taking `searchParams` as a page prop — would have cost: that is what makes `/waitlist` dynamic and is why it has no build artefact for the canonical guard to read. The behaviour given up is reactivity to a mid-page `?topic=` change; the only inbound links carrying the parameter are the two `/mall-operators` CTAs, both to `?topic=mall-operator`, and nothing on `/contact` rewrites it, so the reactive dependency never fired. A `<noscript>` block now names WhatsApp and the support email, because the form renders without JavaScript but submits through a JS handler and cannot send — saying so is the honest state rather than a second silent promise. **Verified in built HTML, not source:** `.next/server/app/contact.html` went from 0 `<form>`, 0 inputs, 1 `BAILOUT_TO_CLIENT_SIDE_RENDERING` and a bare `animate-pulse` div, to 1 `<form>`, 5 input controls, **0** bailout markers, 0 `animate-pulse`, and the router heading present. Guarded by `maanta-app/scripts/check-server-forms.mjs`, chained into `npm run build` beside the token and canonical gates — **not** vitest, because CI runs `test` before `build` and a test reading `.next/` would fail there. It asserts a `<form>`, at least one input control, a string only that page's own form renders (so a header search box could not mask a regression), and zero bailout markers; it treats a missing artefact for a route it lists as prerendered as an **error**, not a skip, since that would mean the route silently went dynamic. `/waitlist` is dynamic and is named in the output rather than dropped. **Mutation-tested six ways**, five against built output — `<form>` removed, bailout marker injected, the form's own copy removed, the artefact deleted, the build directory absent — and, decisively, by reverting both source files to the pre-fix state and running a real `next build`: the guard reports all four `/contact` problems and fails the build, so had it existed, D41 could not have shipped. The wiring is guarded by `maanta-app/src/lib/__tests__/build-gates.test.ts`, whose `&&`-joining assertion was rewritten to derive from its gate table rather than name two scripts literally — otherwise adding this third gate would have left it unchecked, which is the same "the guard only covers what someone remembered" failure D52 closed | eng |

| D42 | closed | doc-stale | 2026-08-01 | Legal | The repo states in three places that the four legal routes carry a `noindex` meta tag — the docblock in `maanta-app/src/app/robots.ts`, the identical docblocks on each legal page, and `maanta-app/src/lib/__tests__/marketing-a11y.test.ts`, which asserts the `robots: DEMO_MODE ? { index: false` source pattern on all four. `docs/ops/marketing-site-gap-audit.md` §6.2 reports, from rendered production HTML, that none of the four emits one. Both cannot be true. The contradiction is sharp because `DEMO_MODE` in `maanta-app/src/lib/marketing/demo.ts` is a plain constant driving **both** the page `noindex` and the `robots.txt` disallow list, and the audit confirms the disallow lines are live — so the constant was `true` at build and the tag should have rendered. Either the audit's scan missed a `name="robots"` tag while looking for `property=`, or the tag genuinely is not emitted and the a11y guard is vacuous in exactly the way rule-2 guards are meant to prevent | **Closed 2026-08-01 — the repo was right and the audit finding is refuted.** Production HTML was fetched for `/privacy` and `/cookies` (via the Vercel deployment fetch, since the network policy blocks direct egress to the host) and **both emit `<meta name="robots" content="noindex, nofollow"/>`**. The audit's §6.2 premise — "carry no `noindex` meta tag" — is wrong; the tag is `name="robots"`, and the audit's other head checks look for `property=`, which is the likely cause of the miss. Guarded by `maanta-app/src/lib/__tests__/marketing-a11y.test.ts`, whose source-level assertion is hereby shown to correspond to rendered output rather than being vacuous — the one guard confirmed against render, not merely read. **Consequence for finish-plan Step 7b: LEG-02 is not a defect and no `noindex` may be added.** The founder ruling narrows to one question — keep or remove the four `Disallow` lines in `maanta-app/src/app/robots.ts` — with `noindex` unaffected by either answer. **Do not flip `DEMO_MODE` to resolve it**: that flip also publishes the legal routes to the sitemap and removes every pre-launch disclosure sitewide. Same pass confirmed LEG-01 exactly: three `PendingValue` markers live, `{{CLERK_REGION}}` and `{{SENTRY_REGION}}` on `/privacy`, `{{AUTH_COOKIE_LIFETIME}}` on `/cookies`, matching `TOKEN_OWNERS` in `maanta-app/src/lib/marketing/legal-docs.ts` with nothing extra. Analysis in `docs/ops/marketing-site-repo-map.md` §8 | founder |
| D47 | closed | doc-stale | 2026-07-30 | Process | `CLAUDE.md`'s Commands section listed only `dev`, `test` and `build`, and called vitest the thing that "also runs in GitHub Actions CI". CI blocks on five things — lint, typecheck, test, build and the `db-tests` SQL job — so the one document every session is told to read described a green run that CI would reject. The nearest miss is a SQL change: nothing in the commands list pointed at `make db-verify`, so a migration could be written, `npm test`-ed, and reported verified without its assertion suite ever running | Commands section rewritten against `.github/workflows/ci.yml` and the root `Makefile`: all five gates named, `make db-verify` named as the check for migrations, plus an explicit line that passing only `npm test` is not verified. no guard: a test could in principle diff the commands list against the CI workflow, but the list is prose with per-command context, not a machine-readable set — the honest guard is that the gates themselves are in CI and fail loudly | eng |
| D48 | closed | doc-stale | 2026-07-30 | Auth | `CLAUDE.md` described the app as "Next.js (App Router) + Supabase" and named Clerk nowhere, while `maanta-app/src/middleware.ts` runs `clerkMiddleware()` on every path and Clerk is the launch auth strategy. A session orienting from `CLAUDE.md` alone would form the wrong model of how routes are gated — the exact confusion `AGENTS.md` documents at length (placeholder Clerk keys make every browser navigation fail the handshake). The `MAANTA_AUTH_STRATEGY` toggle was likewise absent | Layout table now names Clerk in the stack, and a one-line auth summary points at `docs/ops/auth-strategies.md` and `docs/skills/clerk-auth.md`. no guard: the drift was an incomplete sentence in a guidance document; no test can assert that prose describes the stack. The behavioural truth is the middleware, which is unchanged | eng |
| D49 | closed | doc-stale | 2026-08-01 | Ops | `docs/maanta-launch-readiness-tracker.md` — the file its own header calls the gate-status source of truth — described production as "live and **schema-aligned**" in two places: the 2026-07-29 audit block and the E10 gate row. That was accurate on 2026-07-29 and was overtaken the next day by D24 and D25, and on 2026-08-01 by D37. The phrase then propagated: it was copied into `CLAUDE.md` by the 2026-08-01 operating-standard session, which is how a stale tracker line became the orientation every future session reads first | Corrected in place: a dated 2026-08-01 note at the top states production is live and serving but not a clean mirror of `main` and names D24/D25/D37; the 07-29 block is kept but marked a dated record whose schema-aligned finding no longer holds; E10 no longer asserts alignment. New row **E17** tracks the reconciliation itself. no guard: no test can assert that an operational status line still matches a live database — the guard is the read-back named in E17 (`pg_get_functiondef` for `claim_deal` must contain `deal_paused`, and the ledger must match the repo's filenames), which is what closes D24 and D25 | founder |
| D50 | open | product-decision | 2026-08-01 | Marketing site | `CLAUDE.md` states that the demo-data banner "stays on `(shopper)/layout.tsx` and `merchant/(app)/layout.tsx`, where synthetic deal rows actually render", and `maanta-app/src/lib/__tests__/marketing-shell.test.ts` enforces its absence from marketing routes in both directions (D33). The premise under that rule — that no synthetic deal rows render on a marketing route — stopped being true on 2026-08-01. The home hero now renders `maanta-app/src/components/marketing/HeroShot.tsx`, a CSS mockup of the shopper feed containing three invented shops with invented prices, on `/`. So a marketing route displays fabricated deal rows while the banner whose whole purpose is to disclose exactly that is, correctly per D33, not mounted there. The rule is not violated as written; its justification is what no longer holds, and a future session reading D33 would conclude marketing carries no synthetic content | **Founder decision 2026-08-01**, taken with the alternatives on the table (a mechanic-only mockup with no invented merchants, or a scaffold awaiting a real capture) and this consequence named before the choice rather than discovered after. Disclosed rather than silent: `HeroShot` renders a visible caption "Illustration · example shops and prices" directly under the mockup, plus an `sr-only` sentence stating the shops and prices are invented examples; the mockup body is `aria-hidden` so assistive tech gets that sentence instead of walking invented listings as though they were a real feed. Guarded by `maanta-app/src/lib/__tests__/marketing-hero-shot.test.ts`, **proved non-vacuous by mutation**: deleting the caption fails the guard, moving the caption into a JSX comment fails it (the string is still present in the file, so this is the case a naive `includes()` would miss), and rendering a sample price in `text-brand` fails it. Scope limit stated in that file rather than left to be found — it reads source, not built HTML, because `.github/workflows/ci.yml` runs `npm run test` before `npm run build` so `.next/` does not exist at test time; a guard that skipped on a missing directory would pass vacuously every CI run. Close by replacing the mockup with a real capture of the live feed at Node 0 once BBS carries real deals, at which point the caption and this row both go, or by reverting to a mechanic-only illustration. **Residual risk that cannot be guarded:** an invented shop name could coincide with a real Eastleigh business, which would turn an illustration into a claim about that business — the names were chosen to be generic for this reason, but no test can check it | founder |
| D51 | open | code-outlier | 2026-08-01 | Marketing site | The Elite launch offer is stated in two places that cannot agree. `maanta-app/src/app/(marketing)/merchants/page.tsx` renders it from `OFFERS.eliteTrial` in `maanta-app/src/lib/marketing/facts.ts` and wraps the whole block in `isOfferLive(...)`, so a closed offer disappears rather than going stale — risk R7, which the marketing build treats as a rule. `maanta-app/src/app/(marketing)/pricing/page.tsx` does neither: it types the cohort size, the trial length and the grace period as literals in JSX, and states the offer unconditionally with no expiry gate. Two consequences, one already true and one dated. Already true: four numbers duplicate constants that exist, against the marketing hard rule that every number renders from `facts.ts` — and `pricing-copy.test.ts` anticipates exactly this in its own comment, noting that rendering `cohortShops` "states it more reliably than a literal 100 does". Dated: when `OFFERS.eliteTrial.expiresOn` passes, `/merchants` drops the offer and `/pricing` keeps promising it, which is the D34 failure mode — one commercial fact, two pages, no shared source | **Both halves fixed on the pricing page 2026-08-01.** `/pricing` now reads `OFFERS.eliteTrial.cohortShops`, `.days` and `.postTrialGraceDays` and renders the offer block only when `isOfferLive(OFFERS.eliteTrial)`, matching `/merchants`. No visible change today — `expiresOn` is 2026-10-31, so the block still renders; the change is that it will now vanish on the same day `/merchants` drops it instead of outliving it. Guarded by the existing `maanta-app/src/lib/__tests__/pricing-copy.test.ts`, whose launch-offer assertion already required the cap, the node and the success-fee caveat on every page that mentions the trial, and which reads source rather than rendered output so gating the block does not make it vacuous. That guard needed one widening to accept the single-sourced form: its node check tested for a literal `BBS`, which no page rendering `FACTS.launchMall` can satisfy, so it now also accepts `launchMall` — the same accommodation its cap check already made for `cohortShops`, and made for the same reason. Verified by mutation: removing the node reference from `/pricing` fails the guard, and removing the success-fee caveat fails it. Row stays **open** until the offer actually expires and both pages are confirmed to have dropped it together, since that is the behaviour being claimed and it cannot be observed before 2026-10-31 | eng |
| D52 | closed | code-outlier | 2026-08-01 | Marketing site | `maanta-app/src/lib/__tests__/marketing-a11y.test.ts` states its intent as "Every top-level marketing page needs its own title and description, or it inherits the root's and every page shares one snippet in search results", then enumerates ten specific paths. Two marketing pages that ship are absent from that list and have no `metadata` export at all: `/pricing` and `/merchants/join`. So the guard reads as covering the site while leaving the commercial pricing page and the merchant lead-capture form sharing the homepage title and description. This is narrower than D40, which is about canonical and `og:url` across every page; this is the per-page title and description being absent entirely on two of them | **Closed 2026-08-01, and the enumerated list is gone rather than extended.** `/pricing` got its own `metadata` first; `/merchants/join` needed more than an export, which is why it lagged: it is a `"use client"` component, and Next.js forbids `export const metadata` in one — the real reason it never had metadata, not an oversight. It is now split on the `/waitlist` precedent, a server `page.tsx` exporting metadata around `join-form.tsx` holding the form. The split was checked against **D41**, the failure it most resembles: the form does **not** call `useSearchParams` and is not wrapped in `Suspense`, and the built HTML at `.next/server/app/merchants/join.html` contains a `<form>`, the "Shop name" and "Get started" strings, and **zero** `BAILOUT_TO_CLIENT_SIDE_RENDERING` markers — so it still server-renders. **The guard now walks every `page.tsx` under `(marketing)/` instead of a hand-maintained array**, which is the actual fix: a list only checks what someone remembered to add, and both misses were misses of memory. It accepts `generateMetadata` as well as static `metadata`, since a dynamic route would legitimately use it and a guard that failed such a page would teach the next author to delete the check. All 17 marketing pages pass. **Proved non-vacuous by mutation**, including the case the old list could never have caught: creating a brand new page with no metadata fails the guard, and removing the metadata export from `/merchants/join` fails it. One consequence worth recording — the split broke `maanta-app/src/lib/__tests__/marketing-analytics.test.ts`, which asserted the `formSubmit` tracking lived in `page.tsx`; it moved with the form and the guard caught it, which is precisely what that guard is for, since an untracked merchant lead form is invisible in the funnel. Both references now point at `join-form.tsx`. Suite 515/515, build and check-tokens clean | eng |
| D53 | closed | process | 2026-08-01 | Release | While #161 was open and unmerged, production was manually promoted to serve it. Vercel deployment `dpl_kwm9shnHG6T1M17syCDfrKjjeSqW` was created with `action: promote`, `target: production`, and the aliases `www.maanta.app` and `maanta.app`, built from `githubCommitRef: claude/install-superpowers-plugin-na574p` at `githubCommitSha: 8bd96374`. So the public site was served from an open pull request branch, not from `main`. This is the same shape as D37 but a different cause: D37 was a stale branch deployment left behind by an unmerged reconcile, this was a deliberate dashboard promote of a live PR branch. Worth separating because the fix differs — D37 needed commits merged, this needed only the PR merged and a redeploy. The content was never wrong: the tree at `8bd96374` and the tree now on `main` are the identical object `69a462ebfac7babceb06e007a2ce82fa946746c3`, so the site served correct code throughout. What was wrong was provenance — `main` was no longer what was deployed, and a later deploy from `main` before the merge would have silently reverted the live site | **Closed 2026-08-01.** Resolved by merging #161 rather than by rolling back, on the author's instruction. The merge pushed `b6f19716` to `main`, which auto-triggered `dpl_4Zq5F3UK4QgpmeThCQzbgLqCrfqz` with `source: git`, `target: production`, `githubCommitRef: main`, `githubCommitSha: b6f19716`. Confirmed READY, and its alias list now contains **both** `www.maanta.app` and `maanta.app` — verified by reading the deployment, not inferred from the merge. No manual redeploy was needed; Vercel deploys `main` pushes automatically, which is itself the reason the divergence was only ever one merge deep. **Note for next time:** a squash merge mints a new SHA, so the promoted branch commit `8bd96374` never becomes an ancestor of `main` and an ancestry check against it will fail forever even though the content is identical. Compare trees, not commits, when auditing whether a squash-merged deployment matches `main`. no guard: no test in this repo can assert what a Vercel production alias points at — the deployment target lives outside the tree, and a vitest file asserting against the Vercel API would be a network call in CI. The real control is that production deploys are driven by `main` pushes by default, so this only recurs when a human promotes a branch by hand. **Correction appended 2026-08-01: that control was tested by events the same day and did not hold — see D56.** It recurred roughly four hours later, and worse: that time the promoted tree and `main`'s genuinely differed, so a push to `main` would have silently reverted a shipped fix. Do not read this row's `no guard` as evidence the risk is contained. Read-backs for **all three** occurrences, including this one's, are preserved in `docs/ops/production-branch-promote-2026-08-01.md` | founder |
| D54 | open | code-outlier | 2026-08-01 | Design system | Every icon size override in the app is silently discarded. `Svg` in `maanta-app/src/components/ui/icons.tsx` renders `cn("h-5 w-5 shrink-0", className)`, and `cn()` in `maanta-app/src/lib/ui.ts` is `parts.filter(Boolean).join(" ")` — a plain join with no Tailwind conflict resolution, and `tailwind-merge` is not a dependency. Both the default and the caller class land in the attribute and the cascade decides, which `h-5` wins. **Measured, not inferred:** on `/pricing` the tick requested `h-4` (16px) and rendered at 20px, computed in a real browser. 79 call sites pass their own `h-`/`w-`; 56 of them ask for something other than 20px and get 20px anyway — 44 that should be smaller (`h-3`, `h-3.5`, `h-4`) and 12 that should be larger (`h-6`, `h-7`, `h-8`). So the design system reads as though icon sizing is per-call-site when it is in fact fixed, and every one of those class strings is a false statement in the source | **Fixed in code 2026-08-01; row stays open until the visual pass.** `Svg` now applies each default only when the caller supplies that axis — `!setsDimension(className, "h") && "h-5"`, same for `w` — so a default and an override never collide. Each axis is resolved independently, because a caller passing only `w-full` still wants the default height. The predicate is anchored on a word boundary so `min-h-0` and `max-w-full` are not mistaken for a size, which would drop the default and leave the icon dimensionless — a worse bug than the one being fixed. Guarded by `maanta-app/src/lib/__tests__/icon-sizing.test.ts`, which covers both failure directions and also asserts against the real source that the unconditional `cn("h-5 w-5 shrink-0", className)` has not returned, so the mirrored predicate cannot pass while the component regresses. Suite 68 files / 520 tests. **Measured effect, recounted precisely rather than estimated:** 80 icon elements pass an `h-` size, 24 already asked for 20px and are unchanged, and **56 across 26 files now render at the size they always requested** — 28 to 16px, 13 to 14px, 3 to 12px, 2 to 24px, 2 to 28px, 8 to 32px. Verified in a browser on the marketing routes, the only ones reachable without auth: no icon renders dimensionless anywhere (`noSize: 0`), and the header logomark now renders at its requested 28px instead of 20px, which is a visible improvement against the wordmark. **What keeps this row open:** the merchant, shopper and ops surfaces cannot be rendered from a Claude session because they sit behind auth, and they include the counter-side money screens — `redeem-keypad`, `topup-flow`, `redemption-result`, `tickets/[id]` — where icons grow 20px to 32px. Close by citing a human confirmation that those were viewed. no guard: no test can assert a rendered pixel size on an authenticated route; CI runs no browser for unit tests, and the e2e golden path does not cover icon geometry| eng |

| D55 | closed | doc-stale | 2026-08-01 | Marketing site | The two documents that exist to correct the finish plan both misdescribe `/waitlist`, and they misdescribe it in the direction that sets a trap. `docs/ops/marketing-site-repo-map.md` §6 says `waitlist-form.tsx` "prerenders because it is a client component that **does not** call `useSearchParams()`", and **D41**'s own remedy column repeats it as the in-repo precedent to follow. `/waitlist` does not prerender. `maanta-app/src/app/(marketing)/waitlist/page.tsx` accepts a `searchParams` prop to preset the segment, and accepting `searchParams` opts a route into dynamic rendering — it is `ƒ /waitlist` in every build, which is exactly why `scripts/check-canonicals.mjs` has to report it as having no build artefact to inspect. The generalised lesson those documents draw is right and is what Step 5 applied; the sentence supporting it is wrong about the shipped route. The trap is specific: a reader told "copy `/waitlist`, it prerenders" would copy the `searchParams` prop, get a form in the HTML, see the defect fixed, and not notice they had turned `/contact` dynamic and dropped it out of the canonical guard's coverage — trading a visible defect for an invisible one | **Closed 2026-08-01, in the same pass that closed D41 and by not falling into it.** Step 5 took the alternative the repo map names one paragraph later — read the parameter in an effect — precisely because that keeps the prerender, and `/contact` is still `○` in the build output. Both statements corrected in place: `marketing-site-repo-map.md` §6 now separates the component (which would prerender on its own) from the route (which does not, because of the page around it), and D41's remedy text is left as written with the closure text naming the correction, per the append-only rule. `maanta-app/scripts/check-server-forms.mjs` states the fact in its own docblock and lists `/waitlist` under `DYNAMIC_WITH_FORMS`, printing it on every build, so the next reader meets it as build output rather than as prose. no guard: the drift is a sentence in a correction document, and no test can assert that prose describes a route accurately. The nearest machine-checkable proxy already exists in both build gates — each names `/waitlist` as having no artefact on every run — and would start disagreeing loudly with the corrected prose only if `/waitlist` were made static, which is a change worth noticing anyway | eng |

| D56 | open | process | 2026-08-01 | Release | **Second manual promote of an open PR branch to production in one day, and the control named in D53 is what failed.** Deployment `dpl_ALPrV9RBGrbncco8YE2bzpsF7jMd` was created with `action: promote`, `target: production`, `source: redeploy`, from `githubCommitRef: claude/install-superpowers-plugin-na574p` at `githubCommitSha: 602323e` — an **open** PR (#166) — and took the aliases `www.maanta.app` and `maanta.app`. D53 recorded the first occurrence and closed it `no guard`, reasoning that "production deploys are driven by `main` pushes by default, so this only recurs when a human promotes a branch by hand". That reasoning was correct and the control was still insufficient: it recurred within roughly four hours. **This occurrence was materially worse than D53's.** There the promoted tree and `main`'s tree were the identical object, so the site served correct code throughout and only provenance was wrong. Here the trees genuinely differed — deployed `a959d669`, `main` `ed3b3d4d`, two commits apart — so `main` did not contain the `/contact` fix that production was serving. Any push to `main`, or any deploy triggered from it, would have silently reverted `www.maanta.app` to the state D41 describes: a contact page shipping zero `<form>` elements beneath its own promise of a 1-business-day reply. The window was open from 18:31 until the merge. Note also that the divergence was **not** detectable from the PR: GitHub showed a normal preview deployment, and only the Vercel comment's encoded `previewUrl` — `www.maanta.app` rather than the branch alias — gave it away | **Incident resolved 2026-08-01, but this row stays open because what is unresolved is the control, not the incident.** Resolved the same way D53 was, on the author's instruction: merged #166 rather than rolling back, since rolling back would have reverted the `/contact` fix. The merge pushed `225db23` to `main` and auto-triggered `dpl_HiNU4vwbuhTJfEwoS5uUfPmLs3Xk` with `githubCommitRef: main`, `target: production` — verified by reading the deployment, not inferred from the merge. Tree equality confirmed rather than assumed: `main` and the promoted deployment both resolve to `a959d669`. **Why this is not closed:** D53 closed with a stated control, reality tested that control the same day, and it did not hold. Closing this row on the same reasoning would assert the recurrence risk is handled when nothing has changed to handle it, which is precisely the "we already fixed that" failure this register exists to prevent. What would actually close it is a configuration decision, not a code change and not a document: either disabling manual production promotes for this project, or restricting promote to deployments built from `main`. That is a founder call about a Vercel project setting, so no session can take it. **Until then the operational rule is:** if a branch must go live before merge, merge it — a promote from an open branch is a production state no test in this repo can see, and the only reason all three occurrences were caught is that a session happened to decode a Vercel bot comment. Also worth recording for whoever audits this: compare **trees, not commit SHAs**, since a squash merge mints a new SHA and an ancestry check against a promoted branch commit fails forever even when the content is identical (the D53 note), and conversely identical SHAs are not what proves agreement — tree equality is. **Evidence artifact: `docs/ops/production-branch-promote-2026-08-01.md`**, which holds the full Vercel read-backs for all three occurrences, the tree comparison with its commands, the timeline, and a copy-paste procedure for checking what production is actually serving. It exists because the evidence for this row lives outside the repository — a Vercel deployment record ages out and a session transcript is not a durable record — so without it a future audit of D53 or D56 has nothing to check. **Third occurrence 2026-08-02 07:36Z, recorded in §8 of that artifact: `dpl_6zKSsfbFeVWtrjpvbfKuSvSuEaUz`, promoted from `205a7ed` on PR #167 — the pull request whose only content was the documentation of this problem.** Harmless in itself: the trees differed but the whole diff was two documentation files, nothing under `maanta-app/`, so the rendered site was byte-identical to `main` — established with `git diff --name-only origin/main <sha> -- maanta-app/`, which is the check that separates "trees differ" from "the site is wrong" and should be the first one run next time. Resolved by merging #167. **Three occurrences in under 24 hours, one of which put a shipped fix at risk, and writing the incident down did not change the behaviour** — which is the strongest available evidence that what is missing is the Vercel setting named above and not more prose. Two of the three were harmless, and that is the trap rather than the reassurance: a failure mode that is usually harmless is one people learn to tolerate, and the harmful one was the middle of the three. If promoting the working branch is the intended workflow rather than an accident, this row should be **rewritten as a deliberate practice with its risks named** rather than left as an incident awaiting a control that will never be applied; what is not sustainable is a documented incident class with no control and a recurrence rate of once every eight hours | founder |

---

## How to add a row

1. Take the next unused ID. Never reuse one.
2. State the gap as **claim vs reality**, not as a task. "X says A, code does B" ages
   correctly; "fix X" does not.
3. If you are opening and closing it in the same change, still add the row — the
   register is the record that it was ever wrong.
4. Cite evidence as a repo-root-relative path in backticks. The test resolves it.
5. Bump `Last updated` at the top. The test checks it is not older than the newest
   row.
