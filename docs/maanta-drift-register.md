Warning: truncated output (original token count: 141134)
Total output lines: 358

# MAANTA drift register

Last updated: 2026-09-01 (**D169 production reconciliation.** The grant ratchet passed the fresh-database CI gate and is live: production grants `authenticated` no INSERT, UPDATE or DELETE privilege on `merchant_transactions`. The hosted apply recorded MCP-minted version `20260831081622`, while the repository owns `20260830120000`; D169 remains open solely until that one-row ledger mismatch is repaired under separate founder authorisation and read back as 107/107 by version and name. Earlier: engineering-night reconciliation closed D27, D86, D155, D165, D167, D180, D181, D209, D213 and D217 with named evidence and guards. Explicit founder deferrals and field/device proofs remain open. D208 and D211 closed by B2b; all five executive fee surfaces delegate to the production-applied structured SQL wrappers, present net/gross/reversals separately, and preserve unavailable as unavailable.)

Earlier the same day, from the rebased branch, newest first:

- **founder ruling — Elite subscription pricing de-anchored**: KES 3,500 is not a commercially fixed price and is removed from all nine rendered surfaces plus the `FACTS` constant, with no replacement number authorized and "Pricing coming soon" in its place; **D179 closed** as a commercial ruling rather than a sourcing fix, and **D15 closed as moot** since there is no longer a price to source. The KES 30 success fee is untouched and stays explicit. Guard scans rendered surfaces only, so historical documentation stays writable. Previous: UI screen review across all 88 screens: **D179**, **D180**, **D181** opened — the Elite monthly price typed in three merchant screens rather than read from `facts.ts` (the only unsourced commercial number left, and the one under review for Feb 2027), the merchant's own wallet balance rendered without a currency unit while admin renders the same column through `formatKes`, and the founder console having no error/loading boundary. **D167's three copy defects re-verified as still open**, with item 2 traced to the baseline migration rather than a component. Frozen rule 7 was tested and **holds** — a suspected YOU PAY rounding mismatch was disproved. Previous: second founder-requested polish pass, using `kylezantos/design-motion-principles` and the `webapp-testing` skill from `ComposioHQ/awesome-claude-skills`: **D176**, **D177**, **D178** opened and closed the same day - the declared house easing curve unused in 42 of 44 transitions, the Toggle knob animating `left`, and five error boundaries pinned to the top of a full-height shell. The third was found by rendering the running app in Chromium rather than by reading source. Write-up: `docs/ops/motion-and-app-polish-2026-08-24.md`. Earlier the same day: frontend polish pass using the third-party `leonxlnx/taste-skill`: **D173**, **D174**, **D175** opened and closed the same day — an amber focus ring on nine form controls at 1.66:1 under a guard that only read the stylesheet, eight fields whose only name was a placeholder, and a duplicated reduced-motion rule. All three are accessibility defects the skill's mandatory contrast and label checks surfaced and that both MAANTA's frozen UI rules and the skill agree on; each closes with a ratchet verified to fail on a reintroduced violation. The audit, including the places the skill and MAANTA's founder-approved choices genuinely conflict and were deliberately left alone, is `docs/ops/taste-skill-frontend-audit-2026-08-24.md`. Prior: pre-field adversarial security audit: D168, D169, D170, D171 opened. Verdict CONDITIONAL GO for Merchant 01 — no exposure found. All four rows dispositioned by founder ruling the same day: D168 defer (do NOT restore the merchants grant), D169 fix before the next production migration, D170 staff acceptance required before scaled seat issuance, D171 defer. Cross-tenant, money-path, identity and admin boundaries were each adversarially tested and held. Prior: 2026-08-23 full-role E2E run — D164, D165, D166, D167 opened; D162 opened; D163 closed; D160 + D161 closed; D159 parked; D158 closed and applied, ledger 100/100) **Same day, governance reconciliation:** **D172** opened — the golden-path browser E2E has never executed (167/167 workflow runs skipped, `E2E_BASE_URL` never provisioned). It was **originally recorded in Notion as D168** and is renumbered here after colliding with the security D168 above; the repo is canonical for drift IDs and the security D168 is unchanged in meaning, disposition and evidence. The E2E claim was independently re-verified against `main` and **narrowed**: it is the golden-path money loop rather than a "dashboard" suite, the RPC-level invariants are covered in CI, and the gate is a documented unprovisioned opt-in with two anti-false-green guards — not a broken workflow.) **Ruled the same day: D172 is DEFERRED — it does not block Merchant 01, and becomes a hard gate before MAANTA moves from controlled Node 0 validation into routine or scaled releases.**

- UI screen review across all 88 screens: **D179**, **D180**, **D181** opened — the Elite monthly price typed in three merchant screens rather than read from `facts.ts` (the only unsourced commercial number left, and the one under review for Feb 2027), the merchant's own wallet balance rendered without a currency unit while admin renders the same column through `formatKes`, and the founder console having no error/loading boundary. **D167's three copy defects re-verified as still open**, with item 2 traced to the baseline migration rather than a component. Frozen rule 7 was tested and **holds** — a suspected YOU PAY rounding mismatch was disproved. Previous: second founder-requested polish pass, using `kylezantos/design-motion-principles` and the `webapp-testing` skill from `ComposioHQ/awesome-claude-skills`: **D176**, **D177**, **D178** opened and closed the same day - the declared house easing curve unused in 42 of 44 transitions, the Toggle knob animating `left`, and five error boundaries pinned to the top of a full-height shell. The third was found by rendering the running app in Chromium rather than by reading source. Write-up: `docs/ops/motion-and-app-polish-2026-08-24.md`. Earlier the same day: frontend polish pass using the third-party `leonxlnx/taste-skill`: **D173**, **D174**, **D175** opened and closed the same day — an amber focus ring on nine form controls at 1.66:1 under a guard that only read the stylesheet, eight fields whose only name was a placeholder, and a duplicated reduced-motion rule. All three are accessibility defects the skill's mandatory contrast and label checks surfaced and that both MAANTA's frozen UI rules and the skill agree on; each closes with a ratchet verified to fail on a reintroduced violation. The audit, including the places the skill and MAANTA's founder-approved choices genuinely conflict and were deliberately left alone, is `docs/ops/taste-skill-frontend-audit-2026-08-24.md`. Prior: pre-field adversarial security audit: D168, D169, D170, D171 opened. Verdict CONDITIONAL GO for Merchant 01 — no exposure found. All four rows dispositioned by founder ruling the same day: D168 defer (do NOT restore the merchants grant), D169 fix before the next production migration, D170 staff acceptance required before scaled seat issuance, D171 defer. Cross-tenant, money-path, identity and admin boundaries were each adversarially tested and held. Prior: 2026-08-23 full-role E2E run — D164, D165, D166, D167 opened; D162 opened; D163 closed; D160 + D161 closed; D159 parked; D158 closed and applied, ledger 100/100) **Same day, governance reconciliation:** **D172** opened — the golden-path browser E2E has never executed (167/167 workflow runs skipped, `E2E_BASE_URL` never provisioned). It was **originally recorded in Notion as D168** and is renumbered here after colliding with the security D168 above; the repo is canonical for drift IDs and the security D168 is unchanged in meaning, disposition and evidence. The E2E claim was independently re-verified against `main` and **narrowed**: it is the golden-path money loop rather than a "dashboard" suite, the RPC-level invariants are covered in CI, and the gate is a documented unprovisioned opt-in with two anti-false-green guards — not a broken workflow.) **Ruled the same day: D172 is DEFERRED — it does not block Merchant 01, and becomes a hard gate before MAANTA moves from controlled Node 0 validation into routine or scaled releases.**

- second founder-requested polish pass, using `kylezantos/design-motion-principles` and the `webapp-testing` skill from `ComposioHQ/awesome-claude-skills`: **D176**, **D177**, **D178** opened and closed the same day - the declared house easing curve unused in 42 of 44 transitions, the Toggle knob animating `left`, and five error boundaries pinned to the top of a full-height shell. The third was found by rendering the running app in Chromium rather than by reading source. Write-up: `docs/ops/motion-and-app-polish-2026-08-24.md`. Earlier the same day: frontend polish pass using the third-party `leonxlnx/taste-skill`: **D173**, **D174**, **D175** opened and closed the same day — an amber focus ring on nine form controls at 1.66:1 under a guard that only read the stylesheet, eight fields whose only name was a placeholder, and a duplicated reduced-motion rule. All three are accessibility defects the skill's mandatory contrast and label checks surfaced and that both MAANTA's frozen UI rules and the skill agree on; each closes with a ratchet verified to fail on a reintroduced violation. The audit, including the places the skill and MAANTA's founder-approved choices genuinely conflict and were deliberately left alone, is `docs/ops/taste-skill-frontend-audit-2026-08-24.md`. Prior: pre-field adversarial security audit: D168, D169, D170, D171 opened. Verdict CONDITIONAL GO for Merchant 01 — no exposure found. All four rows dispositioned by founder ruling the same day: D168 defer (do NOT restore the merchants grant), D169 fix before the next production migration, D170 staff acceptance required before scaled seat issuance, D171 defer. Cross-tenant, money-path, identity and admin boundaries were each adversarially tested and held. Prior: 2026-08-23 full-role E2E run — D164, D165, D166, D167 opened; D162 opened; D163 closed; D160 + D161 closed; D159 parked; D158 closed and applied, ledger 100/100) **Same day, governance reconciliation:** **D172** opened — the golden-path browser E2E has never executed (167/167 workflow runs skipped, `E2E_BASE_URL` never provisioned). It was **originally recorded in Notion as D168** and is renumbered here after colliding with the security D168 above; the repo is canonical for drift IDs and the security D168 is unchanged in meaning, disposition and evidence. The E2E claim was independently re-verified against `main` and **narrowed**: it is the golden-path money loop rather than a "dashboard" suite, the RPC-level invariants are covered in CI, and the gate is a documented unprovisioned opt-in with two anti-false-green guards — not a broken workflow.) **Ruled the same day: D172 is DEFERRED — it does not block Merchant 01, and becomes a hard gate before MAANTA moves from controlled Node 0 validation into routine or scaled releases.**

Prior to the rebase, from canonical `main`:

Last updated: 2026-08-26 (pre-field adversarial security audit: D168, D169, D170, D171 opened. Verdict CONDITIONAL GO for Merchant 01 — no exposure found. All four rows dispositioned by founder ruling the same day: D168 defer (do NOT restore the merchants grant), D169 fix before the next production migration, D170 staff acceptance required before scaled seat issuance, D171 defer. Cross-tenant, money-path, identity and admin boundaries were each adversarially tested and held. Prior: 2026-08-23 full-role E2E run — D164, D165, D166, D167 opened; D162 opened; D163 closed; D160 + D161 closed; D159 parked; D158 closed and applied, ledger 100/100) **Same day, governance reconciliation:** **D172** opened — the golden-path browser E2E has never executed (167/167 workflow runs skipped, `E2E_BASE_URL` never provisioned). It was **originally recorded in Notion as D168** and is renumbered here after colliding with the security D168 above; the repo is canonical for drift IDs and the security D168 is unchanged in meaning, disposition and evidence. The E2E claim was independently re-verified against `main` and **narrowed**: it is the golden-path money loop rather than a "dashboard" suite, the RPC-level invariants are covered in CI, and the gate is a documented unprovisioned opt-in with two anti-false-green guards — not a broken workflow.) **Ruled the same day: D172 is DEFERRED — it does not block Merchant 01, and becomes a hard gate before MAANTA moves from controlled Node 0 validation into routine or scaled releases.** **Same day, founder ruling on merchant self-onboarding location:** **D162** moves to `pending-deploy` — browser geolocation replaces mandatory what3words, the confirmed coordinates are the canonical store location, and what3words becomes optional enrichment. **D173** opened and closed: `convertTo3Words` ran unbounded despite the module documenting the opposite, found by putting it on the onboarding path. **Later the same day: D162 is DEPLOYED and is no longer a Merchant 01 blocker** — PR #268 merged (`14052ad`) and migration `20260824120000` applied under founder authorization; ledger **101/101** after repairing a ninth MCP-minted version. D162 stays `open` because it closes on live proof — one genuine self-serve onboarding at BBS Mall — not on deployment. **D174** opened and closed the same day: production holds one non-demo `success` from an internal E2E while the field docs read 0, and the founder's ruling splits them into two counters — internal evidence 1, external field validation 0 — so an internal row can never increment the Merchant 01 ladder. **2026-08-25:** **D184** opened and closed — D174's evidence split covered redemptions but not merchant records, so both non-demo merchant rows (`bf66a041` SKANDI SKAN, a founder registration exercise with a family member; `67fe233d` E2E Full Sweep Shop) are now classified as internal in CLAUDE.md's Node 0 evidence block, with a guard that keeps them named. External field validation stays at **0 genuine merchants**; Merchant 01 will be the third non-demo merchant row, not the first. Same day, **D164 landed on this branch**: the claims-tracking migration was renumbered `20260823140000` -> `20260824130000` after production applied `20260824120000`, and `/admin`'s read-failure guard was corrected to exclude its own `app_config` read structurally. **Later 2026-08-25: D164 CLOSED — applied and merged.** PR #272 merged as `061c92c` on a full green board; migration `20260824130000` applied to production under founder authorization before the merge. Ledger **102/102** by version and name after repairing a **tenth** MCP-minted version (`20260825083646`). 401 redemptions, 0 non-NULL `claimed_at` — no history fabricated. Demo mode is OFF: 0 live deals, 2 live merchants, both internal (D184). **Same day, all-screens audit (87 screens, 40 API routes, all six roles):** **D185** and **D186** opened and closed — `/admin/reports` carried D164's defect on the money figure and had never been covered by either ruling; Sentry wizard scaffolding (a public unauthenticated 500-generator) was shipping to production and is removed. **D187** opened: nothing decides which screens owe a read-failure state, deferred past Merchant 01 by recommendation because the fix is a sweep. The audit found **zero** non-existent columns across 190 query sites, **zero** RPC contract mismatches across 21 call sites, **zero** overloads, and **zero** broken internal links across 155 targets. **2026-08-26:** **D188** opened and **D189** closed after re-reading production post-merge. `claim_deal` never sets `redemptions.is_demo`, so every claim through the product is tagged non-demo — the D174 counters cannot be derived from that flag, and the long-cited "5 real redemptions" were 1 internal success plus 4 claims against demo merchants. A sixth arrived 2026-08-25 16:17 against demo merchant "Pepper Pot" (D189), classified internal, row untouched. **Demo mode is ON again by founder ruling 2026-08-26** (prospect demos) — see the decisions log. External field validation remains **0**; the D164 migration is confirmed correct in the wild at 402 redemptions / 401 NULL / 1 stamped.

The **canonical** durable record of **known gaps between what MAANTA claims and
what is true**, with open/closed state per row. Created 2026-07-30 to close FU-1
of `docs/skills/truth-audit-2026-07-30.md`.

A **dated, ranked view of the rows waiting on a ruling** — with each question,
its evidence and its options — is `docs/maanta-decision-queue-2026-08-19.md`.
That file carries no state and closes nothing: it is re-derived, never edited,
and every closure still happens here by ID with a guard named.

"Canonical", not "only": `docs/skills/parity-drift-register-2026-07-30.md` also
exists and is a dated narrative companion, not a state tracker. That split is
itself tracked as **D27** and is not the intended end state — items there should
land here as rows and close by D-number. This paragraph exists because the header
previously said "single durable record" while D27 recorded a second register four
screens below it, which is precisely the claim-vs-reality gap this file is for.

## Why this file exists

Before it, drift was recorded in dated audit documents (`docs/skills/*-audit-*.md`).
Those are good narratives and bad trackers: nothing carries state forward, so a
finding resolved in one audit can be re-raised or forgotten in the next. That is
not hypothetical — it is what happened:

- The Elite-trial "14 vs 30 days" conflict was resolved on 2026-07-29, and a code
  comment still called it open on 2026-07-30 (**D5**).
- The launch offer was frozen in Notion and had never reached the repo mirror at
  all, despite being referenced in passing by another decisions-log entry (**D6**).
- Two invariants this repo described as enforced were not (**D3**, **D9**).

So the register's job is not to be tidy. It is to make *"we already fixed that"*
a checkable claim.

It started earning that before it was even merged. The first draft carried a row
saying `MAANTA_DEMO_MODE` was unverified; while the register sat on a branch, that
verification landed on `main` and turned up three further gaps in the process
(**D17**–**D20**). Rebasing forced the row to be re-checked against reality rather
than shipped as written — which is the entire mechanism, applied to itself.

## The rules

1. **Rows are append-only.** Never delete or renumber a row — close it. The
   history of what was once wrong is the point.
2. **A row may only be `closed` when its evidence names a guard** — a test, a
   migration, or a decisions-log entry: something that would fail or be
   contradicted if the drift came back. `drift-register.test.ts` fails if the
   cited path does not exist, and fails if the only citation is a document the
   row merely relates to.

   Some drift cannot be guarded — a misleading comment, or behaviour that needs
   test infrastructure this repo lacks. Those may still close, but the evidence
   must say **`no guard: <reason>`** so it is a visible choice. The first draft of
   this register did not have that rule, and an open row flipped to `closed`
   passed the suite because it happened to cite a background doc. That hole was
   found by trying it.
3. **`pending-deploy` is not `closed`.** A fix merged to `main` but not yet live
   (an unapplied migration, an unset env var) stays open in substance. This
   distinction exists because "merged" is the exact point at which people stop
   tracking things.
4. **`deferred` needs an owner and a next step**, not just a shrug.
5. Every audit that finds drift adds rows here **before** writing its narrative,
   and closes prior rows by ID rather than re-describing them.

### Status values

| Status | Meaning |
|---|---|
| `open` | The gap exists right now. |
| `pending-deploy` | Fix is merged but not yet live where it counts. Still a gap. |
| `closed` | Fixed, live, and evidenced. |
| `deferred` | Accepted knowingly, with an owner and a trigger for revisiting. |

### Category values

`code-outlier` · `mirror-stale` · `doc-stale` · `db-metadata` · `product-decision` · `prototype-only` · `process`

---

## Register

| ID | Status | Category | Opened | Domain | Claim vs reality | Evidence / next step | Owner |
|---|---|---|---|---|---|---|---|
| D1 | closed | product-decision | 2026-07-30 | Feed ordering | Frozen feed rule locks three per-rail sorts; `/feed` defaulted to distance and re-sorted all three, discarding the boosted order Elite merchants pay KES 500/24h for | `maanta-app/src/lib/deal-list-controls.ts`, `maanta-app/src/lib/__tests__/locked-feed-order.test.ts`, `docs/maanta-decisions-log.md` | founder |
| D2 | closed | product-decision | 2026-07-30 | Trials / money | Frozen launch offer is capped at the first 100 BBS Mall merchants; nothing enforced the cap or the node scope | Enforced by `maanta-app/supabase/migrations/20260730130000_enforce_elite_trial_first_100_cap.sql` + `maanta-app/supabase/tests/elite_trial_cap_test.sql`. **Live on production 2026-07-30**: `elite_trial_cap_status()` returns cap 100, granted 0, remaining 100 — the full offer is unspent, so no backfill surprise | founder |
| D3 | closed | doc-stale | 2026-07-30 | Feed ordering | Notion "Frozen Scope and Rules" claimed the feed structure was "enforced in code", citing a test file that never covered it | Notion bullet corrected with a dated sub-note; now cites `maanta-app/src/lib/__tests__/locked-feed-order.test.ts` | founder |
| D4 | closed | code-outlier | 2026-07-30 | Pricing copy | `/pricing` promised "first month of Elite free" — no cap, no node scope, no fee caveat — against a frozen offer that has all three | `maanta-app/src/app/(marketing)/pricing/page.tsx`, guarded by `maanta-app/src/lib/__tests__/pricing-copy.test.ts` | founder |
| D5 | closed | mirror-stale | 2026-07-30 | Trials | A code comment called the 14-vs-30-day trial conflict "an open spec/DB conflict" after the 2026-07-29 audit had already resolved it to 30 days. First recorded re-discovery | Corrected in `maanta-app/src/app/api/admin/merchants/[id]/approve/route.ts`. no guard: a test cannot assert that prose is not misleading; the 30-day value itself is covered by `maanta-app/supabase/tests/elite_trial_cap_test.sql` | eng |
| D6 | closed | mirror-stale | 2026-07-30 | Trials | The launch offer was a frozen rule in Notion and appeared nowhere in the repo mirror, though another decisions-log entry referenced it in passing | `docs/maanta-decisions-log.md` | eng |
| D7 | closed | code-outlier | 2026-07-30 | Pricing copy | "Free" rendered as Standard's headline price, while a Standard merchant pays the success fee on every verified redemption | Copy fixed on both public pages and guarded by the bare-"Free" check in `maanta-app/src/lib/__tests__/pricing-copy.test.ts` | founder |
| D8 | closed | code-outlier | 2026-07-30 | Pricing | The frozen success fee existed as four independent `30` literals across public pages, page metadata and a fallback | Single-sourced as `SUCCESS_FEE_KES` in `maanta-app/src/lib/pricing.ts`; uniqueness asserted by `maanta-app/src/lib/__tests__/pricing-copy.test.ts` | eng |
| D9 | closed | db-metadata | 2026-07-30 | Pricing | Live `app_config.success_fee_kes` notes gave the Elite price review as "Oct 2026" (superseded by the 2026-07-20 ruling) and cited two files that do not exist in this repo | `maanta-app/supabase/migrations/20260730160000_correct_success_fee_config_notes.sql` (renamed from `20260730120000` on 2026-08-05 to match the ledger — D24) + `maanta-app/supabase/tests/frozen_commercial_config_test.sql`. **Live on production 2026-07-30**, read back from `app_config`: notes now cite `CLAUDE.md` and `docs/maanta-decisions-log.md` and give the Feb 2027 review. Was applied under a different version number than the file carried; filename and ledger agree since 2026-08-05 (D24) | eng |
| D10 | closed | process | 2026-07-30 | Ops | Two merged migrations were not applied to production, so D2 and D9 were merged-but-not-true | Pushed by a human operator per `docs/ops/supabase-migrations.md` and verified read-only on 2026-07-30: cap live at `cap 100, granted 0, remaining 100` (recorded against D2), corrected notes live in `app_config` (D9). no guard: whether a migration has been applied is a property of the live database, not of this repo — the check is the read-back, which is why the numbers are recorded here rather than asserted in a test | founder |
| D11 | closed | code-outlier | 2026-07-30 | Feed ordering | An unrecognised `?sort=` reached the distance branch, undoing the D1 locked order from a URL; an unrecognised `?filter=` emptied every rail and claimed the mall had no deals | `parseDealListSort` / `parseDealListFilter` in `maanta-app/src/lib/deal-list-controls.ts`, covered by `maanta-app/src/lib/__tests__/locked-feed-order.test.ts` | eng |
| D12 | closed | code-outlier | 2026-07-30 | Trials / money | The D2 cap was gated on UPDATE only, so an INSERT carrying a trial bypassed it unstamped — which `maanta-app/supabase/seed/node0_rehearsal_seed.sql` actually does — making the slot count under-report permanently | Trigger widened to `BEFORE INSERT OR UPDATE`; Scenario H in `maanta-app/supabase/tests/elite_trial_cap_test.sql` | eng |
| D13 | closed | code-outlier | 2026-07-30 | Audit trail | The approve route logged the trial *request* as the outcome, so a refused or unverifiable grant was recorded as granted | Three explicit outcomes in `maanta-app/src/app/api/admin/merchants/[id]/approve/route.ts`. no guard: asserting the logged outcome needs a route-level integration test against a live Supabase, which this repo does not have yet — same blocker as the browser golden-path item in the decisions log | eng |
| D14 | open | process | 2026-07-30 | Demo mode | `app_config.demo_mode_enabled` is `true` on production. Correct for rehearsal, and its own notes say it must be `false` at launch — so today the live product shows synthetic shops and deals to anyone who visits | Flip to `false` as part of the launch checklist and close this row with the timestamp. Runbook: `docs/ops/demo-mode-runbook.md`. → **T4 sweep 2026-08-19:** **still accurate, re-read live.** `app_config.demo_mode_enabled` is `'true'` on `axrrslqssmbngbataejg`, and its `notes` still say it must be false at launch | founder |
| D17 | closed | process | 2026-07-30 | Demo mode | The `MAANTA_DEMO_MODE` Vercel var could not be read from a Claude session, so it was assumed rather than known — and the property schema was not evidence, since the only prior server events predated the tagging entirely | Proved from the event stream on 2026-07-30 01:19Z, recorded in `docs/ops/optruth-demo-release-2026-07-29.md`. no guard: a deployed env var cannot be asserted from the repo; the proof is an observed event, so re-verify after any change to it | founder |
| D18 | open | process | 2026-07-30 | Demo mode | The two demo switches can be flipped independently, and `make demo-off` touches only `app_config`. Turning demo mode off while `MAANTA_DEMO_MODE` stays `true` tags **real** events as demo — the precise inversion of what the tagging exists to do | Flip both together at launch, or make the ops target set both. Trap documented in `docs/ops/optruth-demo-release-2026-07-29.md`. → **T4 sweep 2026-08-19:** **not re-measurable from here.** Both switches are live configuration; `app_config.demo_mode_enabled` was re-read as `'true'` (see **D14**), but `MAANTA_DEMO_MODE` is a Vercel environment variable and this session's tooling cannot read Vercel env vars (the same limit **D98** records). Unchanged in substance. → **2026-08-19: the launch check this row needs is now written down** — `docs/ops/demo-mode-runbook.md` carries the four-step flip-pairing verification (DB flag false AND Vercel var false → deploy → a fresh real event carries `is_demo = false` → an anonymous shopper cannot retrieve a demo deal), adopted from the external review of the same date. Same-day verification: demo exclusion is enforced at the SQL layer itself — `deals_public_browse` read back from production carries `(NOT d.is_demo OR is_demo_mode())` on both the deal and the merchant — so the flip closes discovery at the database, and this row's remaining risk is exactly the pairing, not the isolation. Classification: gates PUBLIC LAUNCH; the controlled pilot runs with demo mode on, as every rehearsal has | founder |
| D19 | open | process | 2026-07-30 | Demo mode | `MAANTA_DEMO_MODE` is unset on the Preview environment, so synthetic preview traffic reaches PostHog indistinguishable from production activity. It already cost one untagged event during the D17 verification | Harmless while previews are one-off checks. Set it on Preview before previews are ever used to demo to anyone. See `docs/ops/optruth-demo-release-2026-07-29.md`. → **T4 sweep 2026-08-19:** **not re-measurable from here**, for the same reason as **D18**: it is a Preview-scope environment variable, not a repo fact | founder |
| D20 | closed | code-outlier | 2026-07-30 | Analytics | Every server-side capture — `deal_viewed`, `deal_claimed`, `guardian_outcome`, both top-up events — dropped an unknown share of events: the in-flight fetch died when the Vercel instance froze after the response. Found while verifying D17; predates demo mode | Fixed with `waitUntil` in `captureServerEvent` — `maanta-app/src/lib/analytics.ts`, covered by `maanta-app/src/lib/__tests__/analytics.test.ts`. **Data caveat stays true forever**: any server-side funnel computed over events from before that deploy is a floor, not a measurement | eng |
| D21 | closed | code-outlier | 2026-07-30 | Analytics | `captureDealViewed` sent `clerkUserId ?? "anonymous"`, and browsing needs no account — so most of the top of the funnel collapsed onto one PostHog person. `uniq(person_id)` on `deal_viewed` returned 1 for all signed-out traffic however many people it was, and a view→claim funnel could not join for signed-out users. Stayed hidden because D20 meant the events were not arriving at all | Fixed by reusing the browser's own id — the `analytics-identity.ts` module, covered at the time by its own test. **Data caveat stays true forever**, like D20: signed-out unique-viewer counts and view→claim funnels over events from before this deploy are not measurements. → **Superseded 2026-08-18 by D88**: anonymous analytics went cookieless/in-memory (`persistence: "memory"`, founder ruling), so the cookie-read this row added became dead and `analytics-identity.ts` was deleted. Signed-out server-side `deal_viewed` is now honestly unattributed (source `none`); the attribution logic is pinned by `maanta-app/src/lib/__tests__/analytics.test.ts` | eng |
| D22 | closed | code-outlier | 2026-07-30 | Analytics | The D21 fix reads `distinct_id` from the posthog-js cookie, which is correct only for the current config: default persistence, no `persistence_name` override, and `defaults` below the 2026-05-30 cutoff where `split_storage` moves the id out of that cookie. If any of those changes the read silently returns nothing and the server falls back — **indistinguishable from a genuine first-ever view, so no runtime warning is possible** | Re-verify signed-out attribution after any posthog-js upgrade or config change; `distinct_id_source` (`clerk` / `posthog_cookie` / `none`) is the property to watch — a sudden shift toward `none` is the symptom. no guard: the failure is silent by construction, so a test in this repo cannot detect it; it needs an occasional look at the live property distribution. → **Closed 2026-08-18 by D88's retire.** The config-dependent cookie-read this row warned about no longer exists — anonymous analytics is cookieless/in-memory, so there is no cookie to silently stop reading, and `distinct_id_source` is now just `clerk` / `none`. What D22 said no test could catch was true of the fragile read; that read is gone, and `maanta-app/src/lib/__tests__/analytics-cookieless-posture.test.ts` now pins `persistence: "memory"` so the config and the analytics posture cannot drift apart silently again | eng |
| D15 | closed | doc-stale | 2026-07-30 | Pricing | `/pricing` hardcodes KES 3,500 with no `app_config` key behind it, unlike the success fee and boost fee, so the UI is the only place a price change lands. *(Original next step: add `elite_subscription_kes` when subscription billing is wired to a processor, and revisit at the Feb 2027 Elite price review. T4 sweep 2026-08-19 re-read it live and confirmed it still accurate.)* | **Closed 2026-08-24 as moot, superseded by the founder ruling recorded in D179.** The row asked where the Elite price should be stored; the ruling is that MAANTA publishes no Elite price during Node 0, so there is no value to source, no `app_config` key to add and no Feb 2027 review to hold. KES 3,500 has been removed from all nine rendered surfaces and from `FACTS`. Should Elite ever be priced, this row's original point stands and should be re-read: the price belongs in `app_config` behind `getSuccessFee()`-style access, not typed into a component. Guard: `maanta-app/src/lib/__tests__/pricing-copy.test.ts` now fails if any rendered surface publishes an Elite monthly price at all | founder |
| D16 | closed | process | 2026-07-30 | Process | Drift was tracked only in dated audit narratives, so resolved findings were re-discoverable and "already fixed" was not a checkable claim — the root cause behind D3, D5, D6 and D9 | This file, enforced by `maanta-app/src/lib/__tests__/drift-register.test.ts` | eng |
| D23 | closed | mirror-stale | 2026-07-30 | Trials / money | The D2 cap migration told the reader "The admin UI reads elite_trial_cap_status() to know this will happen before ticking the box" — written as fact when no UI read the RPC at all, so an admin could exhaust the offer with no warning. Fifth recorded case in this audit of a comment outrunning the code it describes | Made true on `main` by #144: three call sites now read the RPC (`maanta-app/src/app/admin/billing/page.tsx`, `maanta-app/src/app/admin/merchants/[id]/page.tsx`, `maanta-app/src/lib/elite-trial.ts`), written up in `docs/skills/e2e-readiness-2026-07-30.md`. no guard: the drift was a false sentence in a migration comment, and no test can assert that prose matches behaviour — the callers are the reality, and they are now what the comment claims | eng |
| D24 | closed | process | 2026-07-30 | Ops | Production's migration ledger and this repo disagree about which migration each version number *is*. Prod records `20260730120000` as `node_scoped_opening_credit_cap` — a migration with **no file anywhere in this repo** — while the repo's `20260730120000` is the success-fee notes fix, which prod recorded at `20260730160000` instead. This contradicts CLAUDE.md's claim that the migration history is "the authoritative record of DB behavior" | **Closed 2026-08-05 — the ledger is fully reconciled.** Read-back diff: production's `supabase_migrations.schema_migrations` and `maanta-app/supabase/migrations/` agree on all **85** version/name pairs, in order, with zero rows on either side unmatched. Three moves, one per mismatch class: (1) prod's uncommitted `node_scoped_opening_credit_cap` **exported verbatim from the ledger's stored statements** into `maanta-app/supabase/migrations/20260730120000_node_scoped_opening_credit_cap.sql` — export only, its change is not in effect, see **D73**; (2) the success-fee notes migration **renamed** to `maanta-app/supabase/migrations/20260730160000_correct_success_fee_config_notes.sql`, completing the renumbering its applied copy had already declared at apply time on 07-30 (executable SQL verified byte-identical to prod's stored statements before the rename); (3) the MCP-minted pause-gate versions (`20260804152939`/`20260804152951`) and the cofounder version **repaired in the ledger** to the repo filenames `20260730180000`/`20260730190000`/`20260804010000` — the same operation `supabase migration repair` performs, done via the founder-authorized MCP channel. no guard: whether the live ledger matches the repo is a property of the live database, not of this repo (same reasoning as D10) — the check is `supabase db push --dry-run` reporting "Remote database is up to date", re-run after any apply. The repo-side half is guarded implicitly: CI `db-tests` applies the full 85-file chain, so a file that breaks the chain fails CI | founder |
| D25 | closed | code-outlier | 2026-07-30 | Core loop | **Implemented in repo, not fully live in production.** Production `claim_deal` still lacks `deal_paused` (confirmed 2026-07-30), so a paused deal can accept new claims despite merchant “No new claims while paused” copy. App rails (#146) and SQL browse filter (#150 / D32) hide paused deals from discovery, but RPC is the enforcement point — deep link / cached client / direct API still get a claim until deploy | **Closed 2026-08-04 by a founder-authorized apply through the Supabase MCP** — an explicit, recorded exception to the Claude-does-not-apply-migrations rule, chosen by the founder over the hand-to-human path when told the gate was open in production. Both repo migrations applied in order from `maanta-app/supabase/migrations/20260730180000_restore_claim_deal_pause_gate.sql` and `maanta-app/supabase/migrations/20260730190000_paused_deals_discovery_filter.sql`, after a pre-flight confirmed `you_pay_kes`, `is_demo_mode()`, the `is_demo`/`is_paused` columns and the existing view all present. **Read-back, not assumption**: `pg_get_functiondef(claim_deal)` now contains `deal_paused`; `pg_get_viewdef(deals_public_browse)` filters `is_paused`; `verify_redemption` still does **not** mention `is_paused`, which is the required half of the pause semantics — claimed tickets stay redeemable. Guard: the migrations and `maanta-app/supabase/tests/claim_deal_pause_gate_test.sql`, run by CI's db-tests on every push. **Caveat feeding D24**: the MCP recorded the two ledger rows as versions `20260804152939` and `20260804152951`, not the repo filenames' `20260730180000`/`20260730190000` — the behavior gap is closed but the ledger/repo version mapping diverges by two more rows, so a future `supabase db push` will see the repo files as unapplied and re-run them; both are idempotent (CREATE OR REPLACE / DROP+CREATE), so that re-run is safe, but D24's reconciliation now covers four rows, not two. **2026-08-05 note:** the MCP-minted ledger versions this row records were repaired to the repo filenames `20260730180000`/`20260730190000` when D24 closed — the content live on production is unchanged | eng |
| D26 | open | process | 2026-07-30 | Design truth | The audit brief asked for a guardrail asserting that design-ahead frames reference open drift rows. It was deliberately skipped, and both `docs/skills/truth-audit-2026-07-30.md` §0 and the docblock in `maanta-app/src/lib/__tests__/drift-register.test.ts` justified the omission by saying this repo has no frames artifact. #146 added `maanta-app/design/current-reality/frames.json`, so that justification is now void — the stated reason for not building it no longer holds | Decide whether every `design-ahead` surface must cite an open drift row, then build the guardrail. It would fail today: the file has exactly one design-ahead surface, `/contact`, and the whole file contains no drift-row reference at all (0 occurrences of "drift"). The stale claims in the two docs are corrected in this change; the missing guardrail is what keeps this row open. → **T4 sweep 2026-08-19:** **still accurate in its conclusion, stale in its evidence.** The row says `frames.json` "contains no drift-row reference at all (0 occurrences of 'drift')"; it now contains **seven** — D103, D104, D116, D79, and the D92/D93/D95 references added to the two new PWA entries today. What has not changed is the thing the guardrail would assert: the file's one `design-ahead` surface, `/contact`, still cites **no** drift row, so the check would still fail today exactly as the row predicts. Its `frontend` path was also pointing at the retired `(public)/` route group and is corrected in this change (**D140**), which matters here because a guardrail about design-ahead frames is easier to write once every citation resolves. **Proposal, for the founder to rule on rather than a session to adopt (T6, 2026-08-19):** the guardrail would assert that every surface with `status: design-ahead` names at least one drift row in its `notes`, in the `D<number>` form the file already uses elsewhere, and that each row named is `open` in `docs/maanta-drift-register.md` — the second half is what stops a design-ahead frame citing a row that closed years ago. It belongs beside the existing checks in `maanta-app/src/lib/__tests__/frames-inventory.test.ts`, which already parses the file and cross-references drift rows for the two PWA entries. **It would fail today, on the single surface it applies to:** `/contact` is the file's only `design-ahead` entry and its `notes` read "Fake success — no API. Do not treat as shipped support channel." — accurate, and citing nothing. So adopting the guardrail means either opening a row for `/contact`'s missing API or deciding that a self-explanatory note is enough, and that is the ruling this row has been waiting for since #146 voided its original excuse. The narrower question is whether the rule should bind `design-ahead` only, or also `gated` and `blocked`, which are likewise states where a design reader needs to know what is holding the surface | eng |
| D27 | closed | process | 2026-07-30 | Process | A second drift register now exists — `docs/skills/parity-drift-register-2026-07-30.md`, added by #146 — recreating the exact pattern D16 closed: a dated narrative that carries no forward state and is enforced by nothing. `docs/README.md` and `CLAUDE.md` both present `docs/maanta-drift-register.md` as the durable record, which is no longer the whole picture. It is already drifting: its M1 entry previously cited the pause gate as `20260730160000`, the number D25 renumbered away from (now `20260730180000` on the consolidation map) | Fold the parity items into this register as rows, or make the parity doc an explicitly narrative companion that closes items by D-number here. Until one or the other, "we already tracked that" is again unanswerable — which is what D16 existed to prevent. → **T4 sweep 2026-08-19:** **still accurate.** `docs/skills/parity-drift-register-2026-07-30.md` still exists alongside this file, and neither has folded into the other → **Closed 2026-08-30:** the parity document now identifies itself at the top as a historical narrative companion, points every state decision to this canonical register, and forbids new findings there. no guard: prose cannot prevent a future author from creating another tracker; the canonical-register rule and this closure make the contradiction reviewable. | eng |
| D28 | closed | code-outlier | 2026-07-30 | Support | `/contact` renders a success state without sending anything: `onSubmit` calls `setSent(true)` and there is no fetch, no action and no API route behind it. A shopper or merchant who uses it believes they have contacted support, and nobody receives the message. `frames.json` classifies the surface `design-ahead` and its own note says "Fake success — no API" | **Closed by wiring the form to a real destination:** `maanta-app/src/app/api/contact/route.ts` delivers the enquiry to `admin@maanta.app` via Resend with `reply_to` set to the sender, and autoresponds to the sender when they gave an email address. The page (`maanta-app/src/app/(marketing)/contact/page.tsx`) now shows its confirmation only after that request succeeds; a failed send surfaces the error and offers WhatsApp. Guarded by `maanta-app/src/app/api/contact/__tests__/route.test.ts`, which asserts a failed delivery returns 502 rather than a success shape, and that the page cannot reach `setSent(true)` straight from the submit handler. The removed "within 24 hours" promise is also guarded — no response time is published until one is committed to (`website-handoff.md` §9) | founder |
| D29 | closed | code-outlier | 2026-07-30 | Core loop | `public.deals.expires_at` is nullable, but `public.redemptions.expires_at` is `NOT NULL`, and `claim_deal` explicitly tolerates a NULL deal expiry (`IF v_deal.expires_at IS NOT NULL AND ... <= NOW()`) before inserting `v_deal.expires_at + INTERVAL '15 minutes'`. A claim on a no-expiry deal therefore dies on a raw NOT NULL constraint violation instead of a clean domain error, and the returned redemption window would be NULL. Latent, not live: 0 of 409 production deals have a NULL expiry, verified read-only 2026-07-30 | Pre-existing — the pause-gate migration is a surgical restore that kept this body, so it did not introduce it. **Deliberately not fixed here**: rejecting or defaulting a no-expiry claim changes claim behaviour on the money path, which is a product decision, not a review fix. Decide the intended semantics (reject with `deal_expired`, require an expiry at write time via a NOT NULL constraint, or define a no-expiry branch), then guard it in `maanta-app/supabase/tests/claim_deal_pause_gate_test.sql` or a sibling suite. Found by CodeRabbit on #137. → **Closed 2026-08-18 (founder-authorized MCP apply).** Resolved with the sanctioned "require an expiry at write time" option: `maanta-app/supabase/migrations/20260818130000_deals_expires_at_not_null.sql` sets `deals.expires_at NOT NULL`. Not a product change — every deal already gets its expiry from the `set_deal_expiry` BEFORE INSERT trigger, guaranteed non-null because `starts_at` is `NOT NULL DEFAULT now()` and `deal_type` is CHECK-constrained to standard/flash (all read back on production; 0 of 1778 deals null, so no backfill). The claim_deal 500 is now unreachable — a null-expiry deal cannot exist. Verified as a rolled-back rehearsal (ALTER + insert/claim/constraint-bite assertions), then applied and read back (`attnotnull = true`, 1778 rows intact, ledger 94/94). Guard: `maanta-app/supabase/tests/deals_expires_at_not_null_test.sql` (schema posture · trigger fills expiry · claim window = expiry+15m · UPDATE→NULL refused) | founder |
| D30 | closed | doc-stale | 2026-07-30 | Ops | `docs/skills/repo-branch-audit-2026-07-30.md` told operators to replace this branch's `170000` pause-gate file with `180000`, and to rebase shared branches while a later step forbade force-pushing rebased history. Both were unfollowable: the `170000` file no longer exists, and rebasing a shared branch can only be pushed by the force-push the same checklist bans | Both corrected in place with the invariant that matters — exactly one `*_restore_claim_deal_pause_gate.sql`, versioned above production's ledger max — and the sync step now says merge, matching the convention the same document states. Recorded per rule 3 (open-and-close in one change still gets a row); the fix would otherwise have left no trace that the instructions were ever wrong. no guard: a test cannot assert that operator prose is followable — the guard for the underlying migration state is `maanta-app/supabase/tests/claim_deal_pause_gate_test.sql` | eng |
| D31 | closed | doc-stale | 2026-07-30 | Pricing copy | `docs/skills/repo-branch-audit-2026-07-30.md` says documents still claiming the launch offer was "removed entirely" **should be read as** "ungoverned free-month line removed". Only the ungoverned wording was withdrawn (D4/D7); the governed first-100 trial stands. Instructing readers to reinterpret stale documents rather than correcting them is the exact failure this register exists to end — the contradictory documents stay discoverable, and a reader who misses the reinterpretation can reintroduce the banned copy | Identify the documents that still say the offer was removed and correct them, or name the single authoritative replacement they should defer to. The banned phrasing itself is guarded by the bare-"Free" and launch-offer checks in `maanta-app/src/lib/__tests__/pricing-copy.test.ts`, so the code path is safe; the exposure is operator and marketing copy written from a stale doc. Raised by CodeRabbit on #137. → **T4 sweep 2026-08-19:** still accurate — with one correction to the sweep itself, same day: the `docs/ops/marketing-site-finish-checklist.md` hit was a **false positive** (its "removed entirely" is about a Suspense boundary, not the offer), so the only live carrier was `docs/skills/repo-branch-audit-2026-07-30.md:73`, the reinterpretation instruction this row is about. → **Closed 2026-08-19**, by the first branch of this row's own remedy: the instruction is replaced with the position stated once — only the ungoverned free-month wording was withdrawn (D4/D7); the governed first-100 trial stands — and the single authoritative replacement is named (`maanta-app/src/lib/marketing/facts.ts` `OFFERS.eliteTrial` + the decisions-log launch-offer entry), so a reader corrects stale documents against those rather than reinterpreting them. A sweep found no other live document carrying the claim; the instruction was its last surviving artifact. The banned phrasing itself stays guarded in code by `maanta-app/src/lib/__tests__/pricing-copy.test.ts` (bare-"Free" and launch-offer checks). no guard for the prose half: no test can assert that a narrative document does not instruct reinterpretation — the control is that the authoritative source is now named where the instruction used to be | founder |
| D32 | closed | code-outlier | 2026-07-30 | Core loop | `deals_public_browse` filtered active + unexpired merchants but **not** `is_paused`, so a paused deal could appear via the SQL discovery view even though `getLiveDeals` hid it and `claim_deal` would raise `deal_paused` | **Closed in-repo via PR #150:** `maanta-app/supabase/migrations/20260730190000_paused_deals_discovery_filter.sql` (`is_paused IS NOT TRUE`); scenarios A–C (+ browse Scenario D) in `claim_deal_pause_gate_test.sql` / `browse_views_test.sql`; `docs/skills/paused-deal-semantics.md`. Production receives `190000` on the **same** human `db push` that closes D25 — until then the browse-view fix is not live on prod, but this row is closed against repo evidence. Track live RPC enforcement under D25 | eng |
| D33 | closed | code-outlier | 2026-07-31 | Marketing site | The demo-mode disclosure is mounted in the **public/marketing** shell, not just the app shells. `(public)/layout.tsx` renders `<DemoModeBanner />` above `PublicNav`, so whenever `app_config.demo_mode_enabled` is `true` every marketing page — `/`, `/pricing`, `/for-merchants`, `/merchants`, `/about` — is topped by "Demo mode — sample data for rehearsal. These shops, deals and codes are not real." Those pages sell the product; the banner tells a prospective merchant or mall operator that the thing they are reading about is not real. The component is correct on the two app shells (`(shopper)/layout.tsx`, `merchant/(app)/layout.tsx`) where synthetic rows actually render; the marketing shell shows no deal data at all, so the disclosure has nothing to disclose there | **Closed by the Phase 1 marketing shell:** the banner is gone from the marketing layout (`maanta-app/src/app/(marketing)/layout.tsx`, renamed from `(public)` in the same change) and both app-shell mounts are intact. Guarded by `maanta-app/src/lib/__tests__/marketing-shell.test.ts`, which fails if the banner reappears on a marketing route **and** if either app shell drops it — scoping it must not become deleting it. Note for whoever fixes it: the switch is **not** an env var — `isDemoModeEnabled()` reads `app_config.demo_mode_enabled` via the service client (`maanta-app/src/lib/demo-mode.ts`, migration `20260729140000_demo_mode_tagging.sql`), so this cannot be verified by inspecting `.env`. Evidence: `maanta-app/src/app/(marketing)/layout.tsx` | founder |
| D34 | closed | code-outlier | 2026-07-31 | Pricing copy | `/merchants` advertises "Boost any deal for KES 500 / 24h" to prospective merchants, but boosts are **Elite-only** and have been since 2026-07-15: `purchase_boost` raises `BOOST_ELITE_ONLY` for any non-Elite merchant, and the gate is explicitly not bypassed by admin or service_role. The lead-gen page that merchants see *before* signing up promises a feature that every merchant who lands on Standard — the default plan — cannot use. `/pricing` is correct (boosts listed only under Elite) but states no boost price, so the two public pages disagree on both availability and cost. Separately, the `500` is inlined in the page while the canonical value is `app_config.boost_fee_kes`, the same single-source pattern `SUCCESS_FEE_KES` exists to enforce | Correct the `/merchants` bullet to state the Elite requirement, and surface the price on `/pricing` where the plan context is. `maanta-app/supabase/migrations/20260715194145_boost_elite_only_gate.sql` is the authority on gating; `20260709175532_deal_pause_boosts_staff.sql` seeds `boost_fee_kes = 500`. **Closed 2026-07-31.** `/merchants` now states the Elite qualifier and `/pricing` carries the price in the Elite card; both read `FACTS.boostPer24hKes` from `maanta-app/src/lib/marketing/facts.ts`, so neither page can restate it. Guarded by `maanta-app/src/lib/__tests__/marketing-shell.test.ts` on both halves — it fails on the phrase "Boost any deal" and on any `KES 500`/`KES 3,500` literal inlined into marketing JSX. Row flipped late: the fix shipped on 2026-07-31 but the status was left `open` while the implementation report already described it as closed, which the register caught on a later sweep — the exact discrepancy rule 5 exists to surface | founder |

| D35 | closed | code-outlier | 2026-07-31 | Marketing site | Marketing copy described the in-mall team as a vague plural — "Our team is in the mall", "Our team works the floors unit by unit", "a team that is in the building most days" — with no structure, no roles and no bound. Both copy decks flagged it (`copy/merchants.md` #8, `copy/mall-operators.md` #8) as "partly true — confirm team capacity matches the promise". A first pass on 2026-07-31 read the founder's "solo founder" answer as meaning no team exists and stripped the claims back; **that reading was wrong**. The founder ruled the same day that the site demonstrates MAANTA at post-launch and that the staffing model is defined: **one node manager and up to four agents per node**, agents shopper- and merchant-facing on the floor, node manager coordinating with mall management | **Closed by stating the model instead of a plural.** `NODE_TEAM` in `maanta-app/src/lib/marketing/facts.ts` holds the structure, cap and both role descriptions; `maanta-app/src/app/(marketing)/mall-operators/page.tsx`, `maanta-app/src/app/(marketing)/merchants/page.tsx` and `maanta-app/src/app/(marketing)/about/page.tsx` all render from it, so the three pages cannot describe the model differently. Frozen in `docs/maanta-decisions-log.md` (2026-07-31) and guarded by `maanta-app/src/lib/__tests__/marketing-shell.test.ts`, which fails if any of the three describes the team without reading `NODE_TEAM` or hardcodes the agent cap in prose. This is stronger copy than either previous version — an operator evaluating a pilot learns the structure and who they call, where "our team" told them nothing. Phrased as how a node is staffed, never as a headcount standing in a named mall on a given day: the model is a product description and belongs in production, whereas a present-tense count is a measured figure and would have to render through `ScenarioStat` | founder |

| D36 | closed | code-outlier | 2026-07-31 | Support | Both live support surfaces linked to `https://wa.me/254700000000` — a placeholder number, not the real support line. `maanta-app/src/app/(shopper)/help/page.tsx` and `maanta-app/src/app/merchant/(app)/support/page.tsx` each rendered a "Chat on WhatsApp" button reaching nobody. `website-footer-legal-docs-plan.md` §2 told the implementer to "reuse the link already live on /help", which would have propagated the dead number onto the marketing footer as well. Same family as D28: a support channel that looks like it works and does not | **Closed**: both surfaces now render `ENTITY.whatsappLink` from `maanta-app/src/lib/marketing/demo.ts`, so the number is declared once and the marketing footer, contact page and both app support surfaces cannot disagree. Guarded by `maanta-app/src/lib/__tests__/marketing-shell.test.ts`, which fails on any hardcoded `wa.me/` link outside the constants module. **Correction 2026-08-01:** that guard did not work when this row was closed — see D38, and D43 for the residual gap D38's fix left. It does now, proven by planting the number and watching it fail | eng |

| D43 | closed | process | 2026-08-01 | Guard integrity | **D38's fix left a narrower version of D38's bug in place.** That row consolidated three private comment-strippers into `maanta-app/src/lib/__tests__/helpers/comment-stripping.ts`, whose `lineCommentAt()` skips a `//` **preceded by a colon** — which repairs `https://` and nothing else. Its docblock stated the limit out loud and argued it was safe, on the grounds that "a false *strip* can only ever make a guard miss something, which the mutation tests in each consumer are there to catch". A guard that misses is not a mitigated failure mode — it is D36 exactly: green while the file contains the string it forbids. And the colon rule still truncates any other literal carrying a double slash: a protocol-relative "//cdn.example.com", a doubled path segment, a regex source held in a string. Each silently deletes the rest of the line before the scan reads it, in a helper that four suites now depend on. The mutation tests cited as the backstop test the *consumers* against planted URLs, so they confirm the colon case and cannot see the others. Found in review of PR #153, independently of D38 | **Closed by making the lexer string-literal aware.** `stripCommentLines()` now tracks single-quote, double-quote and backtick literals with escape handling, so a double slash inside any literal survives regardless of what precedes it, and `lineCommentAt()` is gone as subsumed. Export names and the file path are unchanged, so D38's consumers and citations still resolve. Exercised directly by `maanta-app/src/lib/__tests__/comment-stripping-lexer.test.ts` — first assertion is the wa.me line that started this, then a double slash inside all three quote styles, escaped quotes, a template literal spanning lines, and an unterminated apostrophe that must not swallow the file. Both remaining limits are now stated as limits rather than as safety: regex literals are untracked, and template interpolations count as string interior — both erring toward scanning too much. **Demonstrated rather than argued:** a protocol-relative `href="//wa.me/254700000000"` planted under `src/components/marketing/` now turns the D36 guard red with file and line; run through the colon rule the same line strips to `return <a href="` and the guard regex returns false, so it would have passed. Negative control still green: the same URL inside a genuine `//` comment is ignored. Directory walking moved to `maanta-app/src/lib/__tests__/helpers/source-files.ts` so each helper has one job | eng |

| D44 | closed | code-outlier | 2026-08-01 | Marketing site | `ScenarioStat` threw on an unwrapped modelled figure only when `process.env.NODE_ENV === "development"`. A preview deployment is built with `NEXT_PUBLIC_SCENARIO_MODE=true` **and** `NODE_ENV=production` — which is the only configuration in which modelled figures render at all. The guard was therefore disabled in precisely the build it existed to protect: a stat placed on a page that forgot `<ScenarioNotice>` would render `121` with no `Modelled` badge and no "Preview build" band, to the investor or mall operator the preview is for. The component's own docblock asserted the opposite, reasoning that production is safe because the fallback returns first — true, and irrelevant to the preview case it overlooked. Found in review of PR #153 | **Closed** by dropping the `NODE_ENV` condition entirely (`maanta-app/src/components/marketing/ScenarioStat.tsx`): the throw now fires in any build where a modelled figure would render unlabelled. Real production is unaffected — `NEXT_PUBLIC_SCENARIO_MODE` is unset, so the fallback returns on the preceding line and the guard is unreachable, meaning no visitor can be shown a crash in place of honest copy. Guarded by `maanta-app/src/lib/__tests__/scenario-stat.test.ts`, which asserts the render matrix behaviourally and asserts that the guard reads no `NODE_ENV` at all. That second assertion exists because `NODE_ENV` cannot be varied within one vitest process — React binds `react/jsx-dev-runtime` at module load — so the property is checked at its source rather than simulated | eng |

| D45 | closed | process | 2026-08-01 | Guard integrity | `scripts/check-tokens.mjs` was described — in `CLAUDE.md`, in `docs/ops/website-handoff.md` §8 and in the implementation report — as failing the production build if any `{{TOKEN}}` reaches rendered output. It scanned `.next/server/app` for `.html`, `.rsc` and `.body` only. Most routes in this app build as `ƒ` (dynamic) and emit **no HTML at build time**, and a token inside a client component ships in a JS chunk rather than in any HTML at all, so two whole classes of token could reach a visitor while the gate printed "clean". Same family as D38 and D43: a check whose reported success did not mean what its callers believed. Raised in review of PR #153 | **Closed by a second pass over compiled JS** (`.next/server/app`, `.next/server/chunks`, `.next/static/chunks`) with a narrower pattern — `{{SCREAMING_SNAKE}}`, which is MAANTA's token convention — because vendor libraries bundle lowercase `{{url}}`, `{{key}}`, `{{auto}}` and `{{source}}` placeholders of their own and failing on those would make the gate noise. Served documents keep the original any-token pattern. Proven on both missed classes: a token planted in a client component fails on `.next/static/chunks/app/(marketing)/waitlist/page-*.js`, and one planted in a **server** component on the same dynamic route — which reaches no chunk directory and no prerendered HTML — fails on `.next/server/app/(marketing)/waitlist/page.js`. Coverage went from 47 files to 47 rendered files plus 392 compiled chunks. Source maps are excluded: they retain comments, and `facts.ts` discusses token names in prose. The scan scope itself is guarded by `maanta-app/src/lib/__tests__/check-tokens-scope.test.ts` — the script fails CI on any token in scope, but nothing else would notice the scope being narrowed back | eng |

| D46 | closed | code-outlier | 2026-08-01 | Marketing site | Every marketing page footer carries `PrelaunchNotice` — "Pre-launch demonstration. MAANTA is not yet trading." — while two surfaces asserted the opposite: the OG image footer read "Live at BBS Mall, Eastleigh · Nairobi", and the root metadata description read "Now live at BBS Mall, Eastleigh." Both render **before** the page does — an OG image is what a WhatsApp forward shows, and the description is the search-result snippet — so they are precisely the surfaces the footer disclosure cannot reach. In a market where WhatsApp is how these links travel, the contradicting claim reached more people than the page that qualified it. `demo-mode-spec.md` §2a does sanction the "Live at" string, but as the `#hero` status line **on `/mall-operators`**, above that page's footer; reading the sanction as covering any surface is what let it onto these two. Raised independently by two reviewers on PR #153 (CodeRabbit, and the Cursor audit as "'Live at BBS Mall' vs prelaunch footer") and **dismissed once** on the grounds that the string was spec-sanctioned — a check of the string that never checked the surface | **Closed** by deriving both from `DEMO_MODE`: `OG_STATUS_LINE` in `maanta-app/src/lib/marketing/og.tsx` states the location without a trading claim, and the description in `maanta-app/src/app/layout.tsx` says "Launching at". Flipping `DEMO_MODE` to false at launch restores both. Guarded by `maanta-app/src/lib/__tests__/prelaunch-consistency.test.ts`, which asserts the invariant rather than the strings — no surface may assert trading while `DEMO_MODE` holds — and first asserts that `PrelaunchNotice` still says "not yet trading", so the suite cannot pass vacuously if that copy changes. Verified by reinstating the original line and watching it fail | founder |

| D37 | closed | process | 2026-08-01 | Release | `main` and the production deployment had diverged in **both** directions, so neither was a superset of the other. `origin/main` was `314b5ef8`; production served `038e3bc0` from `claude/maanta-marketing-site-y8fesm`. `main` was missing three commits including `038e3bc0`, the fix that closed two vacuously-passing marketing guards — confirmed in the files, not merely inferred from the commit graph: at `origin/main` the comment stripper in `maanta-app/src/lib/__tests__/marketing-shell.test.ts` still treated `://` as a line comment, and `copyText()` in `maanta-app/src/lib/__tests__/pricing-copy.test.ts` performed no comment stripping at all. So `main`'s suite was green for the wrong reason while CLAUDE.md presented those guards as enforced. Production, conversely, was missing `314b5ef8` (#154), which changed only `.gitignore` and a docs file. **Proved by mutation:** planting `https://wa.me/254700000000` into a marketing page failed the merged guard and **passed 7/7 against `origin/main`'s copy**, so `main` would have shipped the exact placeholder number D36 exists to keep out | **Closed 2026-08-01 — both stated conditions verified, not assumed.** The row required `git merge-base --is-ancestor <production-sha> origin/main` to succeed **and** Vercel to report `githubCommitRef: main`. Both now hold. Production is `dpl_4Zq5F3UK4QgpmeThCQzbgLqCrfqz`, `state: READY`, `target: production`, `githubCommitRef: main`, `githubCommitSha: b6f19716`, holding `www.maanta.app` and `maanta.app`; and `git merge-base --is-ancestor b6f19716 origin/main` passes, trivially, because it is `main`'s tip. The three commits this row named as missing are each now ancestors of `main`, checked individually: `480bdf6`, `6a22c0f` and `038e3bc0` all return true. The guard vacuity that made this row urgent was separately fixed and is covered by D38, whose remedy — one shared comment lexer at `maanta-app/src/lib/__tests__/helpers/comment-stripping.ts` imported by every copy guard — is what stops the underlying defect returning. Closing this row does **not** mean branch-served production cannot recur: it did recur the same day by a different route, tracked separately as **D53**. no guard: no test can assert which commit a Vercel production alias serves; that state lives outside the repository, and the honest control is that `main` pushes drive production deploys automatically | founder |

| D38 | closed | code-outlier | 2026-08-01 | Testing | `maanta-app/src/lib/__tests__/held-claims.test.ts` is cited across this repo as the guard that keeps §9 held claims out of shippable content, but its `codeOnly()` helper strips line comments with `.replace(/\/\/.*$/gm, "")` — which truncates every line from the first `//`, including the `//` inside any `https://` URL. Any held claim sharing a line with a URL is therefore invisible to the scanner. This is the identical defect that `038e3bc0` fixed in `maanta-app/src/lib/__tests__/marketing-shell.test.ts` and `maanta-app/src/lib/__tests__/pricing-copy.test.ts`; that commit patched two of the three files carrying it and left this one, on both branches. Neither the gap audit nor the finish plan records it | **Closed 2026-08-01.** `lineCommentAt()` ported into `maanta-app/src/lib/__tests__/held-claims.test.ts`, and `codeOnly()` rewritten to strip line comments line-by-line through it instead of by blanket regex — so the fix covers all three assertions the helper feeds, not just the `HELD`-pattern scan. **Verified by mutation in both directions, against both trees.** Positive: a held claim (`anything left in your balance stays yours`) planted on the *same line as an `https://` link* — the exact shape the old stripper deleted before scanning — turns the guard red; **the identical mutation passes 5/5 against `origin/main`'s copy of the file**, which is the blindness demonstrated rather than argued. Negative controls, both still green so the fix does not over-correct: the same claim inside a `{/* … */}` JSX comment, and after a genuine `//` line comment, are each still correctly ignored. De-vacuuming it revealed **no** hidden violation — the guard was blind but the content behind it was clean, the same outcome as the two files `038e3bc0` fixed. Suite 481/481 after the change. **The triplication is also gone**, in the follow-up change: the one implementation now lives in `maanta-app/src/lib/__tests__/helpers/comment-stripping.ts` and all three guards import it, removing 131 lines of duplicated lexer for 19. A fourth private copy is how this defect recurs, and it was found twice — once by `038e3bc0`, once as this row — so the duplication is the root cause rather than a tidiness point. The helper is deliberately not named `*.test.ts`, because the suite glob in `maanta-app/vitest.config.ts` matches only that suffix and would otherwise collect the helper and run it as an empty suite; the count stays 61 files / 481 tests. Its docblock records the trap and states the scope limit out loud (a small lexer, not literal-aware — a `//` inside a quoted string is still treated as a comment start, which can only ever make a guard miss, never false-positive). **Re-verified after the extraction, since it refactors already-verified guards:** four positive mutations all red (hardcoded `wa.me`; `KES 40 success fee`; a held claim on a line with a link; `Boost any deal` in JSX) and four negative controls all green (each of those planted inside a `{/* */}` or `//` comment, including a `wa.me` URL *inside* a comment — which is the both-directions case, since the stripper must ignore `//` in `https://` while still honouring a genuine `//` comment that contains a URL) | eng |

| D39 | open | doc-stale | 2026-08-01 | Marketing site | `docs/ops/marketing-site-gap-audit.md` GAP-03 records `/how-it-works` as a live **rewrite** serving `/shoppers` at a second URL, on the evidence of an HTTP 200 with `x-matched-path: /shoppers` and a byte-identical body. The repo says otherwise: `maanta-app/next.config.mjs` declares `{ source: "/how-it-works", destination: "/shoppers", permanent: true }` inside `redirects()`, which is a 308, and `rewrites()` contains only the three PostHog `/ingest/*` proxy rules. The audit's observations are also exactly what a **followed** 308 looks like to a redirect-following HTTP client, and the PostHog pageviews it cites are explained by `/how-it-works` having been a real page until the marketing rebuild (`docs/skills/e2e-surface-matrix-2026-07-30.md`). So either the finding is a measurement artifact or something outside the repo is intercepting the path |  Resolve with `curl -sI` and redirect-following **off**: a 308 closes this row and closes finish-plan Step 3 with no code change; a genuine 200 means the cause is outside `maanta-app/next.config.mjs` and must be found in the Vercel project configuration before anything is edited. **Do not edit the redirect on the strength of the audit alone.** Reasoning in `docs/ops/marketing-site-repo-map.md` §4. **Partial progress 2026-08-02, and a correction to an earlier attempt at closing this row.** This row was briefly marked `closed` on the reasoning below and is **reverted to `open`**, because that reasoning contained an error caught in review on #169. **What is now established, and is not in doubt:** the repository's compiled behaviour is a 308. The generated `routes-manifest.json` at `.next` lists `/how-it-works` → `/shoppers` with `statusCode: 308`, `how-it-works` appears in the `rewrites` array zero times, there is no `vercel.json` in the tree, and `maanta-app/src/middleware.ts` does not mention the path. That is guarded going forward by `maanta-app/src/lib/__tests__/marketing-redirects.test.ts` — see **D57**, which stands on its own and is closed. **What is also established:** the audit's *evidence* for GAP-03 is uninformative. Fetching production reproduced the audit's observation for `/how-it-works` (HTTP 200, no `location`, `x-matched-path: /shoppers`) and reproduced it identically for `/for-shoppers` and `/for-merchants`, which are undisputed 308 redirects, with `x-matched-path` tracking each one's own destination. A followed 308 and a rewrite are indistinguishable under this measurement, so the audit's 200 supports neither conclusion. **The error, stated plainly because it was asserted with confidence:** the first closure argued that the rewrite hypothesis "would additionally require `/for-shoppers` and `/for-merchants` to be rewrites too". That does not follow. A Vercel-side rewrite on `/how-it-works` **alone** is entirely consistent with the other two being ordinary redirects; the control shows the measurement cannot distinguish, not that the hypothesis is excluded. Sufficiency of the redirect explanation is not exclusion of the rewrite one. **What remains open, and why this row does:** nothing available to a Claude session can rule out routing configured outside the repository. The network policy refuses the CONNECT tunnel to `www.maanta.app` with the proxy's own 403 — the D42 constraint — the Vercel fetch tool that can reach the host follows redirects, and `get_project` does not expose project-level routing. **Next step, unchanged from the original remedy:** one unfollowed `curl -sI https://www.maanta.app/how-it-works` from an ordinary terminal. A 308 with `location: /shoppers` closes this row; a genuine 200 means the cause is outside `maanta-app/next.config.mjs` and must be found in the Vercel project configuration. Recorded rather than resolved silently, per the register's own rule about conflicts between documentation and production evidence. → **T4 sweep 2026-08-19:** **repo side unchanged and still settled; the measurement is still owed.** `maanta-app/next.config.mjs` declares `{ source: "/how-it-works", destination: "/shoppers", permanent: true }` inside `redirects()`, and `rewrites()` still holds only the PostHog proxies. The `curl -sI` with redirect-following off was **not** performed this session: this environment's egress proxy refuses `maanta.app` directly (HTTP 403), and the Vercel fetch that works instead follows redirects, so it cannot answer the question this row asks | eng |

| D40 | closed | code-outlier | 2026-08-01 | Marketing site | Not one page on the marketing site emits `<link rel="canonical">` — there is no `alternates` key anywhere under `maanta-app/src/app/(marketing)/`. Separately, `maanta-app/src/app/layout.tsx` sets `openGraph.url` to the bare site origin rather than each page's own URL, so every share of a sub-page canonicalises its unfurl to the homepage; and because a page-level `openGraph` object **replaces** the parent's wholesale in the App Router rather than merging field-by-field, the five pages that declare their own lose `og:url`, `og:site_name`, `og:locale` and `og:type` entirely. The site therefore claims per-page social identity it does not have. `metadataBase` is already correctly set, so this is narrower than it first appears | Add a shared metadata helper setting `alternates.canonical` and spreading the shared `openGraph` base before per-page overrides, and route all 15 marketing pages through it. The helper must read the same `NEXT_PUBLIC_APP_URL` fallback constant as `maanta-app/src/app/layout.tsx`, or preview deployments will emit production canonicals. **Closed 2026-08-01 (finish-plan Step 4).** `maanta-app/src/lib/marketing/page-metadata.ts` builds every marketing page's `Metadata`: `alternates.canonical` plus a shared `openGraph` base (`type`, `siteName`, `locale`) spread in **before** the page's overrides, so a page can restyle its social title without silently dropping the other four fields. **All seventeen** marketing `page.tsx` files now go through it — enumerated, not sampled: `find src/app/(marketing) -name page.tsx` returns seventeen, and all seventeen call `pageMetadata`. Two of them reached this row by a different path than the one originally written here. `/pricing` had no metadata at all when this fix was authored and `/merchants/join` was a `"use client"` module that could not export any; both were fixed independently in the intervening work (**D52**, and the `/pricing` card rebuild), so this pass wraps metadata they already had rather than creating it. The `/merchants/join` server/client split therefore predates this change — `maanta-app/src/app/(marketing)/merchants/join/join-form.tsx` already existed, mirroring the `waitlist` shape, and is unchanged here. `path` is passed **relative** and resolved against the root layout's `metadataBase`, so this module holds no second copy of the origin and a preview deployment cannot emit production canonicals. GAP-05 fixed in the same pass: `/mall-operators` and `/about` no longer split one sentence across `og:title` and `og:description`, both halves recombined from each page's own approved copy. **Verified in built HTML, not source** — 16 prerendered routes each carry a canonical and `og:url` equal to their own URL, with all three OG base fields; `/waitlist` is dynamically rendered so it has no build artefact to read and is reported by name rather than skipped. Guarded by `maanta-app/scripts/check-canonicals.mjs`, chained into `npm run build` beside the token gate — **not** a vitest suite, because CI runs `test` before `build` and a test reading `.next/` would fail there. It derives its expectations from the generated sitemap, so "sitemap URL and page canonical agree" is enforced rather than coincidental, and it refuses to report success when it cannot do its job: missing build dir, truncated sitemap, or fewer than 14 routes inspected all exit 1, and it asserts in the inverse direction that the 404 has **no** canonical. Mutation-tested four ways — canonical removed, `og:url` reverted to the bare origin, `og:site_name` dropped, canonical wrongly added to the 404 — each turns it red; and two vacuity controls (absent build dir, sitemap with zero pages) make it refuse rather than pass, which is the specific failure D38 and the three strippers before it were made of. **The wiring is guarded too**, by `maanta-app/src/lib/__tests__/build-gates.test.ts`: a post-build gate is only as durable as the `build` script that invokes it, and deleting the `&& npm run check:canonicals` would have disabled it silently with a green suite and a green build. That test asserts both output gates exist, are named in `build`, run **after** `next build`, and are joined with `&&` rather than `;` so a failure actually fails the build — mutation-tested by unchaining one and by re-joining with `;`. It also closes the same hole for `check-tokens.mjs`, which had shipped unguarded in exactly that way since it was written | eng |

| D41 | closed | code-outlier | 2026-08-01 | Marketing site | `/contact` is a target marketing page whose enquiry form **does not exist in server HTML**. `maanta-app/src/components/marketing/EnquiryRouter.tsx` is a client component calling `useSearchParams()` to read `?topic=`, and `maanta-app/src/app/(marketing)/contact/page.tsx` wraps it in a `Suspense` whose fallback is a bare pulsing rectangle — so the rendered page ships zero `<form>` and zero inputs, directly above server-rendered copy promising "This form and email — We reply within 1 business day". Without JavaScript the page promises a form it never renders. This is **D28's successor**: D28 was a form that POSTed nowhere while telling the sender it had arrived, closed by wiring `/api/contact` to Resend; the form is now correctly wired and is no longer rendered. The same failure mode, one layer down | Move the topic-dependent behaviour off the render path so the form markup is server-rendered, following the in-repo precedent — `maanta-app/src/app/(marketing)/waitlist/waitlist-form.tsx` prerenders because it is a client component that never calls `useSearchParams()`, **not** because it is a server component, which the finish plan states incorrectly. Add a `<noscript>` naming WhatsApp and the support email. Close by citing a guard asserting `/contact`'s built HTML contains a `<form>` and zero `BAILOUT_TO_CLIENT_SIDE_RENDERING` markers. Note the analytics half needs no work: `marketing_form_submitted` already exists in `maanta-app/src/lib/marketing/analytics-events.ts` and is already called — it has simply never fired, because the form has never rendered. Detail in `docs/ops/marketing-site-repo-map.md` §6. **Closed 2026-08-01 (finish-plan Step 5).** `EnquiryRouter` no longer calls `useSearchParams()`; it reads `?topic=` from `window.location.search` in a mount effect, which never runs on the server and so cannot affect rendered HTML. The `Suspense` boundary and its pulsing-rectangle fallback are deleted from `maanta-app/src/app/(marketing)/contact/page.tsx` — with nothing suspending, a boundary there would only be a fallback with nothing to fall back from. **`/contact` stays a static prerender** (`○ /contact` in the build output), which the alternative in `marketing-site-repo-map.md` §6 — taking `searchParams` as a page prop — would have cost: that is what makes `/waitlist` dynamic and is why it has no build artefact for the canonical guard to read. The behaviour given up is reactivity to a mid-page `?topic=` change; the only inbound links carrying the parameter are the two `/mall-operators` CTAs, both to `?topic=mall-operator`, and nothing on `/contact` rewrites it, so the reactive dependency never fired. A `<noscript>` block now names WhatsApp and the support email, because the form renders without JavaScript but submits through a JS handler and cannot send — saying so is the honest state rather than a second silent promise. **Verified in built HTML, not source:** `.next/server/app/contact.html` went from 0 `<form>`, 0 inputs, 1 `BAILOUT_TO_CLIENT_SIDE_RENDERING` and a bare `animate-pulse` div, to 1 `<form>`, 5 input controls, **0** bailout markers, 0 `animate-pulse`, and the router heading present. Guarded by `maanta-app/scripts/check-server-forms.mjs`, chained into `npm run build` beside the token and canonical gates — **not** vitest, because CI runs `test` before `build` and a test reading `.next/` would fail there. It asserts a `<form>`, at least one input control, a string only that page's own form renders (so a header search box could not mask a regression), and zero bailout markers; it treats a missing artefact for a route it lists as prerendered as an **error**, not a skip, since that would mean the route silently went dynamic. `/waitlist` is dynamic and is named in the output rather than dropped. **Mutation-tested six ways**, five against built output — `<form>` removed, bailout marker injected, the form's own copy removed, the artefact deleted, the build directory absent — and, decisively, by reverting both source files to the pre-fix state and running a real `next build`: the guard reports all four `/contact` problems and fails the build, so had it existed, D41 could not have shipped. The wiring is guarded by `maanta-app/src/lib/__tests__/build-gates.test.ts`, whose `&&`-joining assertion was rewritten to derive from its gate table rather than name two scripts literally — otherwise adding this third gate would have left it unchecked, which is the same "the guard only covers what someone remembered" failure D52 closed | eng |

| D42 | closed | doc-stale | 2026-08-01 | Legal | The repo states in three places that the four legal routes carry a `noindex` meta tag — the docblock in `maanta-app/src/app/robots.ts`, the identical docblocks on each legal page, and `maanta-app/src/lib/__tests__/marketing-a11y.test.ts`, which asserts the `robots: DEMO_MODE ? { index: false` source pattern on all four. `docs/ops/marketing-site-gap-audit.md` §6.2 reports, from rendered production HTML, that none of the four emits one. Both cannot be true. The contradiction is sharp because `DEMO_MODE` in `maanta-app/src/lib/marketing/demo.ts` is a plain constant driving **both** the page `noindex` and the `robots.txt` disallow list, and the audit confirms the disallow lines are live — so the constant was `true` at build and the tag should have rendered. Either the audit's scan missed a `name="robots"` tag while looking for `property=`, or the tag genuinely is not emitted and the a11y guard is vacuous in exactly the way rule-2 guards are meant to prevent | **Closed 2026-08-01 — the repo was right and the audit finding is refuted.** Production HTML was fetched for `/privacy` and `/cookies` (via the Vercel deployment fetch, since the network policy blocks direct egress to the host) and **both emit `<meta name="robots" content="noindex, nofollow"/>`**. The audit's §6.2 premise — "carry no `noindex` meta tag" — is wrong; the tag is `name="robots"`, and the audit's other head checks look for `property=`, which is the likely cause of the miss. Guarded by `maanta-app/src/lib/__tests__/marketing-a11y.test.ts`, whose source-level assertion is hereby shown to correspond to rendered output rather than being vacuous — the one guard confirmed against render, not merely read. **Consequence for finish-plan Step 7b: LEG-02 is not a defect and no `noindex` may be added.** The founder ruling narrows to one question — keep or remove the four `Disallow` lines in `maanta-app/src/app/robots.ts` — with `noindex` unaffected by either answer. **Do not flip `DEMO_MODE` to resolve it**: that flip also publishes the legal routes to the sitemap and removes every pre-launch disclosure sitewide. Same pass confirmed LEG-01 exactly: three `PendingValue` markers live, `{{CLERK_REGION}}` and `{{SENTRY_REGION}}` on `/privacy`, `{{AUTH_COOKIE_LIFETIME}}` on `/cookies`, matching `TOKEN_OWNERS` in `maanta-app/src/lib/marketing/legal-docs.ts` with nothing extra. Analysis in `docs/ops/marketing-site-repo-map.md` §8 | founder |
| D47 | closed | doc-stale | 2026-07-30 | Process | `CLAUDE.md`'s Commands section listed only `dev`, `test` and `build`, and called vitest the thing that "also runs in GitHub Actions CI". CI blocks on five things — lint, typecheck, test, build and the `db-tests` SQL job — so the one document every session is told to read described a green run that CI would reject. The nearest miss is a SQL change: nothing in the commands list pointed at `make db-verify`, so a migration could be written, `npm test`-ed, and reported verified without its assertion suite ever running | Commands section rewritten against `.github/workflows/ci.yml` and the root `Makefile`: all five gates named, `make db-verify` named as the check for migrations, plus an explicit line that passing only `npm test` is not verified. no guard: a test could in principle diff the commands list against the CI workflow, but the list is prose with per-command context, not a machine-readable set — the honest guard is that the gates themselves are in CI and fail loudly | eng |
| D48 | closed | doc-stale | 2026-07-30 | Auth | `CLAUDE.md` described the app as "Next.js (App Router) + Supabase" and named Clerk nowhere, while `maanta-app/src/middleware.ts` runs `clerkMiddleware()` on every path and Clerk is the launch auth strategy. A session orienting from `CLAUDE.md` alone would form the wrong model of how routes are gated — the exact confusion `AGENTS.md` documents at length (placeholder Clerk keys make every browser navigation fail the handshake). The `MAANTA_AUTH_STRATEGY` toggle was likewise absent | Layout table now names Clerk in the stack, and a one-line auth summary points at `docs/ops/auth-strategies.md` and `docs/skills/clerk-auth.md`. no guard: the drift was an incomplete sentence in a guidance document; no test can assert that prose describes the stack. The behavioural truth is the middleware, which is unchanged | eng |
| D49 | closed | doc-stale | 2026-08-01 | Ops | `docs/maanta-launch-readiness-tracker.md` — the file its own header calls the gate-status source of truth — described production as "live and **schema-aligned**" in two places: the 2026-07-29 audit block and the E10 gate row. That was accurate on 2026-07-29 and was overtaken the next day by D24 and D25, and on 2026-08-01 by D37. The phrase then propagated: it was copied into `CLAUDE.md` by the 2026-08-01 operating-standard session, which is how a stale tracker line became the orientation every future session reads first | Corrected in place: a dated 2026-08-01 note at the top states production is live and serving but not a clean mirror of `main` and names D24/D25/D37; the 07-29 block is kept but marked a dated record whose schema-aligned finding no longer holds; E10 no longer asserts alignment. New row **E17** tracks the reconciliation itself. no guard: no test can assert that an operational status line still matches a live database — the guard is the read-back named in E17 (`pg_get_functiondef` for `claim_deal` must contain `deal_paused`, and the ledger must match the repo's filenames), which is what closes D24 and D25 | founder |
| D50 | open | product-decision | 2026-08-01 | Marketing site | `CLAUDE.md` states that the demo-data banner "stays on `(shopper)/layout.tsx` and `merchant/(app)/layout.tsx`, where synthetic deal rows actually render", and `maanta-app/src/lib/__tests__/marketing-shell.test.ts` enforces its absence from marketing routes in both directions (D33). The premise under that rule — that no synthetic deal rows render on a marketing route — stopped being true on 2026-08-01. The home hero now renders `maanta-app/src/components/marketing/HeroShot.tsx`, a CSS mockup of the shopper feed containing three invented shops with invented prices, on `/`. So a marketing route displays fabricated deal rows while the banner whose whole purpose is to disclose exactly that is, correctly per D33, not mounted there. The rule is not violated as written; its justification is what no longer holds, and a future session reading D33 would conclude marketing carries no synthetic content | **Founder decision 2026-08-01**, taken with the alternatives on the table (a mechanic-only mockup with no invented merchants, or a scaffold awaiting a real capture) and this consequence named before the choice rather than discovered after. Disclosed rather than silent: `HeroShot` renders a visible caption "Illustration · example shops and prices" directly under the mockup, plus an `sr-only` sentence stating the shops and prices are invented examples; the mockup body is `aria-hidden` so assistive tech gets that sentence instead of walking invented listings as though they were a real feed. Guarded by `maanta-app/src/lib/__tests__/marketing-hero-shot.test.ts`, **proved non-vacuous by mutation**: deleting the caption fails the guard, moving the caption into a JSX comment fails it (the string is still present in the file, so this is the case a naive `includes()` would miss), and rendering a sample price in `text-brand` fails it. Scope limit stated in that file rather than left to be found — it reads source, not built HTML, because `.github/workflows/ci.yml` runs `npm run test` before `npm run build` so `.next/` does not exist at test time; a guard that skipped on a missing directory would pass vacuously every CI run. Close by replacing the mockup with a real capture of the live feed at Node 0 once BBS carries real deals, at which point the caption and this row both go, or by reverting to a mechanic-only illustration. **Residual risk that cannot be guarded:** an invented shop name could coincide with a real Eastleigh business, which would turn an illustration into a claim about that business — the names were chosen to be generic for this reason, but no test can check it. → **Widened 2026-08-16 to a second surface, by founder decision taken before it was built rather than discovered after.** The hero guard asserted `HeroShot` was Home-only and said in as many words that adding it elsewhere "needs its own decision rather than inherited silently" — so the decision was put to the founder with the alternatives priced, and answered: **illustrated rather than screenshotted, shopper flow, `/shoppers` only.** `maanta-app/src/components/marketing/ShopperWalkthrough.tsx` now draws the product's three real screens under the existing "Three steps" heading — the feed row, the claimed-code card from `maanta-app/src/app/(shopper)/tickets/[id]/claimed-code.tsx`, and the `status === "success"` verified state — each drawn from the real component so a shopper recognises the screen at the counter. `HeroShot` remains Home-only and the guard still asserts it. **Screenshots were the declined alternative, and the reason belongs in this row:** a capture taken today would show **demo data** (213 of 214 merchants are `is_demo`, per **D111**), so it would present the same invented shops while *looking* like genuine evidence — a labelled illustration is the more honest artefact, not the lazier one, and it also costs no image bytes on mall wifi and cannot go stale unnoticed, since no build gate can detect a stale PNG. **The invented names did not multiply:** both surfaces now read from one `maanta-app/src/lib/marketing/sample-deals.ts`, and the walkthrough follows `SAMPLE_DEALS[0]` through all three panels — one shopper, one deal — so it introduced **zero** new names, which keeps the unguardable residual risk above exactly as large as it already was rather than tripling it. Guards added to `maanta-app/src/lib/__tests__/marketing-hero-shot.test.ts`: visible disclosure, `aria-hidden` body plus the `sr-only` sentence, money never amber, **no `KES` inside the code card** (frozen rule 6 — verified in *rendered* `.next/server/app/shoppers.html` as well as source, where the card region contains no `KES`), no amber fill outside the status dot (frozen rule 1 — the page's one amber action stays its CTA), and that no invented name is retyped outside the shared list. → **Widened again 2026-08-17 to a third surface — `/merchants`, on the founder's instruction and on the same terms** (illustrated rather than screenshotted, that page only). `maanta-app/src/components/marketing/MerchantWalkthrough.tsx` draws the four counter screens under the existing "How it works at your counter" heading: the new-deal wizard's fields, **the shopper's code card as it appears on the customer's phone** (included so staff recognise what will be held up at them), the redeem keypad from `maanta-app/src/app/merchant/(app)/redeem/redeem-keypad.tsx`, and the `disclose` screen carrying `FeeDisclosure`'s exact wording. **This one exists for a sharper reason than familiarisation:** a merchant's fear is a surprise debit, the product's answer is a two-step resolve-then-charge where the fee is on screen before the only button that charges, and that is a sentence nobody believes and a picture everybody reads. It is therefore the first illustration to carry an **amber fill** — exactly one, the drawn `Confirm redemption — KES 30 fee` button — and that is load-bearing rather than decorative: stripping its amber to tidy the page palette would delete the thing the panel teaches, that a single named deliberate action is the only thing that charges. It is `aria-hidden`, non-interactive, and inside a bordered device panel under an Illustration caption, so it does not compete with the page's real CTA as an action. The guard pins it at **exactly one** amber fill so the justification cannot be borrowed for a second element later. Still zero new invented names — a third surface reading the same `sample-deals.ts` and the same `SAMPLE_DEALS[0]`. Additional guards: the fee-disclosure copy and the fee-bearing button label (if either goes, the panel stops teaching "no surprise debit" and becomes a picture of a keypad), `FACTS.successFeeKes` rather than a typed `KES 30`, and no `KES` inside the shopper's code card. Rendered-output checks on `.next/server/app/merchants.html` confirm all four panels and the empty-of-price code card. → **T4 sweep 2026-08-19:** **still accurate.** `HeroShot.tsx` still renders the mockup and still carries the "Illustration · example shops and prices" caption the founder decision required | founder |
| D51 | open | code-outlier | 2026-08-01 | Marketing site | The Elite launch offer is stated in two places that cannot agree. `maanta-app/src/app/(marketing)/merchants/page.tsx` renders it from `OFFERS.eliteTrial` in `maanta-app/src/lib/marketing/facts.ts` and wraps the whole block in `isOfferLive(...)`, so a closed offer disappears rather than going stale — risk R7, which the marketing build treats as a rule. `maanta-app/src/app/(marketing)/pricing/page.tsx` does neither: it types the cohort size, the trial length and the grace period as literals in JSX, and states the offer unconditionally with no expiry gate. Two consequences, one already true and one dated. Already true: four numbers duplicate constants that exist, against the marketing hard rule that every number renders from `facts.ts` — and `pricing-copy.test.ts` anticipates exactly this in its own comment, noting that rendering `cohortShops` "states it more reliably than a literal 100 does". Dated: when `OFFERS.eliteTrial.expiresOn` passes, `/merchants` drops the offer and `/pricing` keeps promising it, which is the D34 failure mode — one commercial fact, two pages, no shared source | **Both halves fixed on the pricing page 2026-08-01.** `/pricing` now reads `OFFERS.eliteTrial.cohortShops`, `.days` and `.postTrialGraceDays` and renders the offer block only when `isOfferLive(OFFERS.eliteTrial)`, matching `/merchants`. No visible change today — `expiresOn` is 2026-10-31, so the block still renders; the change is that it will now vanish on the same day `/merchants` drops it instead of outliving it. Guarded by the existing `maanta-app/src/lib/__tests__/pricing-copy.test.ts`, whose launch-offer assertion already required the cap, the node and the success-fee caveat on every page that mentions the trial, and which reads source rather than rendered output so gating the block does not make it vacuous. That guard needed one widening to accept the single-sourced form: its node check tested for a literal `BBS`, which no page rendering `FACTS.launchMall` can satisfy, so it now also accepts `launchMall` — the same accommodation its cap check already made for `cohortShops`, and made for the same reason. Verified by mutation: removing the node reference from `/pricing` fails the guard, and removing the success-fee caveat fails it. Row stays **open** until the offer actually expires and both pages are confirmed to have dropped it together, since that is the behaviour being claimed and it cannot be observed before 2026-10-31. → **T4 sweep 2026-08-19:** **still accurate.** `maanta-app/src/app/(marketing)/pricing/page.tsx` imports `OFFERS`, `isOfferLive` and reads the trial from `OFFERS.eliteTrial`, so both halves are fixed as recorded; the row is…91134 tokens truncated…/src/components/ui/inputs.tsx` were the multiplier: `SearchField` is rendered by five admin screens (`deals`, `customers`, `approvals`, `billing`, `merchants`) and shopper `/search`, and **every one of the six call sites passed a placeholder and no name**; `PhoneField` rendered its label as a `<span>` rather than a `<label>`, so the tel input had no name at all, and its country-code button announced as "+254, expanded" with no hint that pressing it picks a country. The country filter input inside that dropdown had nothing either. A placeholder is not an accessible name: it fails WCAG 3.3.2 on its own and it disappears the moment someone types. `Toggle`, in the same file, already carried a comment describing this exact defect for its own control — so the pattern was understood and the fix had not been applied to its neighbours *(Originally recorded as **D174** on the branch this landed from, and renumbered to **D183** on rebase: canonical `main` had independently taken D174 for an unrelated finding. The repository is canonical for drift IDs, so the already-merged row keeps the number and this one moves — the same rule applied to the D168/D172 collision. Nothing about this finding changed.)* | Fixed 2026-08-24: `PhoneField` now pairs a real `<label htmlFor>` with a `useId()`, the country button and country search carry `aria-label`, and `SearchField` derives one from its effective placeholder (trailing ellipsis stripped; a caller-supplied `aria-label` still wins through `rest`). Guard: `maanta-app/src/lib/__tests__/field-labels.test.ts` walks every `<input>`/`<textarea>`/`<select>` with a placeholder and requires `aria-label`, `aria-labelledby`, an `id`, or a still-open wrapping `<label>` — verified to fail when an `aria-label` is removed. One named exception with its reason in-file: the `new-ticket-form.tsx` textarea whose `<label>` comes from a local `field()` helper a lexical scan cannot see through | engineering |
| D184 | closed | doc-stale | 2026-08-25 | Node 0 / evidence counting | **D174 split the evidence counters for redemptions and left MERCHANT records unclassified, so a census reading `merchants WHERE is_demo = false` gets 2 and nothing in the repo says neither is a customer.** The follow-on to D174, found while checking whether PR #270 already covered it: it does not. #270 rules on `redemptions.status = 'success'` — internal 1, external 0 — and names "E2E Full Sweep Shop" only as that redemption's merchant. Production read back 2026-08-25: two non-demo merchant rows, both `status = 'active'` at BBS Mall — `bf66a041` **SKANDI SKAN**, created 2026-08-16, a founder registration exercise run with a family member, and `67fe233d` **E2E Full Sweep Shop**, created 2026-08-23 by the full-role E2E sweep. Neither is a merchant who chose MAANTA. The fact was already written down once, in `docs/security/pre-field-adversarial-audit-2026-08-24.md` ("2 merchants, both internal"), but a dated audit report is not where a future census looks; meanwhile `docs/skills/staff-seat-linking-and-search-pause-2026-08-19.md` and `docs/maanta-decision-queue-2026-08-19.md` still read "real merchants: 1 — SKANDI SKAN" unqualified. Same failure mode as D174 one table over: a bare `SELECT count(*)` is one careless step from reporting MAANTA's first acquisition as already done, and Merchant 01 is the **third** non-demo merchant row, not the first | **Closed 2026-08-25 — classification only; no schema change, no production write, and the rows were not touched.** The split now covers merchant records in `CLAUDE.md`'s Node 0 evidence block, beside the D174 redemption counters, naming both ids and both provenances and restating that `is_demo = false` is a real *record*, not a real *customer*. Guard: `maanta-app/src/lib/__tests__/node0-evidence-counters.test.ts` asserts CLAUDE.md still names `bf66a041`, `67fe233d` and `72f95ac8` and still states the external counter as zero. It asserts **identifiers, not counts** on purpose — the counts change the moment Merchant 01 onboards, and a guard that has to be edited then is a guard that gets deleted then. It cannot verify provenance, which is unguardable by construction: an internal test row and a genuine merchant are identical to the schema, which is exactly why the classification has to be prose | eng |
| D185 | closed | code-outlier | 2026-08-25 | Admin reporting / money | **`/admin/reports` had D164's exact defect on the money figure, and had never been covered by either ruling.** Found by the all-screens audit, not by a report. The page destructured five reads straight off `Promise.all` — `const [{ count: verified }, { data: feeRevenue }, …] = await Promise.all([…])` — discarding every `error`. One failed query therefore rendered **"Verified redemptions 0"** and **"Success-fee revenue KES 0"** as confident statements about the business. This is the same shape D149 fixed on `/founder` and D164 fixed on `/admin`; reports was the third KPI surface and nothing was watching the set, only its known members. It is also the surface that puts the zero next to money, which the frozen UI rules treat as the most dangerous place for a false figure | **Fixed 2026-08-25** in the same audit. `results` is inspected before any destructure and the page renders the shared `LeadsReadError` — identical wording and component to the other two, so an operator learns one failure shape. Unlike `/admin` there is nothing to exclude here: every read is a metric, so the guarded set is the whole array. Guard: `maanta-app/src/lib/__tests__/claims-metric.test.ts` now enumerates **all three** KPI surfaces explicitly rather than globbing — a glob would pass silently the day a fourth dashboard is added, which is exactly how this one went two rulings uncovered. Verified to fail against the pre-fix source on all three new assertions | eng |
| D186 | closed | code-outlier | 2026-08-25 | Observability / production surface | **Sentry wizard scaffolding shipped to production: a public, unauthenticated endpoint whose only behaviour is to throw a 500.** `src/app/api/sentry-example-api/route.ts` raised `SentryExampleAPIError` on every GET, and `src/app/sentry-example-page/` rendered the page that calls it. Both were compiled into the production build — `.next/server/app/api/sentry-example-api/route.js` present, `/sentry-example-page` in `routes-manifest.json` — with no middleware or config exclusion anywhere. Blast radius is the error channel rather than data: anyone could generate unlimited 500s and Sentry events on demand, and Node 0 field validation is precisely when a real error has to be distinguishable from noise. A public page that exists only to break is also not something to have live while the first genuine merchant is being shown the product | **Removed 2026-08-25** — both the route and the page deleted; `npm run build` re-run and neither appears in `routes-manifest.json`. Sentry itself is untouched and still captures real errors. no guard: a test asserting "no file named sentry-example-* exists" would be a rule about one vendor's scaffolding rather than about MAANTA, and the deletion is verified by the route manifest, which the build already produces | eng |
| D187 | open | code-outlier | 2026-08-25 | Read-failure handling / all roles | **The read-failure pattern is applied to three KPI surfaces and to nothing else, and there is no rule saying where it belongs.** The 2026-08-25 all-screens audit checked every `page.tsx` that reads data: of 62 data-reading screens, only `/admin`, `/founder` and `/admin/reports` (after D185) inspect their query errors. The rest destructure and render. **Most of those are genuinely fine and this row is not a request to sweep them** — the audit checked the dangerous shape specifically. `merchant/(app)/redeem` degrades to hiding an informational banner, and the verify path is RPC-enforced regardless; `admin/agents` and `merchant/(app)/deals` use `Map.get() ?? 0`, a legitimate map-miss default; `admin/customers/[id]` reads a column off an already-fetched row. The one remaining case of the dangerous shape is **`merchant/(app)/deals/[id]`**, where a failed `verifiedCount` read renders `0` verified against a deal — merchant-facing, lower stakes than money, and left alone deliberately under the Node 0 freeze rather than swept the week of Merchant 01. The real gap is that **nothing decides** which screens owe a read-failure state. D149 and D164 each fixed the surface in front of them; D185 found the third by audit rather than by rule | **Founder decision, deliberately deferred past Merchant 01.** Options: (a) require the guard on any screen rendering a count, sum or money figure — the narrow rule, and the one that would have caught all three; (b) require it on every data-reading screen — safe but a 60-file sweep; (c) leave it case-by-case and accept the fourth instance will also be found by audit. Recommend (a) **after** first field evidence, not before: the sweep itself is the risk the freeze exists to prevent. Not a Merchant 01 blocker — the money path is enforced in `claim_deal` / `verify_redemption`, not in any of these reads | founder |
| D188 | open | code-outlier | 2026-08-26 | Evidence counting / demo boundary | **`claim_deal` never sets `redemptions.is_demo`, so EVERY claim made through the product is tagged `is_demo = false` — including a claim against a synthetic merchant.** Read from production: the RPC's INSERT names ten columns (`deal_id, merchant_id, user_id, otp_code, success_fee_charged, consumer_device_id, consumer_gps, status, expires_at, amount_kes`) and `is_demo` is not among them, so it takes the table default from `20260729140000` — `BOOLEAN NOT NULL DEFAULT FALSE`. Demo tagging on redemptions therefore comes **only** from the seed scripts, never from the live path. **Consequence: `redemptions.is_demo` is not a discriminator at all, and the D174 counters cannot be derived from it.** Measured 2026-08-26 — of 6 redemptions tagged `is_demo = false`, exactly **1** has a non-demo merchant (the internal E2E `success` `72f95ac8`) and **5 are `pending` claims against DEMO merchants**. This is not new: 4 of the 5 predate today, which means the "5 real redemptions" cited in `CLAUDE.md`, the session briefs and the demo-mode census were never 5 real redemptions — they were 1 internal success plus 4 demo-merchant claims wearing a non-demo tag. **It compounds while demo mode is on** (founder ruling 2026-08-26 — see the decisions log): real prospects browsing a synthetic marketplace can claim, and every such claim increments the non-demo count. One arrived within about eight hours of the flag going back on (D189) | **Open — founder decision, no code change under the freeze.** The correct field-evidence query joins through the parent and never trusts the flag alone: `... FROM redemptions r JOIN merchants m ON m.id = r.merchant_id JOIN deals d ON d.id = r.deal_id WHERE NOT r.is_demo AND NOT m.is_demo AND NOT d.is_demo AND r.status = 'success'` — which currently returns **1**, the internal E2E survivor, so external field validation remains **0** and the ladder is unaffected. Options: (a) have `claim_deal` inherit `is_demo` from the deal/merchant, which fixes it at the source but is a money-path RPC change and is exactly what the freeze exists to prevent right now; (b) leave the column and make every census join through the merchant, documenting the join as the only valid form; (c) both, after first field evidence. Recommend (b) now and (a) after Merchant 01. **No row is to be edited or deleted either way** | founder |
| D189 | closed | doc-stale | 2026-08-26 | Node 0 / evidence counting | **A real person claimed a synthetic deal on production, and it landed in the non-demo count.** Redemption `aa1f74b1`, 2026-08-25 16:17 UTC, against **"Pepper Pot"** (`merchants.is_demo = true`), status `pending`, `is_demo = false`, since expired unredeemed. Found while re-reading production after the #273 merge, not reported by anyone. It is the first observed instance of the D188 mechanism under the 2026-08-26 demo-on ruling, and it is the concrete form of the risk the 2026-08-24 clearing ruling named in the abstract: "a first independent merchant or shopper who sees a synthetic marketplace contaminates the evidence the pilot exists to collect" | **Closed 2026-08-26 — classified, and the row was NOT touched.** `aa1f74b1` is **internal/demo-marketplace activity, not field evidence**, alongside the four earlier demo-merchant claims and the E2E `success` (D174, D184). It never reached `success`, so neither the internal nor the external success counter moved: internal 1, **external field validation 0**. The D164 migration is separately confirmed correct by this row — it is the first claim stamped after the migration, and production reads **402 redemptions, 401 still NULL, exactly 1 stamped**. No history was fabricated. no guard: no test can tell a prospect's demo-marketplace claim from a genuine one, because the database cannot either (D188) — that is the finding, and the mitigation is the merchant-join census documented in D188 rather than an assertion | eng |
| D175 | closed | code-outlier | 2026-08-24 | Design tokens / motion | **`globals.css` carried two `prefers-reduced-motion` blocks with different durations.** One unlayered at 0.001ms, one inside `@layer base` at 0.01ms, both `!important` on `*`, both collapsing motion to nothing. So the accessibility rule had two homes to keep in step, and because either value is visually instant, **nothing would ever have surfaced the day they diverged** — a second place to enforce a rule is a second place to drift | Fixed 2026-08-24: consolidated to the single unlayered block, whose comment now records why the duplicate was deleted rather than reconciled. Guard: `maanta-app/src/lib/__tests__/marketing-a11y.test.ts` now asserts **exactly one** such block rather than merely that one exists | engineering |
| D176 | closed | code-outlier | 2026-08-24 | Design tokens / motion | **The app declared one shared easing curve, instructed authors to use it, and 42 of 44 transitions ignored it.** `globals.css` has defined `--ease-standard: cubic-bezier(0.22, 1, 0.36, 1)` since the Frozen UI pass, commented "One shared easing for app motion - a calm, decelerating curve. Keep new transitions on this so overlays, presses, and fades feel like one system." Exactly two call sites did (`components/ui/overlays.tsx`). Every other transition used a bare `transition`, which resolves to Tailwind's default `cubic-bezier(0.4, 0, 0.2, 1)` - an ease-in-out that *starts slow*. That curve was running the marketing primary CTA's hover-lift and press response (`components/marketing/sections.tsx`), every card hover, and every chip: on a press it reads as lag, the control lifting late rather than answering the finger. Nothing failed - no test, no type error, no visual diff - the product just felt fractionally cheaper than its own design system specified | Fixed 2026-08-24 by wiring the token as `transitionTimingFunction.DEFAULT` in `maanta-app/tailwind.config.ts`, so a bare `transition` gets the house curve without an author remembering to ask; the 42 call sites were deliberately not edited, because a rule each author must remember is a rule that drifts and this one already had. Verified in the compiled stylesheet: `.transition` now emits `transition-timing-function:var(--ease-standard,cubic-bezier(.22,1,.36,1))` and **zero** occurrences of the Tailwind default remain. Resting-state renders are byte-identical before and after (Playwright, mobile + desktop), so the change is motion-only. Guard: `maanta-app/src/lib/__tests__/motion-system.test.ts` asserts both halves - the token in `globals.css` and the config wiring, including the literal fallback, since an undefined `var()` silently reverts to `ease` | engineering |
| D177 | closed | code-outlier | 2026-08-24 | Motion / performance | **The Toggle knob slid on `left` under `transition-all`, re-running layout every frame.** `transition-all` animates every animatable property including the layout ones, and the knob moved between `left-0.5` and `left-[calc(100%-1.625rem)]` - the one property class motion guidance singles out as never worth animating, on a control used across notification preferences and settings. Two further blanket `transition-all` uses existed (`app/onboarding/page.tsx`, `components/marketing/ScenarioNotice.tsx`) | Fixed 2026-08-24: the knob now travels on `translate-x-5` from a fixed `left-0.5`, composited on the GPU. Geometry verified pixel-identical by rendering both rules side by side in Chromium - 22px left-inset and 2px right-inset in each. The other two now name their properties instead of animating everything; the onboarding pager deliberately keeps animating `width` rather than `scaleX`, which would squash the pills' round caps, and says so in-file. Guard: `maanta-app/src/lib/__tests__/motion-system.test.ts` fails on any `transition-all` in a `.tsx` file | engineering |
| D178 | closed | code-outlier | 2026-08-24 | Shopper / merchant / admin / agent - error states | **Every error boundary rendered its state pinned to the top of a full-height shell, with the rest of the screen empty.** `CLAUDE.md` sets the bar that "Every state is designed: loading, empty, error, offline, expired, denied. A half-built state is what makes a product look unfinished." The *content* met it - icon plus word, body text in ink and only the glyph in flame red, greyscale-readable, per frozen rule 4. The *composition* did not: all five boundaries used `<main className="px-4 pt-10">` with no height, so on a 390x844 phone the block occupied 240px at the top and left roughly 600px of void above the tab bar. Found by rendering the running app rather than by reading source - the shopper feed's error state is what a shopper sees when the deals query fails | Fixed 2026-08-24: all five now use `flex min-h-[70dvh] flex-col items-center justify-center`, which is **the pattern this repo already uses** in `maanta-app/src/app/merchant/(app)/redeem/redeem-keypad.tsx`, `maanta-app/src/app/merchant/(app)/staff/new/page.tsx` and `maanta-app/src/app/(marketing)/download/page.tsx`, so the fix extends a convention rather than introducing one. Measured before and after in Chromium at 390x844: the block moved from y=100 in a 240px main to y=239 in a 591px main. An intermediate `min-h-full` attempt was measured and discarded - the shells wrap children in a `flex-1` block whose flex-derived height is not definite, so the percentage resolved to zero. no guard: the defect is compositional, and a test that pinned specific layout utilities on five files would break on any legitimate restyle while still not proving the state is well-composed; the durable check is the render pass in `docs/ops/motion-and-app-polish-2026-08-24.md` | engineering |
| D179 | closed | product-decision | 2026-08-24 | Merchant / public — Elite pricing | **Opened as a sourcing defect and closed as a commercial ruling: there is no Elite subscription price to source.** The row was raised by the 2026-08-24 UI review because `FACTS.elitePerMonthKes = 3_500` was read by four marketing pages while three merchant screens typed `KES 3,500` as a literal, making it the only commercial number in MAANTA without a single source — and the one under review for Feb 2027, so the one most likely to drift. **The founder ruling of 2026-08-24 disposes of the underlying question rather than the plumbing:** KES 3,500 is not a commercially fixed Elite price, MAANTA is in Node 0 field validation, and merchants and the public will not be anchored to a monthly figure before there is genuine merchant evidence about the value MAANTA creates. **Nine rendered surfaces carried the number and all nine were cleared**: `(marketing)/pricing`, `(marketing)/merchants`, `(marketing)/about`, `(marketing)/faq`, `merchant/(app)/plan`, `merchant/(app)/plan/upgrade`, `merchant/(app)/deals/new/new-deal-wizard`, the rendered `src/content/legal/merchant-terms.md`, and the constant itself. **No replacement number is authorized**; where a price or status must appear the surfaces read *"Pricing coming soon"*. Elite is unchanged as a product and its benefits are still listed everywhere they were. **The KES 30 success fee is deliberately untouched** and remains explicit on both plan cards, in the upgrade panel, in the wizard's comparison table and in the merchant terms — it is a live Node 0 commercial hypothesis and the two must not be blurred. The constant was removed rather than zeroed or left in place, because a canonical `elitePerMonthKes` is exactly the anchor the ruling withdraws | Ruling recorded in `docs/maanta-decisions-log.md` (2026-08-24, sixth entry) and in `CLAUDE.md` frozen business rules, which now states that paid Elite has no published price and supersedes the Feb 2027 review date. Guard: `maanta-app/src/lib/__tests__/pricing-copy.test.ts` ("publishes no Elite subscription price on any rendered surface") scans `src/app`, `src/components` and `src/content/legal` only — **documentation is deliberately not scanned**, so the dated audits and this register can still record that KES 3,500 was once assumed. Verified to fail when the price is reintroduced on a merchant surface, and verified NOT to fire on a legitimate four-digit deal price. Supersedes **D15**, which tracked the missing `app_config` key behind the same number | founder |
| D180 | closed | code-outlier | 2026-08-24 | Merchant dashboard | **The merchant's own wallet balance renders with no currency unit, while the admin view of the same value renders with one.** `merchant/(app)/dashboard/page.tsx:56` passes `Math.round(merchant.account_balance).toLocaleString("en-KE")` into a `KpiCard` labelled "Wallet balance", producing a bare **`270`**. The three tiles beside it in the same grid are counts — "Redemptions today", "This week", "Active deals 1/1" — so the one money figure on the screen is visually indistinguishable from an integer count. Admin renders the identical column through `formatKes()` (`admin/merchants/page.tsx:67`, `admin/merchants/[id]/page.tsx:64`, `admin/redemptions/[id]/page.tsx:285`), giving **`KES 270`**. So the person whose money it is gets the unlabelled version and the staff viewing it get the labelled one. Not a miscalculation — the number is correct — but it is the merchant's balance on the screen they check before deciding whether they can still post a deal | open — **owner: engineering**, low. One-line fix: `formatKes(merchant.account_balance)`, which already rounds. Worth confirming with the founder that the KPI grid should carry a currency unit at all rather than a `KES` suffix in the label, since it is the only money tile in that row. no guard proposed yet: a test that every money render passes through `formatKes` would need to distinguish money from counts, which is a judgement the type system does not currently carry → **Closed 2026-08-30:** the merchant dashboard now renders this value through `formatKes(merchant.account_balance)`, matching the administrative money surfaces. Guard: `maanta-app/src/lib/__tests__/merchant-dashboard-wallet.test.ts`. | engineering |
| D181 | closed | code-outlier | 2026-08-24 | Founder console — error states | **The founder surface has no error or loading boundary, so a failed query drops to the bare last-resort screen.** Segment coverage measured 2026-08-24: shopper 2 `error.tsx` / 2 `loading.tsx` over 16 pages, merchant 1/1 over 23, admin 1/1 over 17, agent 1/1 over 4, **founder 0/0 over 2** — and both founder pages fetch data (`src/app/founder/page.tsx`, `src/app/founder/layout.tsx`). A failed read therefore escapes to `src/app/global-error.tsx`, which by design replaces the whole document: no nav, no shell, no Tailwind tokens, and an inline system font stack. That screen is correct as a true last resort — its raw hex is deliberate and documented, because the root layout's tokens are unavailable there — but it is the wrong response to one metrics query failing on an internal console. Marketing's 0/0 is not part of this: those routes are prerendered and static | open — **owner: engineering**, low, and not a Merchant 01 blocker. Fix is the pattern the other four segments already use, copied from `maanta-app/src/app/(shopper)/error.tsx`: an error boundary rendering `ErrorState` in the `min-h-[70dvh]` centred frame adopted 2026-08-24, plus a loading boundary. *(The proposed new filenames are named in prose rather than cited as paths — this register's guard resolves every cited path, and a file that does not exist yet cannot be cited as evidence.)* no guard: a test asserting every route segment carries a boundary would fail on segments where one is genuinely unnecessary, and deciding which those are is a design call rather than a rule → **Closed 2026-08-30:** `/founder` now carries segment-level `error.tsx` and `loading.tsx`; the error boundary reports to Sentry, preserves the application shell and exposes retry. Guard: `founder-boundaries.test.ts`. | engineering |
| D193 | closed | code-outlier | 2026-08-26 | Merchant till queue (PR C, unmerged) | **Tapping a queue row navigated to `/merchant/redeem?code=<OTP>` — a LIVE 6-digit redemption credential in the till URL.** From there it persisted in shared-counter browser history, `Referer` headers, server access logs and PostHog's `$current_url` on every pageview and autocaptured event, exactly the leak class the repo had already fixed once for the merchant-join phone handoff (`maanta-app/src/lib/merchant-join-handoff.ts`). A later user of the shared till device, or anyone reading URL-derived logs/analytics, could obtain a still-pending OTP for that shop. Bounded — the code only reaches `can_verify` staff's own queue and verification still runs the normal fee-disclosed path at that one merchant — but a credential does not belong in a URL. Found 2026-08-26 by the Cursor Security Agent (MEDIUM) on unmerged PR #277 head `7e0ee13`; no production exposure existed | closed 2026-08-26 on branch `claude/pr-c-merchant-qr-queue`: the panel and keypad share one page, so the code now travels tap → in-memory subscription → keypad state (`maanta-app/src/lib/queue-code-handoff.ts`) and exists nowhere else — no URL, no navigation, no storage; the keypad runs the identical preflight → fee disclosure → explicit Confirm flow, and the redeem page no longer reads `searchParams` at all. Guards: `maanta-app/src/lib/__tests__/queue-code-handoff.test.ts` — mechanism tests plus a D193 ratchet asserting the panel never navigates with the code, the page takes no code from `searchParams`, and the keypad receives codes only through the handoff | engineering |
| D194 | closed | code-outlier | 2026-08-26 | Active-deal cap audit (`enforce_deal_limit()`) | **The cap's `tier_flags` audit row can never persist.** The trigger inserts a `deal_limit_exceeded` (or `flash_not_allowed`) row into `public.tier_flags` and then `RAISE EXCEPTION`s in the same subtransaction, so the exception that reports the refusal also rolls back the record of it. Production agrees and is unambiguous: `public.tier_flags` holds **0 rows of any type** despite the cap having been exercised repeatedly in demo and E2E work. Operators triaging "which merchants keep hitting their limit" have been reading an empty table that looks like "nobody ever hits it". **The cap itself is correct and unaffected** — only its audit trail is dead. Not fixed in PR 0 by founder scope (ratchet the rule, do not re-engineer the trigger); a fix means logging from the caller or an autonomous transaction, and belongs with the admin-operations package that would read it | Measured 2026-08-26: production `public.tier_flags` = 0 rows, 0 distinct flag types, while 274 active deals exist and 0 merchants are over cap. Reproduced locally on the fresh 104-migration chain — the refusal raises, the flag row does not survive. Pinned (as the observed zero) by `maanta-app/supabase/tests/deal_limit_cap_test.sql` scenarios A, B and D, which fail if the audit ever becomes durable so the change is deliberate **Resolved in PR #283:** refusal logging moved to the authenticated server caller after the failed INSERT returns, so the audit write is outside the trigger subtransaction; both publish and repost paths persist `deal_limit_exceeded` / `flash_not_allowed` best-effort, and the admin console reads recent `tier_flags`. The SQL trigger remains unchanged and still enforces the cap. | founder |
| D195 | closed | code-outlier | 2026-08-26 | Merchant "Active deals n/limit" (dashboard, deals list) | **The merchant UI counts active deals differently from the trigger that enforces the cap.** The trigger counts `is_active = TRUE` only; the UI counts `countLiveDeals` (`src/lib/merchant-lifecycle.ts`) which additionally excludes deals past their claim window. Nothing in the product sets `is_active = FALSE` on expiry — only the merchant's archive action and the admin remove action do — so a merchant whose only deal expired sees **"Active deals 0/1"** and is then refused a new deal with HTTP 409 `Deal limit reached`. Two honest numbers, two different questions, one screen. **Not "fixed" in PR 0 deliberately**: reconciling them means choosing which definition is canonical, and the founder ruling for this package forbids silently redefining "active". Both definitions are now pinned — the trigger's by `supabase/tests/deal_limit_cap_test.sql` scenarios E and F, the UI's by `src/lib/__tests__/merchant-lifecycle.test.ts`. Fixing it is a candidate for the PR 3 wizard pre-flight, which needs one canonical count anyway | Trigger definition: `maanta-app/supabase/migrations/20260630231915_maanta_schema_v3_baseline.sql` (counts `is_active = TRUE` only). UI count: `maanta-app/src/lib/merchant-lifecycle.ts` (`countLiveDeals`, excludes past-window deals). Consumers: `maanta-app/src/app/merchant/(app)/dashboard/page.tsx`, `maanta-app/src/app/merchant/(app)/deals/page.tsx`. Boundary proved by `maanta-app/supabase/tests/deal_limit_cap_test.sql` scenario E **Resolved in PR #282:** merchant UI now names `is_active=true` occupancy as **Deal slots**, pre-flights that exact predicate before the create wizard, and exposes expired-but-unarchived slot occupants so the merchant can explicitly archive them. The trigger/lifecycle semantics were not changed. | founder |
| D192 | closed | code-outlier | 2026-08-26 | Fast Visit reward (PR B/C, unmerged) | **`record_shopper_arrival` was executable by `authenticated`, so a shopper could stamp a qualifying arrival with a bare Data API call — no scanned QR token, no visit — while the migration's own comment claimed the merchant id is "resolved server-side from the scanned counter QR token".** The grant made the token skippable: a shopper's ticket already shows every id the RPC needs (own user id, redemption id, merchant), so `POST /rest/v1/rpc/record_shopper_arrival` right after claiming would persist a qualifying `fast_visit_qualified_at` with the feature ON, banking reward eligibility from the couch. Bounded — points still require staff verification of a real redemption, and no money moves — but it defeated the check-in evidence the arrival exists to record. Found 2026-08-26 by the Codex review bot (P1) on unmerged PR #276; no production exposure existed | closed 2026-08-26 on branch `claude/pr-b-fast-visit-points` (migration revised in place — never applied anywhere): EXECUTE on the arrival RPC is now `service_role`/`postgres` only, so the QR check-in route — the code that validates the scanned token — is the only door; the route passes the session-derived shopper id and the RPC still enforces claim ownership, merchant match and pending/unexpired against it, with the caller-identity check kept as defence in depth. The same review's P2 (a verify retry exited 409 without repairing an award lost between verify and the original award call) was fixed in the same commit: the `redemption_already_verified` path now re-runs the idempotent award. Guards: `maanta-app/supabase/tests/fast_visit_points_test.sql` scenario A (authenticated and anon must NOT execute the arrival RPC) and the already-verified award-repair case in `maanta-app/src/app/api/redemptions/verify/__tests__/route-fast-visit.test.ts` | engineering |
| D191 | closed | code-outlier | 2026-08-26 | Fast Visit reward (PR B/C, unmerged) | **The Fast Visit design as first implemented decided reward qualification at AWARD time, so flipping `fast_visit_enabled` rewrote history in both directions.** `record_shopper_arrival` stamped `arrived_at` with no gate check, and `award_fast_visit_points` read the CURRENT `fast_visit_enabled()` — so (a) a shopper who scanned while the feature was OFF would retroactively earn points if the founder later turned it ON before staff verified, and (b) a shopper who legitimately qualified while it was ON would silently lose the earned reward if it was turned OFF before verification. Both violate the locked rule that qualification is a fact about the moment of arrival. Found 2026-08-26 by the release-candidate review of branch `claude/pr-b-fast-visit-points` before any merge or apply — no production exposure existed at any point | closed 2026-08-26 on the same branch (revised in place — the migration was never applied anywhere): `record_shopper_arrival` now decides the whole qualification at first arrival (gate ON at that instant + `claimed_at` known + within the inclusive 15-minute boundary) and persists it as `redemptions.fast_visit_qualified_at`, immutable ever after; `award_fast_visit_points` requires that persisted fact and no longer reads the gate at all. Guards: `maanta-app/supabase/tests/fast_visit_points_test.sql` scenarios I1 (gate OFF at arrival → ON later → verified → NO award, re-scan cannot upgrade) and I2 (qualified while ON → OFF later → verified → award still lands), plus the arrival-time boundary scenarios in F | engineering |
| D190 | closed | code-outlier | 2026-08-26 | Shopper ticket / time rendering | **The claimed-ticket screen rendered every wall-clock time in the SERVER's timezone and asserted "today" unconditionally.** `tickets/[id]/page.tsx` carried a private `hhmm()` calling `toLocaleTimeString("en-KE", …)` with no `timeZone` — on Vercel that resolves to UTC, three hours behind Nairobi, so "code valid until 18:45" meant 21:45 on the shopper's clock. Its call sites also hardcoded the word "today" ("code valid until HH:MM today", "Redeemed today HH:MM"), false for any code window crossing midnight — exactly the >24h case that produced D167's `1449:12` — and for any success screen revisited a day later. Found 2026-08-26 while fixing D167 item 3 on the same screen | closed 2026-08-26 on branch `claude/pr-a-claim-expiry-timing` — all four call sites now render through `absoluteTimeLabel` in `maanta-app/src/lib/claim-ticket-time.ts` (Africa/Nairobi wall clock; day word computed in Nairobi's calendar: today / tomorrow / yesterday / explicit date). Guard: the Nairobi-midnight and timezone assertions in `maanta-app/src/lib/__tests__/claim-ticket-time.test.ts` | engineering |
| D196 | closed | code-outlier | 2026-08-27 | Shopper QR check-in | **Cancelling a check-in at a single-claim shop stranded the shopper on a permanent "Checking you in…" screen — the exact opposite of what they had just asked for.** `qr-check-in.tsx`'s cancel handler set state back to `{kind:"idle"}`, but the single-claim auto-check-in effect is one-shot (guarded by an `autoFired` ref, with deps that never change), so nothing re-fired. The render branch `claims.length === 1 && state.kind === "idle"` then showed "Checking you in…" indefinitely with no request in flight, and only a full page reload escaped it. Shipped in PR C (#277) and live on `main`; found by the pre-merge adversarial pass, whose report arrived after the merge. The SSR-only component suite structurally cannot catch it — `vitest.config.ts` runs `environment: "node"`, so effects and handlers never execute | closed 2026-08-27 — cancel now lands in a distinct `cancelled` terminal state naming what is and is not affected ("You've left the queue. Your claim is still valid.") with "Check in again" and "Show my code". Guards in `maanta-app/src/components/__tests__/qr-check-in.test.ts`: the effect-free single-claim branch must reach a terminal screen, and a source assertion that the cancel handler targets `cancelled` and never `idle` — the second because the first cannot execute the handler Originally recorded as **D194** on the unmerged follow-up branch and renumbered to **D196** when PR #278 merged first and took D194/D195 canonically for the active-deal cap findings; the repository is canonical and the already-merged row keeps its number (same rule as the D173/D174 collision, 2026-08-24 seventh entry). Nothing about the finding changed — only the identifier. | engineering |
| D197 | closed | code-outlier | 2026-08-27 | Shopper QR check-in / queue | **A check-in could answer `checkedIn: true, renewed: true` while leaving the shopper off the queue entirely.** `POST /api/qr/check-in` selected the live waiting row, then set `renewed = true` **before** issuing the UPDATE and never inspected what the UPDATE matched. If staff dismissed the entry (or the shopper cancelled in another tab) in the window between the two statements, `.eq("status","waiting")` matched zero rows — so the shopper was told they were queued and staff never saw them. Bounded: the arrival evidence was still recorded by the RPC, so Fast Visit eligibility was unaffected, and a re-scan self-healed; the cost was one shopper waiting to be called who never would be. Shipped in PR C (#277) and live on `main` | closed 2026-08-27 — the renew now terminates with `.select("id")` and sets `renewed` from the rows it actually matched, falling through to a fresh insert on a miss. Guard: the renew-race case in `maanta-app/src/app/api/qr/check-in/__tests__/route.test.ts`, whose service-client mock now models `select` as both a read-chain opener and an update terminator so a zero-row update is expressible at all Originally recorded as **D195** on the unmerged follow-up branch and renumbered to **D197** when PR #278 merged first and took D194/D195 canonically for the active-deal cap findings; the repository is canonical and the already-merged row keeps its number (same rule as the D173/D174 collision, 2026-08-24 seventh entry). Nothing about the finding changed — only the identifier. | engineering |
| D198 | closed | code-outlier | 2026-08-27 | Fast Visit / shopper ticket | **Turning the Fast Visit gate OFF erased an already-earned qualification from the shopper's ticket, while the award still paid.** `tickets/[id]/page.tsx` rendered the reward panel on `fastVisitOn` alone, so a shopper who arrived and qualified while the gate was ON — verdict persisted immutably as `redemptions.fast_visit_qualified_at` (D191) — lost the "You made it / Points pending" confirmation the moment the founder flipped `app_config.fast_visit_enabled`, even though `award_fast_visit_points` deliberately never re-reads the gate and would still pay at verification. The screen therefore contradicted both the persisted verdict and the money. The migration states the invariant outright ("earned eligibility is never erased") and `you/page.tsx` already implemented the carve-out; this screen did not. Shipped in PR C (#277), live on `main` and applied to production | closed 2026-08-27 — the panel now renders on `fastVisitOn` OR a non-null `ticket.fast_visit_qualified_at`. The reverse case was already correct (gate OFF at arrival, ON now → a null verdict short-circuits). Guard: the D198 case in `maanta-app/src/lib/__tests__/rewards-not-money.test.ts`, which also pins the sibling carve-out on `/you` Originally recorded as **D196** on the unmerged follow-up branch and renumbered to **D198** when PR #278 merged first and took D194/D195 canonically for the active-deal cap findings; the repository is canonical and the already-merged row keeps its number (same rule as the D173/D174 collision, 2026-08-24 seventh entry). Nothing about the finding changed — only the identifier. | engineering |
| D199 | closed | code-outlier | 2026-08-27 | Shopper QR check-in / queue | **A lapsed queue entry was revived carrying its ORIGINAL arrival time, letting a shopper jump a queue whose whole purpose is oldest-first.** The same `qr_token` is printed at the entrance and the till by design, so: scan at 10:00 → entry with `arrived_at` 10:00, `expires_at` 10:10; the entry lapses and correctly drops off the staff list; the shopper reaches the counter at 10:40 and scans again. The renew lookup matched on `redemption_id` + `status = 'waiting'` with **no expiry filter**, found the dead row, and updated only `{ expires_at }` — so the staff list re-showed her as "arrived 40m ago", sorted ahead of everyone who had checked in between. On a multi-day deal (claim window = deal end + 15 min) it read "arrived 2d ago" for someone standing at the counter. The 10-minute TTL exists precisely so that "row present" means "checked in within 10 minutes" | closed 2026-08-27 — the lookup deliberately still matches lapsed rows, because the partial UNIQUE index covers every `waiting` row and a "fresh insert" would collide with one; instead the write branches on age: a live row is extended (arrival time preserved — a re-scan by someone already queued is not a new arrival), a lapsed row is superseded in place with a NEW `arrived_at`, and only a live extension reports `renewed`. Guards: the lapsed-supersede and live-extend cases in `maanta-app/src/app/api/qr/check-in/__tests__/route.test.ts`, asserting on the actual update payload Originally recorded as **D197** on the unmerged follow-up branch and renumbered to **D199** when PR #278 merged first and took D194/D195 canonically for the active-deal cap findings; the repository is canonical and the already-merged row keeps its number (same rule as the D173/D174 collision, 2026-08-24 seventh entry). Nothing about the finding changed — only the identifier. | engineering |
| D200 | closed | code-outlier | 2026-08-27 | Fast Visit / shopper ticket | **The ticket's award self-heal swallowed every failure with no observability, so a backstop that had stopped working was indistinguishable from "this shopper did not qualify".** `tickets/[id]/page.tsx` called `award_fast_visit_points` and read `reward_events` with both results discarded, inside a `try/catch` that could never fire: supabase-js resolves PostgREST failures as `{ error }` rather than throwing. Permission drift on the service role, or any constraint change, would silently stop the crash-window repair — no log line, no metric, and the reward card simply absent. The verify route had already hit and documented this exact trap seventy lines away | closed 2026-08-27 — both calls now destructure and log their error codes. No behaviour change and no double-award risk (the UNIQUE reference still holds); this makes a silent failure visible. `no guard: an observability-only change — a test asserting that a log line is emitted would pin the logger, not the invariant` Originally recorded as **D198** on the unmerged follow-up branch and renumbered to **D200** when PR #278 merged first and took D194/D195 canonically for the active-deal cap findings; the repository is canonical and the already-merged row keeps its number (same rule as the D173/D174 collision, 2026-08-24 seventh entry). Nothing about the finding changed — only the identifier. | engineering |
| D201 | closed | code-outlier | 2026-08-27 | Shopper QR check-in | **A malformed redemption id answered HTTP 500 instead of 400.** `POST /api/qr/check-in` shape-checked `token` strictly (`^[0-9a-f]{32}$`) but accepted any non-empty string as `redemptionId`, so a value like `"abc"` reached the RPC and failed uuid coercion inside PostgREST. That message matches none of the route's mapped error branches, so it fell through to `console.error` and a generic 500 — meaning any signed-in shopper could emit server-error log noise at the 10/min rate-limit ceiling from what is simply bad input | closed 2026-08-27 — `redemptionId` is now UUID-shape-checked alongside the token, before the rate limit and before the RPC. Guard: the non-UUID case in `maanta-app/src/app/api/qr/check-in/__tests__/route.test.ts`, which also asserts the RPC is never reached Originally recorded as **D199** on the unmerged follow-up branch and renumbered to **D201** when PR #278 merged first and took D194/D195 canonically for the active-deal cap findings; the repository is canonical and the already-merged row keeps its number (same rule as the D173/D174 collision, 2026-08-24 seventh entry). Nothing about the finding changed — only the identifier. | engineering |
| D202 | closed | code-outlier | 2026-08-27 | Rewards / You | **A reward read FAILURE hid the Rewards row entirely — asserting "you have no rewards" from an error.** `getRewardBalance` returns `null` on a read failure specifically so callers can distinguish it from zero, but `/you` computed `showRewards` as `fastVisitOn` OR `(rewardBalance ?? 0) > 0`, collapsing null into 0. A shopper holding 150 points, with the gate since turned off and a transient Supabase error, lost the row — and with it the only link to `/you/rewards`, the one page that would have told them honestly that this was a read error rather than an empty balance. The same D164/D185 failure class the rest of this package guards against | closed 2026-08-27 — the row now shows when the balance is `null`, so the read-error state on `/you/rewards` is reachable. Guard: the D202 case in `maanta-app/src/lib/__tests__/rewards-not-money.test.ts` Originally recorded as **D200** on the unmerged follow-up branch and renumbered to **D202** when PR #278 merged first and took D194/D195 canonically for the active-deal cap findings; the repository is canonical and the already-merged row keeps its number (same rule as the D173/D174 collision, 2026-08-24 seventh entry). Nothing about the finding changed — only the identifier. | engineering |
| D203 | closed | code-outlier | 2026-08-27 | Fast Visit / shopper ticket | **The reward countdown was a second, weaker copy of the claim countdown's sub-hour logic — able to reproduce the exact raw-minute string D167 item 3 had just removed from the timer directly above it.** `formatRewardCountdown` had no hour rollover and no non-finite guard, and the panel computes its remaining time from the DEVICE clock. A phone running ~50 minutes slow (common on handsets with no NTP, and accepted as an input by the panel's own docblock) keeps the window open and renders "65:00" in `font-code` immediately beneath a claim countdown that was taught to roll over; an unparseable value rendered "NaN:NaN". Two formatters for one rule on one screen is exactly how the original defect returns | closed 2026-08-27 — `formatRewardCountdown` now delegates to `formatClaimCountdown`, so there is one implementation. Output under an hour is unchanged. Guard: the D203 case in `maanta-app/src/lib/__tests__/fast-visit-window.test.ts` Originally recorded as **D201** on the unmerged follow-up branch and renumbered to **D203** when PR #278 merged first and took D194/D195 canonically for the active-deal cap findings; the repository is canonical and the already-merged row keeps its number (same rule as the D173/D174 collision, 2026-08-24 seventh entry). Nothing about the finding changed — only the identifier. | engineering |
| D204 | closed | code-outlier | 2026-08-27 | Merchant till queue | **A just-redeemed shopper stayed listed and tappable for up to a full poll, and tapping that row showed staff a rejection screen for a customer they had served seconds earlier.** `QueuePanel` refreshes only on its own 8-second interval; `reset()` in the keypad calls `router.refresh()`, which re-renders the RSC tree but not a client component holding its own timer. So after a successful verification the served shopper remained in the list and in the "N waiting" count, and a second tap produced the full-screen dark "Code not valid — Invalid or already-used code" takeover. Charged nothing and self-corrected on the next poll, but it is a frightening screen generated by the HAPPY path, on every redemption at a queue-using till | closed 2026-08-27 — a reverse channel in `maanta-app/src/lib/queue-code-handoff.ts`: the keypad announces a completed verification and the panel reloads immediately. It carries no data — a refresh nudge only, with the server still the authority on queue membership. Guard: the D204 block in `maanta-app/src/lib/__tests__/queue-code-handoff.test.ts` (fan-out, unsubscribe, empty payload, no-listener no-op) Originally recorded as **D202** on the unmerged follow-up branch and renumbered to **D204** when PR #278 merged first and took D194/D195 canonically for the active-deal cap findings; the repository is canonical and the already-merged row keeps its number (same rule as the D173/D174 collision, 2026-08-24 seventh entry). Nothing about the finding changed — only the identifier. | engineering |
| D205 | closed | code-outlier | 2026-08-27 | Rewards / guards | **The not-money ratchet skipped the one reward surface that renders a points balance beside a money figure.** `rewards-not-money.test.ts` walked `you/rewards`, the Fast Visit panel and the two libs, but not `tickets/[id]/page.tsx` — which renders "Fast Visit reward earned / +N MAANTA Points · Balance: N" on the same screen as "KES {pay}". A future edit colouring that balance, adding `text-brand`, or writing a KES equivalent onto the card would have passed CI green. The file cannot simply join the surface list, because it legitimately contains "KES" | closed 2026-08-27 — a scoped assertion over the reward card's own source region rather than the whole file. Guard: the D205 case in `maanta-app/src/lib/__tests__/rewards-not-money.test.ts` Originally recorded as **D203** on the unmerged follow-up branch and renumbered to **D205** when PR #278 merged first and took D194/D195 canonically for the active-deal cap findings; the repository is canonical and the already-merged row keeps its number (same rule as the D173/D174 collision, 2026-08-24 seventh entry). Nothing about the finding changed — only the identifier. | engineering |
| D206 | closed | code-outlier | 2026-08-27 | Active-deal cap (`enforce_deal_limit()`) | **The cap was enforced on INSERT only — an UPDATE setting `is_active = TRUE` bypassed it entirely.** `enforce_deal_limit_trigger` was `BEFORE INSERT` with no UPDATE-path equivalent, so reactivating an archived or ended deal never met the rule. Measured on production 2026-08-27: **28 merchants above their plan's cap**, up from 0 hours earlier with no new deals inserted in between. Attribution: **27 Elite** merchants at 3 active — 2 seed-batch rows plus 1 `autoreseed` flash row, where `reseed_demo_flash_deals()` legitimately inserted while under cap (it deactivates expired demo deals first to free allowance) and the nightly `refresh_demo_seed_deals()` then blindly reactivated both batch rows on top; and **1 Standard** merchant at 2 active from the same blanket reactivation. **All 28 are demo merchants; no genuine merchant was ever over cap** (2 non-demo active deals, 0 over cap), so the commercial rule was never breached in the field — but the same gap would apply to any future merchant-facing "reactivate a paused deal" action, and it left the founder ruling of 2026-08-26 (*no paused deal may be reactivated over the cap*) with no database enforcement behind it | Fix authored 2026-08-27 on branch `claude/d206-cap-update-guard`, **not yet applied to production**: migration `maanta-app/supabase/migrations/20260827120000_cap_enforce_on_slot_entry.sql` makes the trigger `BEFORE INSERT OR UPDATE` but fires only on ENTRY INTO OCCUPANCY (`OLD.is_active = FALSE AND NEW.is_active = TRUE`), so a merchant at cap can still edit, pause, extend or archive the deal that owns its slot; flash stays Elite-only including on reactivation; no `is_demo` exemption; INSERT behaviour unchanged. `refresh_demo_seed_deals()` is rewritten cap-compliant BY CONSTRUCTION — it subtracts slots held by rows it does not manage, fills the remaining allowance in a stable `created_at, id` order, skips flash rows for Standard merchants and retires the surplus, so the next scheduled run repairs the existing over-cap state without anyone editing production by hand. Guards: `maanta-app/supabase/tests/deal_limit_cap_test.sql` scenarios H–L and `maanta-app/supabase/tests/demo_seed_refresh_cap_test.sql` A–E, verified by four mutants (naive `INSERT OR UPDATE` freezing merchants out, revert to INSERT-only, blanket demo reactivation, and an unqualified scratch-table reference) each failing the suites. The fourth came out of the substitute security review run when both automated review gates were unavailable on PR #284: the rewritten `refresh_demo_seed_deals()` uses a temp table, and because the function pins `search_path = public, pg_temp` an explicitly-listed `pg_temp` is searched LAST — so an unqualified `_refresh_keep` resolves to `public._refresh_keep` first. Demonstrated, not theorised: the unqualified draft DROPPED an unrelated `public._refresh_keep` table from inside a SECURITY DEFINER body. Every reference is now `pg_temp.`-qualified and scenario E holds it there. **A second review finding, from Codex on the same PR and confirmed by reproduction:** the documented seed rerun path (`AGENTS.md` — apply `maanta-app/supabase/seed/node0_100_deals_seed.sql`) ends in a blanket `UPDATE ... SET is_active = true` over its whole id range, which under the new guard **aborts the entire seed transaction** as soon as one of those merchants has a slot held by a row outside the range — an `autoreseed` flash deal is exactly that. Measured: `Deal limit reached. elite plan allows 2 active deal(s).` Both that seed and `maanta-app/supabase/seed/node0_rehearsal_seed.sql` now use the same allowance-based selection as `refresh_demo_seed_deals()` — already-active rows refreshed unconditionally, inactive rows activated only within the slots left, oldest id first — verified before and after on the same probe state (blanket form aborts, fixed form completes and lands at exactly cap). **APPLIED to production 2026-08-27** under founder authorization, after PR #284 merged as `5aaf522` (tree-verified against the audited head `87ce088`). The apply minted `20260827074843` — eleven for eleven — repaired to the repo filename before anything else; **ledger 105/105**, high-water `20260827120000`. Read back: trigger fires `INSERT UPDATE`, transition check live, refresh `pg_temp.`-qualified and cap-aware, exactly one overload each, EXECUTE still `postgres`/`service_role` only, cron `30 2 * * *` active. A live negative test on production, written to roll itself back, confirmed all three behaviours on real data: `reactivate_over_cap = [Deal limit reached. elite plan allows 2 active deal(s).]`, `edit_at_cap = [allowed]`, `leave_occupancy = [allowed]`; row counts identical before and after (2408 deals / 331 active / 215 merchants / 404 redemptions / 0 tier_flags). **Census still reads 28 over cap and that is correct** — the migration changes behaviour, not rows, per the ruling not to clean production rows by hand. **Still not closed**: closure is the 02:30 UTC scheduled `maanta_demo_seed_refresh` on 2026-08-28 succeeding, taking over-cap 28 to 0 and not recreating it, with total active deals landing at **303** — 261 batch rows kept of 289 active (28 retired, exactly the 28 over-cap merchants) plus 40 `autoreseed` plus 2 genuine, measured on production. An earlier draft of the ops doc set that target at 288; Codex caught it on PR #285 and it was wrong twice over — it counted only the batch and assumed a single dropped row, ignoring the 40 slots `autoreseed` already holds. A 288 target would have accepted the silent loss of ~15 deals or made a correct run look wrong. **CLOSED 2026-08-28 by the scheduled run, measured read-only.** `cron.job_run_details` runid 769: **succeeded**, 02:30:00.095767+00 -> 02:30:00.495429+00, `1 row` — the first scheduled run under the new trigger did NOT abort, which was the whole risk of the rewrite. Census after it: **over_cap_total 0** (genuine 0, demo 0), Standard max **1**, Elite max **2**. The run set 258 rows active and 29 inactive; all 29 retirements were Elite merchants at 3 active losing exactly 1 to land at 2 — **no merchant was cut below its cap** and no Standard merchant lost a deal. The over-cap state was repaired by the function itself, with no hand-editing of production rows. **Supply landed at 300 active, not the 303 this row predicted, and the prediction was the thing that was wrong.** 303 assumed one retirement per over-cap merchant (289 - 28 = 261 batch); the function instead allocates per merchant — cap minus foreign-held slots, filled in stable `created_at, id` order. Recomputing that rule independently from the merchant/deal tables predicts **258** batch rows and matches observed state for **208 of 208** batch merchants, zero deviations. Reconciliation of the 289 batch rows: 258 kept + 29 retired + 2 already inactive. Those 2 are expired flash rows deactivated by `reseed_demo_flash_deals()`, whose retire step never sets `updated_at` (confirmed via `pg_get_functiondef`) — pre-existing, unrelated to D206, and the reason the pre-run batch figure was 287 not 289. Neither was eligible to return: one is a Standard merchant's flash row (D207's row, refused by the Elite-only rule), the other sits behind a fuller allowance. Not starved: 272 shopper-visible in `deals_public_browse`, dark fixture shops still **0**, and the 13 Elite merchants at 1/2 own only one batch deal each (0 promotable rows) — supply, not the guard. Ledger **105/105**, high-water `20260827120000`, full version+name read-back md5-identical to the repo directory (`608e0dabc573bf0f0226ae83ebf50ec1`). Record: `docs/ops/d206-cap-slot-entry-2026-08-27.md` §12 | guard: migration `20260827120000_cap_enforce_on_slot_entry.sql` + `supabase/tests/deal_limit_cap_test.sql` H–L + `supabase/tests/demo_seed_refresh_cap_test.sql` A–E |
| D207 | open | code-outlier | 2026-08-27 | `maanta-app/supabase/seed/node0_ops_personas_seed.sql` | **A documented seed file cannot run — and this predates D206.** The file inserts a **flash** deal (`d0000000-…005`, 'Flash: 20% off lip gloss sets') for **Bilan Beauty & Cosmetics** (`c0000000-…002`), which `maanta-app/supabase/seed/node0_rehearsal_seed.sql` creates on the **standard** tier. Flash is Elite-only and always has been, enforced on the INSERT path of `enforce_deal_limit()` — a path this PR leaves byte-identical. Reproduced on a fresh chain: inserting that exact row shape raises `Flash deals are only available on the Elite plan.` and rolls the seed back. Nothing about D206 caused it; it was found while verifying Codex's seed-rerun finding on PR #284. The file's own blanket `is_active = true` refresh carries the same cap hazard the other two seeds were just fixed for, but the file aborts earlier than that line, so fixing only the refresh would not make it runnable | **Not fixed here — deliberately out of scope.** The fix is a product-data decision, not a mechanical one: either Bilan becomes Elite (which changes what the rehearsal personas demonstrate — she is the 'recently onboarded, still learning' standard merchant) or the deal stops being flash (which removes the flash rail from the persona set). Founder picks. Evidence: `maanta-app/supabase/seed/node0_ops_personas_seed.sql`, `maanta-app/supabase/tests/deal_limit_cap_test.sql` scenario D pins the Elite-only rule that refuses it | founder |
| D208 | closed | code-outlier | 2026-08-27 | `maanta-app/src/app/admin/page.tsx` success-fee sum | **`/admin` sums success fees the way D149 says not to.** It pulls `merchant_transactions` rows into JS and `.reduce()`s them, while `/admin/reports` and `/founder` deliberately call the `admin_success_fee_revenue` RPC precisely because PostgREST's 1000-row cap silently truncates the row set and under-reports the SUM. Same money figure, two methods, and the app-layer one fails quietly and low — the worst direction for a revenue number. Harmless today at 3 fee rows in all of production, which is exactly why it has survived; it becomes wrong without warning somewhere past 1000 fee rows. Found while building PR 5, which needs per-merchant fee sums the global RPC cannot provide | **Not fixed in PR 5 — out of its locked scope**, and the fix is a judgement call rather than a mechanism: either a node/merchant-scoped RPC (a migration, so founder-authorized) or a bounded read that reports UNAVAILABLE at the cap. PR 5 took the second option for its own per-merchant column (`FEE_ROW_CAP`, renders a dash rather than a low number) and left `/admin` alone. Evidence: `maanta-app/src/app/admin/page.tsx`, `maanta-app/src/app/admin/reports/page.tsx`, `maanta-app/src/app/founder/page.tsx`  Guard: `maanta-app/src/lib/__tests__/ledger-fee-semantics.test.ts`. **CLOSED 2026-08-30 by B2b:** the bounded JavaScript sum is gone; global views call `admin_fee_totals_global` and node views call `admin_fee_totals_for_merchants`. | founder |
| D209 | closed | code-outlier | 2026-08-27 | `maanta-app/src/lib/ui.ts` `friendlyTime()` | **D190 was fixed on one screen; the same defect is still live on every merchant, admin and agent surface.** `friendlyTime()` formats with `toLocaleTimeString("en-KE")` and compares days with `toDateString()`, both in the **server's** timezone — UTC on Vercel, three hours behind Nairobi. So "Today, 10:42am" renders as "Today, 7:42am", and between 21:00 and 24:00 EAT the day word is wrong outright: a redemption at 22:30 Tuesday says "Today" to a server that still thinks it is Tuesday 19:30, then flips to "Yesterday" three hours early. Call sites: `/merchant/wallet` and `/merchant/wallet/[id]` (money rows a merchant reconciles), `/merchant/redemptions` and `/merchant/dashboard` via `RedemptionRow`, `/admin/redemptions`, `/admin/redemptions/[id]`, `/admin/customers`, `/admin/customers/[id]`, `/admin/agents/[id]`, `/agent/leads/[id]`. **No shopper surface is affected** — verified by sweep, see the closing note on D211 | **Not fixed in PR 1 — out of its locked scope**, which is shopper surfaces only, and `/admin` is frozen under the D208 ruling. Merchant surfaces are PR 3 territory. The fix is mechanical once authorized: route `friendlyTime` through `NAIROBI_TZ` in `maanta-app/src/lib/claim-ticket-time.ts` rather than forking a second formatter. Raised now because a merchant reading a wrong wall-clock on their own money rows is a Merchant 01 problem, not a cosmetic one. Evidence: `maanta-app/src/lib/ui.ts`, `maanta-app/src/lib/claim-ticket-time.ts` **Renumbered from D208 to D209 on 2026-08-28** — the fifth drift-ID collision, resolved by the established rule: PR #286 merged first as `4dff726` and holds **D208** canonically for the `/admin` fee-aggregation finding, so this row, opened the same day on the unmerged shopper-clarity branch, takes the next contiguous id. Nothing about the finding changed — only the identifier. → **Closed 2026-08-30:** `friendlyTime` formats and compares calendar days in `Africa/Nairobi`, including the UTC/EAT midnight boundary. Guard: `maanta-app/src/lib/__tests__/friendly-time.test.ts`. | founder |
| D210 | open | doc-stale | 2026-08-27 | `maanta-app/design/current-reality/frames.json` | **Three shipped surfaces are missing from the canonical surface inventory.** CLAUDE.md names `frames.json` the design-side source of truth that classifies each surface as live/gated/rehearsal/design-ahead/blocked/superseded. It holds **32** surfaces and was last amended 2026-08-19. Since then three routes shipped to `main` and none was added: `(shopper)/qr/[token]` (the arrival check-in), `(shopper)/you/rewards` (the Points surface, currently dark) and `merchant/(app)/qr/print` (the printable counter QR). A design reader consulting the inventory would conclude those surfaces do not exist — the same class of failure as D94, where the inventory omitted the installed product's entry point, and D103/D104, where entries described a guarantee instead of a rendered surface. The existing guard, `frames-inventory.test.ts`, is a plain existence check on cited `frontend` paths (narrowed deliberately in D140) and so cannot detect an omission | **Not fixed in PR 1 — it is design-side inventory, not shopper clarity**, and classifying `/you/rewards` requires the founder's word on whether a dark surface is `gated` or `design-ahead`. Recorded rather than guessed. Worth pairing with a coverage guard so an omission fails CI instead of waiting for the next audit. Evidence: `maanta-app/design/current-reality/frames.json`, `maanta-app/src/lib/__tests__/frames-inventory.test.ts` **Renumbered from D209 to D210 on 2026-08-28**, carried by the same collision: this row keeps its position immediately after the `friendlyTime` row it was opened alongside. Nothing about the finding changed — only the identifier. | founder |
| D211 | closed | product-decision | 2026-08-28 | Fee KPI reversal semantics | **The success-fee KPI measures GROSS linked fee entries, and nothing on the surface says so.** PR 5 moved both fee figures off `redemptions.success_fee_charged` and onto the ledger, joined by `reference_id` over `success_fee` and `success_fee_arrears` — correct, because `verify_redemption` commits a successful redemption even when the fee step throws, leaving a fee amount on the row that never reached the ledger. What that fix did NOT decide is how a reversal should read. `reverse_success_fee` writes a positive `fee_reversal` row and, when arrears were standing, a negative `arrears_settlement` row, both carrying the redemption's `reference_id`. Neither type is in the billed set, so a reversal cannot double-count today (verified) — but a reversed fee still counts in full toward a KPI a reader will take as revenue. Zero reversals exist in production, so there is no live misstatement; the semantics are simply undecided | **Founder ruling 2026-08-28: for PR 5, "Success fees" means gross fee ledger entries actually created for genuine verified redemptions. It is not net revenue, and a successful redemption with no linked fee entry stays UNAVAILABLE rather than counted.** Open question deferred deliberately: whether founder/admin reporting should later present **gross fees, reversals and net fees separately** rather than silently subtracting rows behind a single number — which keeps an audit trail and avoids turning an operational KPI into accounting logic by accident. **Do not assume `fee_reversal` / `arrears_settlement` signs or subtraction semantics without verifying the ledger contract**; the two rows have opposite signs against one redemption. Evidence: `maanta-app/src/lib/evidence-scope.ts` `sumLedgerSuccessFees`, `reverse_success_fee` read back from production 2026-08-28, PR #286 **PARTIALLY ADDRESSED by PR B, 2026-08-29 — and DELIBERATELY LEFT OPEN.** Every surface that read `sumLedgerSuccessFees` now reports **gross, reversals and net**, with net as the headline: `/admin/pilot` (ladder card, cohort table, per-merchant table) and `/founder/yesterday` (all-class and external cards). The ledger contract moved into a single table in `evidence-scope.ts`, one entry per `merchant_transactions` type, each sign **read back from the live RPC bodies on production**: `success_fee` `-p_amount`, `success_fee_arrears` `p_amount` (**positive**), `fee_reversal` `v_fee_amount`, `arrears_settlement` `-v_settled` (excluded — it moves an amount a `success_fee_arrears` row already counted as billed, so gross would double the fee and reversals would subtract one nobody reversed). Classification left the callers entirely: all three sites filtered `.in("transaction_type", FEE_LEDGER_TYPES)` — a correctness rule living in three callers, none of which had an opinion about a reversal — and one reader, `readLedgerFeeTotals`, now owns both the D188 parent chain and the contract. `Math.abs` is gone as the generic rule in TypeScript: it was a guess ("whatever sign this row carries, treat it as billed"), exactly wrong for a reversal, and it absorbed rows the money path could not have written; a row contradicting its type's declared sign now makes that figure unknown. The window follows each movement's own `created_at`, so a reversal posted inside it against an older redemption counts. **What is NOT done, and why the row stays open.** Two executive money surfaces answer this question through a different mechanism and were untouched: `/founder` renders *"Fee revenue (7d)"* (`src/app/founder/page.tsx:160`) and `/admin/reports` renders *"Success-fee revenue"* (`src/app/admin/reports/page.tsx:125`), both from `admin_success_fee_revenue`. That RPC — verified against production 2026-08-29 — is `SUM(ABS(amount)) WHERE transaction_type = 'success_fee'`: it **omits arrears entirely**, **counts a reversed charge as revenue in full**, carries the same `ABS` magnitude guess in SQL, and applies **no D188 genuine-tagged scope at all**. Found by review of PR B, verified in source and on production before recording. Fixing it is a **migration**, which PR B's authorised scope excluded, and those surfaces do not read `sumLedgerSuccessFees`, so they were outside its stated scope twice over. As with the original finding there is **no live misstatement today**: production holds three `success_fee` rows and no arrears, reversal or settlement row at all. Evidence: `LEDGER_TYPE_CONTRACT`, `aggregateLedgerFees` and `readLedgerFeeTotals` in `maanta-app/src/lib/evidence-scope.ts`; `FEE_FIGURE_LABELS` in `maanta-app/src/components/admin/fee-figures.tsx`; guard `maanta-app/src/lib/__tests__/ledger-fee-semantics.test.ts` — the six required proofs plus a PostgREST fake that evaluates its filters, **18 mutants proven to fail**; `admin_success_fee_revenue` in `20260726200000_architecture_now_fixes.sql`; PR B. **Next step, founder-owned:** rule whether `admin_success_fee_revenue` should be brought into the gross/reversals/net model — a migration plus relabelling two surfaces — or whether those two cards should be narrowed to say what they actually count. Close D211 only then. **Founder ruled 2026-08-29: bring it into the same model, in two phases.** **PR B2a (database, this PR) is written and not yet applied**: `_fee_totals(p_since, p_until, p_scoped, p_merchant_ids)` holds the whole contract, with two explicit wrappers — `admin_fee_totals_global` and `admin_fee_totals_for_merchants` — because a single nullable array where NULL means global is one forgotten argument away from a node-scoped operator reading marketplace-wide money. Availability is **all-or-nothing**: any missing fee row or invalid polarity makes gross, reversals and net NULL together while the diagnostic counts stay populated, and zero legitimate activity returns available zeros — *nothing happened* and *we could not tell* are different answers. Three window questions are kept apart in comments and in tests because conflating any two produces a wrong figure: totals follow the movement's `created_at`; the completeness candidate set follows `redeemed_at`; the search for a candidate's fee row spans **all dates**. Rules that emerged from review and are pinned by fixture cases: every row must agree with the redemption it hangs from (merchant, deal owner, amount snapshot, ordering against verification); a reversal must be corroborated by its own `fee_reversals` audit row; a fee movement with **no redemption parent at all** is counted rather than discarded by the join; an **unplaceable timestamp** (`infinity`) belongs to no window so every window surfaces it; **scope matches any merchant a chain names** — the redemption's, the movement's and the deal owner's — on both the classification and the completeness paths, so no owner reads an available zero over corruption on a chain they are part of. **Founder ruling 2026-08-29: a flagged success still counts** — `status = 'success'` is the authoritative financial event, `fraud_flags` and `review_required` do not independently remove it from earned-fee totals, and an invalidating adjudication corrects it through an explicit `fee_reversal`. Two partial indexes: `idx_mtx_reference_id` on `reference_id` for the parent probes, and `idx_mtx_fee_window` on `created_at` limited to the three fee types the global window reads. Both contracts are proved by one shared semantic fixture (48 cases) with a generator drift check, rather than by a source-text parser. **B2b still owes**: switching `/founder`, `/admin/reports` and `/admin` to the structured RPCs, per-card unavailable rendering, and moving the TypeScript reader to all-or-nothing — every `tsDivergence` and `notYetInTypeScript` entry names the change that deletes it. **CLOSED 2026-08-30 by B2b:** all five fee surfaces delegate to the structured wrappers, every card renders net, gross and reversals separately, and an unavailable contract row maps all three to an em dash. The TypeScript reader no longer reimplements the relational contract; it validates and maps the SQL result, eliminating every recorded parity gap. | founder |
| D212 | open | process | 2026-08-28 | Durable operational records under `docs/ops/` and `docs/maanta-drift-register.md` | **A durable operational record becomes FALSE at the moment the commit that fixes a defect lands, and nothing detects it.** This is not ordinary staleness: the record is most wrong precisely when the codebase is most right, which is the opposite of what a reader assumes. Demonstrated four times in one review round on PR #288 — the register announced a branch "rebased onto `main`" when the commit was a two-parent merge; a correction then said "the branch head is a two-parent merge", true for exactly one commit because the head moves; `docs/ops/pr1-shopper-clarity-2026-08-27.md` §12 recorded `missed` for an unverdicted Fast Visit arrival minutes after `e11a2de` changed it to `hidden`; and §4 recorded one unconditional wayfinding string after `27478d7` had made the copy conditional on the shop record. Each described the defect as intended behaviour, which is what a later session reads before changing that code. **Prose-scanning guards are not an acceptable solution and are ruled out** — three were written during PR #288 and all three failed: one read the wrong file (D38 vacuity), one treated a missing file as success so a rename was a permanent blind spot, and the third encoded a blacklist of the exact sentences already observed, which trivial variants pass | **Not fixed — the guard was REMOVED from PR #288 by founder ruling 2026-08-28** rather than rewritten a fourth time inside a release gate; `drift-register.test.ts` is back to its pre-PR state. The remedy is structural: encode what a record ASSERTS in a machine-checkable form that can be validated against git and against the code it describes — a merge commit SHA and both parents, the ids renumbered and from what — rather than matching sentences. A phrase list cannot express the invariant, which is that the record agrees with reality. **Scope: this row is about durable-record validation only.** It is NOT a bucket for brittle guards in general — the two mechanism-pinned shopper guards removed on 2026-08-28 were deleted outright and left no surviving gap. Design deliberately and outside a merge gate. Evidence: PR #288 threads on `38a07e6`, `27fc213`, `be84573`, `c52133e`; `docs/ops/pr1-shopper-clarity-2026-08-27.md` sections 15, 16.2, 16.4 and 16.5 | founder |
| D213 | closed | code-outlier | 2026-08-28 | Shopper time-decay: `/feed` "Ending soon", `/my-deals` Fast Visit chip, horizontal `DealCard` | **Server-rendered time-sensitive claims go stale on a page left open, and two of them assert something false rather than merely out of date.** `/my-deals` is a server component with no tick: `fastVisitChipState` is evaluated once at render, so a row keeps asserting "Fast Visit open" after `claimed_at + 15 minutes` has passed. `/feed` computes `endingSoonDeals(allDeals)` once during its server render, so a deal that expires while the feed is open remains under a heading that says "Ending soon". The horizontal `DealCard` variant compounds it by rendering the server-computed static `expiryLabel`, while the tall variants already use the ticking `CountdownChip` (30s interval) — an inconsistency that PREDATES PR #288 and is not a regression of it. Both surfaces were verified live during review; the ticking machinery already exists, so this is a design decision about these surfaces rather than a missing capability | **Deferred by founder ruling 2026-08-28 — a controlled deferral, not a dismissal.** Closing it means introducing client reactivity across `/feed` and `/my-deals`, a cross-surface change deliberately kept out of PR #288 after that PR was narrowed to shopper clarity. **Acceptance criteria, set by the founder so the deferral cannot soften. Criteria 1 and 2 are unchanged from 2026-08-28; criterion 3 was WIDENED and criterion 4 ADDED the same day, after review evidence showed the original three could all pass with the class still live: (1)** the Fast Visit chip closes when its deadline passes on an open page, without navigation or refresh; **(2)** expired deals leave the "Ending soon" collection without navigation or refresh; **(3)** every time-derived label, section membership and claimability state reflects the current time on an open page, and no two rendered elements contradict one another; **(4)** inventory exhaustion is reflected: when the final available claim is taken, an already-open shopper surface stops advertising the deal as available **within one defined refresh interval — 30 seconds unless implementation establishes a stricter bound.** Criterion 3 was widened TWICE on 2026-08-28 and the second widening matters as much as the first. It began as "horizontal expiry labels"; the first widening extended it to every shopper-rendered variant but demanded only that elements not contradict **one another**, which is internal consistency, not truth — an implementation could strip the ticking chip and leave a server-rendered expiry label and claim CTA stale **together**, perfectly self-consistent and uniformly wrong, while criteria 1, 2 and 4 (scoped to Fast Visit, "Ending soon" and inventory exhaustion) said nothing about it. The final wording therefore requires **both**: external accuracy against the current time AND internal consistency. Accuracy alone would miss two elements each defensible in isolation that disagree; consistency alone was the hole. The first widening was prompted because the **vertical** `DealCard` renders a ticking `CountdownChip` AND the static `expiryLabel` on one card (`deal-card.tsx` 247 and 257), and `/browse` passes both (`src/components/browse/browse-client.tsx:229`, `variant="vertical"`) — so the narrow wording could be satisfied in full while a live card contradicted itself. Criterion 4 exists because the `c52133e` claim-cap filter runs once per server render: a deal capped by another shopper while the page is open kept being advertised, and criteria 1–3 said nothing about it. Both criteria cover the defect wherever it lives, not only where PR #288 surfaced it. Note the claim-cap exclusion added in `c52133e` is a SEPARATE concern and does not address any of the four criteria — a capped deal is now excluded at render time, but membership still does not decay while the page is open. **Both cases below were verified on 2026-08-28 and were NOT covered by the original three criteria. The founder ruled the same day that they must be: (a) is now criterion 3 as widened, (b) is now criterion 4.** (a) The **vertical** `DealCard` renders a ticking `CountdownChip` AND the static `expiryLabel` on the same card (`deal-card.tsx` lines 247 and 257), and `/browse` passes both (`src/components/browse/browse-client.tsx:229`, `variant="vertical"`) — so one card carries two claims about one deal and only one of them decays. Criterion 3 **as originally written** named horizontal labels only, so it could have been satisfied while `/browse` stayed stale — which is why it was widened the same day. (b) A deal that reaches its cap **while a page is already open** is not removed: `claims_count` and `endingSoon` membership are computed once per server render, so the card remains under "Claim windows closing within the hour" though `claim_deal` now raises `deal_claim_limit_reached`. The `c52133e` filter only excludes deals already capped at render time. Under the original three criteria, all of them could pass with (a) and (b) still live — which is why they were widened rather than left frozen. **Criterion 3 clarified by founder ruling 2026-08-29, while PR A was in review — a clarification of the existing criterion, not a fifth one.** Two things it covers that an implementation had read narrowly: **(i) Active/Past tab membership on `/my-deals` is section membership.** Making the ROW time-derived while the page still partitioned the rows during its server render moved the contradiction one containment level out — an expired ticket flipped its own chip to EXPIRED while the tab holding it still called it active, and a Past tab opened before the boundary never admitted it. A collection is a rendered element. **(ii) The server render and the FIRST client render must agree structurally.** Deriving state from a clock makes subtrees clock-CONDITIONAL — a claim flow swapped for an ended CTA, a countdown removed, a whole section added or dropped — and React cannot patch a structural mismatch: it discards the server tree and rebuilds the branch, which on a money surface means the claim flow torn down under a shopper's finger. `suppressHydrationWarning` covers text, not structure, so it is not a remedy here; the requirement is one server-generated instant, seeded at the shopper layout boundary, that both passes read, advancing only after hydration. This obligation did not exist before the clock-derived rendering created it. Evidence: PR #288 threads on `c0130a1`; `maanta-app/src/lib/fast-visit-chip.ts`, `maanta-app/src/lib/ending-soon.ts`, `maanta-app/src/components/ui/claude/deal-card.tsx`, `maanta-app/src/components/ui/chips.tsx` → **Closed 2026-08-30:** criteria 1–3 remain on the shared server-seeded shopper clock; criterion 4 now refreshes every inventory-advertising shopper route within 30 seconds and on resume, while excluding ticket/QR/account routes so mutation-capable flows are not replayed. Guards: `maanta-app/src/lib/__tests__/shopper-time-decay.test.ts` and `maanta-app/src/lib/__tests__/shopper-inventory-refresh.test.ts`. | founder |
| D214 | closed | code-outlier | 2026-08-28 | `maanta-app/src/app/(shopper)/shops/[id]/page.tsx` | **The shop storefront listed PAUSED deals under a heading that says "Live deals".** The frozen paused-deal rule (**D25**) is that pausing removes a deal from shopper discovery immediately, and CLAUDE.md stated `/search` was the only surface that had ever missed it (**D119**, closed 2026-08-19). That was wrong: `shops/[id]/page.tsx` builds its own `deals` select rather than reading `getLiveDeals`, filtering `is_active`, `expires_at > now` and `is_demo` but **not** `is_paused` — the identical omission, in the identical shape, on a second surface. A merchant who paused a deal still had it advertised on their own storefront under "Live deals". **Following the card did NOT normally produce a 409** — `/deals/[id]` reads `is_paused`, sets `claimable` false and renders the disabled "Deal paused by merchant" state instead of mounting `ClaimFlow`; the `deal_paused` 409 is the stale/deep-link path CLAUDE.md already describes. The defect is the false discovery claim, not a broken claim attempt. **A third surface had the same omission**: `(shopper)/notifications/page.tsx` selected `deals` on `is_active` + `created_at` only, so a merchant who posted and then paused within 24h kept being advertised as "New deal from a saved shop". Found while narrowing the discoverable-is-not-claimable doctrine, whose claim that paused deals are "withdrawn from discovery entirely" was false while these stood | **Fixed in the same change**: the query now carries `.eq("is_paused", false)` beside the existing liveness filters, with the D25/D119 reasoning inline. Guard `maanta-app/src/lib/__tests__/shop-page-paused-filter.test.ts` is behavioural — it executes the page and records the filters actually applied to `deals`, because a source grep would pass on a commented-out predicate and fail on an equivalent rewrite. Two mutants proven to fail: removing the predicate restores the defect, and swapping `is_active` for it trades one discovery leak for another. **The general lesson is D119's, not this row's: a shopper surface that builds its own deal query instead of reading `getLiveDeals` will miss a predicate the shared path carries.** Three surfaces have now done it, so this was audited as a class rather than patched instance by instance: every direct `from("deals")` select in the app was checked. The shopper-discovery set is `getLiveDeals`, `/search` (D119), `shops/[id]` and `/notifications` — all four now carry `is_paused`. The merchant, admin and founder surfaces are deliberately OUTSIDE the rule and were left alone; `merchant/(app)/redeem` filters `is_paused = true` on purpose, because a merchant must see their own paused deals. **Two further omissions on `/notifications` were found by the same audit and are NOT fixed here.** Founder ruling 2026-08-28: they are split into their own separately traceable pre-launch rows rather than folded into this paused-deal repair, because they are different rules with different failure modes — **D215** (no merchant-visibility gate) and **D216** (launch mode does not exclude demo deals) — both since closed, implemented together on 2026-08-28. Evidence: `maanta-app/src/lib/__tests__/shop-page-paused-filter.test.ts`, `maanta-app/src/lib/__tests__/notifications-paused-filter.test.ts`, `maanta-app/src/lib/__tests__/search-paused-filter.test.ts` | claude |
| D215 | closed | code-outlier | 2026-08-28 | `maanta-app/src/app/(shopper)/notifications/page.tsx` | **The notifications inbox applies no merchant-visibility gate, so a suspended or shadow-banned merchant can still reach shoppers.** Every other shopper surface reads merchant availability through `withPublicMerchant` / `withPublicMerchantRows`, which pin `status = 'active'`, `is_visible = true` and `is_shadow_banned = false` — the same three conditions `claim_deal` enforces as `merchant_not_available`, and the read-side contract D123/D147 hardened. This page selects `deals` joined to `merchants(merchant_name)` filtered only on `merchant_id IN (favourites)`, `is_active` and a 24-hour `created_at` bound, so a merchant who is suspended or shadow-banned **after** a shopper favourited them keeps generating "New deal from a saved shop" entries. Shadow-ban is the sharper case: its whole purpose is that the merchant does not know they are hidden, and an inbox notification tells them otherwise the moment a shopper mentions it | **FIXED 2026-08-28, together with D216 as required.** The saved-shop read now goes through `withPublicMerchant` with `includeDemo` threaded from `isDemoModeEnabled()`, so the canonical policy is applied by the shared helper rather than re-hand-written. The guard derives its assertions from `PUBLIC_MERCHANT_CONDITIONS` itself, so adding a fourth condition to the policy fails this surface until it carries it. **The join was changed to `merchants!inner`** — on a left join PostgREST nulls the embed instead of dropping the row, so every `merchants.*` predicate would have passed the test and gated nothing. Originally split out by founder ruling 2026-08-28 so it is separately traceable rather than folded into the paused-deal repair, which is a different rule. Found during the D214 audit of every direct `from("deals")` select. **Pre-launch**: Merchant 01's onboarding at Node 0 involves real suspension and approval states, so a notification escaping the visibility gate would reach a real shopper. The fix is to route this read through the shared helper rather than to re-hand-write the three conditions — a second place to enforce the rule is a second place to drift. **But it must thread `isDemoModeEnabled()` into the helper, and D215 and D216 must be implemented together.** `withPublicMerchant`'s default path adds `is_demo = false` AND `merchants.is_demo = false` when `opts.includeDemo` is falsy (`data.ts` 299-304), so adopting the helper naively would suppress these notifications **even with demo mode ON** — over-correcting past D216, which requires them to keep working in that mode. Closing D215 alone, exactly as a careless reading of this row would prescribe, would therefore introduce a new defect rather than only fixing one. **Scope deliberately bounded, and the bound is REDEEMABILITY rather than visibility** — a distinction reached only after a first attempt got it wrong. The two `redemptions` reads carry the demo exclusion but not the discovery policy. First reasoning: `verify_redemption` read back from production has no merchant status or shadow-ban check, so the ticket still verifies. **That was true and incomplete** — `requireMerchant("can_verify")` returns 403 for `suspended` / `rejected` / `churned` BEFORE the RPC runs (`maanta-app/src/lib/merchant-api.ts`), so the application path is the real gate and those tickets cannot be redeemed through the product at all. The expiry notice therefore excludes exactly the verification-blocking statuses, and **keeps** the notice for a merely hidden or shadow-banned merchant, who can still verify. Using the discovery policy here would have been wrong twice over: it would strip the deadline from a redeemable ticket, and `status = 'active'` would also exclude a `pending` merchant, who is not blocked. The list lives in `VERIFICATION_BLOCKING_MERCHANT_STATUSES` and is consumed by both `requireMerchant` and this surface, so the two cannot drift; a mutant adding a status to the route without carrying it here fails. Eight mutants proven to fail, covering both directions of every rule: gate removed, helper adopted without `includeDemo`, left join instead of `!inner`, demo filtered on the row but not the parent, **over-correction** by gating owned redemptions on the discovery policy, the redeemability gate removed, the discovery policy substituted for it, and the blocked-status list drifting from the route's. Evidence: `maanta-app/src/lib/__tests__/notifications-visibility.test.ts`, `maanta-app/src/lib/merchant-visibility.ts`, `maanta-app/src/lib/merchant-api.ts`, `maanta-app/src/lib/data.ts` `withPublicMerchant` | founder |
| D216 | closed | code-outlier | 2026-08-28 | `maanta-app/src/app/(shopper)/notifications/page.tsx` | **Launch-mode notifications do not exclude demo deals, so a synthetic deal can notify a real shopper.** The frozen rule is that no synthetic row renders unless demo mode is on, and every other shopper read threads `includeDemo` from `isDemoModeEnabled()` and filters `is_demo` when it is false — `shops/[id]` carries the filter per-row precisely so the rule holds even if a demo deal ever hangs off a non-demo merchant. This page reads neither: it applies no `is_demo` predicate in either mode, so with demo mode OFF a synthetic deal from a favourited demo merchant still produces "New deal from a saved shop". **This is the D189 contamination shape**, where a prospect claimed a synthetic deal within ~8 hours and it landed in the non-demo count via D188 — except arriving in an inbox, which is a push toward the deal rather than a passive listing | **FIXED 2026-08-28, together with D215.** Demo predicates now follow `isDemoModeEnabled()` in **both** directions on all three reads: OFF excludes synthetic deals and synthetic merchants, ON leaves the notifications working. Both sides are filtered per D188 — `claim_deal` never sets `redemptions.is_demo`, so a claim against a synthetic merchant is tagged `false` and filtering the row alone would still render a demo shop's NAME in launch mode. Originally split from D215 by founder ruling 2026-08-28 because they are different rules with different failure modes, though they had to be implemented together. **Demo-mode behaviour must remain explicit**: with demo mode ON these notifications are correct and wanted, since the marketplace doubles as a sales-demonstration surface (founder ruling 2026-08-26); the defect is only that launch mode does not exclude them. So the fix reads `isDemoModeEnabled()` and filters on the result rather than hardcoding either answer. **Pre-launch**: demo mode must be OFF for Merchant 01's onboarding and Shopper 01's claim, and that is exactly the configuration in which this leaks. Evidence: `maanta-app/src/lib/__tests__/notifications-visibility.test.ts`, `maanta-app/src/lib/demo-mode.ts`, `docs/ops/demo-mode.md` | founder |
| D217 | closed | code-outlier | 2026-08-29 | `maanta-app/src/app/(shopper)/qr/[token]/qr-check-in.tsx`, `maanta-app/src/app/(shopper)/qr/[token]/page.tsx` | **An open `/qr/[token]` page keeps claiming "You're already checked in." and "Staff will call your name." after the `merchant_presentations` entry behind those words has lapsed.** The page derives `alreadyCheckedInFor` from a query filtered `.gt("expires_at", nowIso)` and seeds the component's state as `{ kind: "checked-in", already: true }` from it; nothing re-evaluates that afterwards. Once the queue entry lapses both sentences are false at once — the system no longer holds the shopper in the queue, so the interface has no basis for promising that staff will call them — and this is a **counter** surface, where a shopper acts on the claim by standing still and waiting. Found by the D213 sweep and **verified in source before filing**, at the founder's instruction to file only on verification. **Deliberately NOT fixed by D213's clock work**, and not semantically covered by criterion 4: `alreadyCheckedInFor` gates a mutation-capable flow rather than presentation, so decaying it on the client would re-expose the auto check-in path and could re-join the queue on the shopper's behalf while they were doing nothing. Local time alone may never declare the entry expired | **Founder ruling 2026-08-29 — the correction is constrained, and the constraints are the row.** Queue membership is **server-authoritative**. (1) Within 30 seconds after the expected lapse, the client must attempt server confirmation and stop displaying both checked-in claims. A confirmed lapse renders an expired/not-checked-in state. (2) If confirmation fails or does not complete within that bound, the client must render a neutral unable-to-confirm state—never checked-in certainty and never expired certainty. (3) Re-joining requires **explicit shopper action** and a new successful server mutation. (4) **No automatic rejoin and no client-only decay** is permitted, in any implementation. Open and **pre-launch**: it misleads a shopper standing at a counter, which is the situation Node 0 exists to observe. Tracked **separately from D213 criterion 4** on purpose — criterion 4 is about inventory a shopper might still claim, this is about a commitment the merchant has already released, and folding them together would let a refresh interval stand in for a mutation-safety rule. Next step: a founder-authorized implementation PR carrying all four constraints, with the confirmation bound and the unable-to-confirm state each proven by a failing mutant. no guard: this records-only PR deliberately does not implement the missing server-confirmation path; the implementation PR must add the behavioural guard that closes D217 → **Closed 2026-08-30:** at the expected lapse the UI first withdraws checked-in certainty, then confirms a live queue row plus its pending claim through an authenticated server endpoint. A confirmed lapse and an unavailable confirmation are distinct states; neither auto-rejoins, and only the explicit Check in again action can issue a new POST. Guards: the QR component and route suites. | founder |
| D218 | open | doc-stale | 2026-08-29 | Ledger sign contract | **The ledger's fee sign contract, founder-ratified 2026-08-29 — and a superseded instruction that said otherwise.** The PR B authorisation of 2026-08-29 specified "`success_fee` and `success_fee_arrears`: negative ledger amounts that increase displayed gross". `20260720120000_security_hardening.sql` inserts `-p_amount` for `success_fee` and **`p_amount`** for `success_fee_arrears`, and `pg_get_functiondef` on production's live `deduct_success_fee_or_record_arrears` returns the same two lines verbatim (read back 2026-08-29). The implementation followed the ledger. **The founder then ruled the earlier instruction wrong and superseded it (2026-08-29), ratifying the ledger's own contract as authoritative:** `success_fee` — negative wallet debit, increases gross; `success_fee_arrears` — positive accrued debt, increases gross; `fee_reversal` — positive, increases reversals and reduces net; `arrears_settlement` — negative debt settlement, excluded from newly generated fees. The arrears leg is positive because it accrues a debt rather than moving the wallet: the charge leg debits `account_balance`, the arrears leg increments `outstanding_arrears`. Two rows against one redemption therefore carry opposite signs, which is why sign can never classify a row and the type must. Had the uniform-sign model been implemented literally, an arrears fee would have **reduced** gross by 30 instead of increasing it by 30 — a 60 KES error per arrears fee, understating exactly the merchants who ran out of balance, which is the population the pilot watches most closely | **This row is now the contract, not an open conflict.** It stays open as the durable statement of a rule represented by `LEDGER_TYPE_CONTRACT` in TypeScript and enforced by `_fee_totals` in `20260829120000_fee_totals_contract.sql`. Any future prose restating these four signs should point here rather than repeat them, and any instruction contradicting them is superseded by this entry unless it is a new founder ruling that says so explicitly. Guards: `maanta-app/src/lib/__tests__/ledger-fee-semantics.test.ts` asserts the two billed legs hold **opposite** orientations, so a change making both negative fails CI; `maanta-app/supabase/tests/fixtures/fee-contract-cases.json` proves the SQL contract through `fee-contract-parity.test.ts` and the generated `fee_contract_cases.generated.sql`, while `ledger-fee-semantics.test.ts` pins the TypeScript sign table. Production evidence: three `success_fee` rows, all `-30.00`; no arrears, reversal or settlement row exists yet, so no live figure has ever depended on the disputed sign. no guard: the code is held to the ledger by the tests named above, but nothing in this repo can detect a sentence written elsewhere that contradicts them — which is why the contract is written down here | founder |

---

## How to add a row

1. Take the next unused ID. Never reuse one.
2. State the gap as **claim vs reality**, not as a task. "X says A, code does B" ages
   correctly; "fix X" does not.
3. If you are opening and closing it in the same change, still add the row — the
   register is the record that it was ever wrong.
4. Cite evidence as a repo-root-relative path in backticks. The test resolves it.
5. Bump `Last updated` at the top. The test checks it is not older than the newest
   row.
